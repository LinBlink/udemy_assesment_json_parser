{
    "count": 60,
    "next": null,
    "previous": null,
    "results": [
        {
            "_class": "assessment",
            "id": 136571775,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>Which statement is true about the <code>@PropertySource</code> annotation in Spring?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework - @PropertySource Documentation](https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context/annotation/PropertySource.html)",
                    "[Spring Boot Externalized Configuration](https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.external-config)"
                ],
                "feedbacks": [
                    "<p><strong>Incorrect</strong>, <code>@PropertySource</code> is not used to designate the directory of the <code>application.properties</code> file. Spring Boot automatically loads the <code>application.properties</code> or <code>application.yml</code> file from predefined locations (e.g., <code>src/main/resources</code>), and <code>@PropertySource</code> is not required for this purpose.</p>",
                    "<p><strong>Incorrect</strong>, <code>@PropertySource</code> is not used for loading <code>application.properties</code> in Spring Boot. Spring Boot automatically handles the loading of <code>application.properties</code> or <code>application.yml</code> without requiring <code>@PropertySource</code>.</p>",
                    "<p><strong>Correct</strong>, <code>@PropertySource</code> is used to load properties from an external file into the Spring <code>Environment</code>. It allows adding key-value pairs from a specified properties file to the application context.</p>",
                    "<p><strong>Incorrect</strong>, <code>@PropertySource</code> does not provide a mechanism for directly looking up or returning a single property value. Instead, it loads the entire properties file into the Spring <code>Environment</code>, and individual properties can be accessed using <code>@Value</code> or <code>Environment</code>.</p>"
                ],
                "explanation": "<p><strong>What is </strong><code><strong>@PropertySource</strong></code><strong>?</strong><br>The <code>@PropertySource</code> annotation in Spring is used to declare a properties file that should be loaded into the Spring <code>Environment</code>. It allows externalized configuration by loading key-value pairs from a specified file into the application context.</p><p><strong>Key Features of </strong><code><strong>@PropertySource</strong></code><strong>:</strong></p><ol><li><p><strong>Loading External Properties:</strong></p><ul><li><p><code>@PropertySource</code> specifies the location of a properties file to be loaded into the Spring <code>Environment</code>.</p></li></ul></li><li><p><strong>Integration with </strong><code><strong>@Value</strong></code><strong>:</strong></p><ul><li><p>Once the properties file is loaded, individual properties can be accessed using the <code>@Value</code> annotation.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">@Value(\"${my.property}\")\nprivate String myProperty;\n</pre></li></ul></li><li><p><strong>Used in Java Configuration:</strong></p><ul><li><p><code>@PropertySource</code> is typically used in Java-based configuration classes annotated with <code>@Configuration</code>.</p></li></ul></li><li><p><strong>Supports Multiple Files:</strong></p><ul><li><p>Multiple <code>@PropertySource</code> annotations can be used to load multiple properties files.</p></li></ul></li></ol><p><strong>Loading Properties with </strong><code><strong>@PropertySource</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">@Configuration\n@PropertySource(\"classpath:custom.properties\")\npublic class AppConfig {\n\n    @Value(\"${custom.property}\")\n    private String customProperty;\n\n    @Bean\n    public MyBean myBean() {\n        System.out.println(\"Custom Property: \" + customProperty);\n        return new MyBean();\n    }\n}</pre><ul><li><p><strong>Key Points in the Example:</strong></p><ul><li><p>The <code>@PropertySource</code> annotation loads the <code>custom.properties</code> file from the classpath.</p></li><li><p>The <code>@Value</code> annotation is used to inject the value of <code>custom.property</code> into the <code>customProperty</code> field.</p></li></ul></li></ul><p><strong>Contents of </strong><code><strong>custom.properties</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">custom.property=Hello, World!</pre><p><strong>Best Practices for Using </strong><code><strong>@PropertySource</strong></code><strong>:</strong></p><ol><li><p><strong>Use for Non-Standard Properties Files:</strong></p><ul><li><p>Use <code>@PropertySource</code> to load properties files that are not automatically handled by Spring Boot (e.g., files other than <code>application.properties</code> or <code>application.yml</code>).</p></li></ul></li><li><p><strong>Avoid Overusing </strong><code><strong>@PropertySource</strong></code><strong>:</strong></p><ul><li><p>For Spring Boot applications, prefer using <code>application.properties</code> or <code>application.yml</code> for configuration. Use <code>@PropertySource</code> only when necessary.</p></li></ul></li><li><p><strong>Use </strong><code><strong>@Value</strong></code><strong> or </strong><code><strong>Environment</strong></code><strong> to Access Properties:</strong></p><ul><li><p>Use the <code>@Value</code> annotation or the <code>Environment</code> object to access individual properties loaded via <code>@PropertySource</code>.</p></li></ul></li><li><p><strong>Handle Missing Files Gracefully:</strong></p><ul><li><p>Use the <code>ignoreResourceNotFound</code> attribute to avoid errors if the specified properties file is missing.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">@PropertySource(value = \"classpath:optional.properties\", ignoreResourceNotFound = true)</pre></li></ul></li></ol>",
                "answers": [
                    "<p>Used to designate the file directory of the application.properties file in a Spring Boot application</p>",
                    "<p>Used to designate the location of the application.properties file in a Spring Boot application</p>",
                    "<p>Used to add a set of name / value pairs to the Spring Environment from an external source</p>",
                    "<p>Used to easily look up and return a single property value from some external property file</p>"
                ]
            },
            "correct_response": [
                "c"
            ],
            "section": "Spring Core",
            "question_plain": "Which statement is true about the @PropertySource annotation in Spring?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571777,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are true regarding the @DataJpaTest annotation in Spring Boot?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot - Testing JPA Repositories](https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.testing.spring-boot-applications.testing-slices.data-jpa-tests)",
                    "[Spring Framework - TestEntityManager](https://docs.spring.io/spring-boot/api/java/org/springframework/boot/test/autoconfigure/orm/jpa/TestEntityManager.html)",
                    "[Baeldung - Spring Boot @DataJpaTest Annotation](https://www.baeldung.com/spring-boot-datajpatest)"
                ],
                "feedbacks": [
                    "<p>Incorrect, @DataJpaTest is specifically designed for testing JPA components and does not support NoSQL components. For NoSQL testing, other annotations like @DataMongoTest (for MongoDB) should be used.</p>",
                    "<p>Incorrect, @DataJpaTest is specifically designed for testing JPA components and does not configure JdbcTemplate. For testing JdbcTemplate, a different configuration or annotation like @JdbcTest should be used.</p>",
                    "<p>Correct, <code>@DataJpaTest</code> automatically configures a <code>TestEntityManager</code> bean, which provides a simplified API for testing JPA operations. It includes some methods provided by <code>EntityManager</code> and additional helper methods for testing.</p><ul><li><p>Example:</p><pre class=\"prettyprint linenums\">@Autowired\nprivate TestEntityManager testEntityManager;\n\n@Test\npublic void testSaveEntity() {\n    MyEntity entity = new MyEntity();\n    entity.setName(\"Test\");\n    MyEntity savedEntity = testEntityManager.persistFlushFind(entity);\n    assertEquals(\"Test\", savedEntity.getName());\n}</pre></li></ul>",
                    "<p>Incorrect, <code>TestEntityManager</code> provides only a subset of <code>EntityManager</code> methods that are useful for tests. It also includes additional helper methods for common testing tasks, but it does not provide the full functionality of <code>EntityManager</code>.</p><ul><li><p>Example of helper methods:</p><ul><li><p><code>persistFlushFind</code>: Persists an entity, flushes changes, and retrieves it from the database.</p></li><li><p><code>remove</code>: Removes an entity.</p></li></ul></li></ul>",
                    "<p><strong>Correct</strong>, if an embedded database (e.g., H2, HSQL, or Derby) is present on the classpath, Spring Boot will automatically configure it as the <code>DataSource</code> for the test. This behavior simplifies testing by providing an in-memory database for JPA operations.</p><ul><li><p>Example:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;com.h2database&lt;/groupId&gt;\n    &lt;artifactId&gt;h2&lt;/artifactId&gt;\n    &lt;scope&gt;test&lt;/scope&gt;\n&lt;/dependency&gt;</pre></li></ul>"
                ],
                "explanation": "<p><code><strong>@DataJpaTest</strong></code><strong> Annotation in Spring Boot:</strong><br>The <code>@DataJpaTest</code> annotation is a specialized test annotation in Spring Boot used for testing JPA components (e.g., repositories, entities). It provides a lightweight testing environment by configuring only the necessary components for JPA testing.</p><p><strong>Key Features of </strong><code><strong>@DataJpaTest</strong></code><strong>:</strong></p><ol><li><p><strong>Auto-Configuration:</strong></p><ul><li><p>Automatically configures JPA-related beans, such as <code>EntityManager</code>, <code>TestEntityManager</code>, and JPA repositories.</p></li><li><p>Does not load the full application context, making tests faster and more focused.</p></li></ul></li><li><p><strong>Embedded Database Support:</strong></p><ul><li><p>If an embedded database (e.g., H2, HSQL, or Derby) is present on the classpath, it will be used as the <code>DataSource</code> by default.</p></li><li><p>This simplifies testing by providing an in-memory database for JPA operations.</p></li></ul></li><li><p><strong>TestEntityManager:</strong></p><ul><li><p>A <code>TestEntityManager</code> bean is auto-configured, providing a simplified API for testing JPA operations.</p></li><li><p>It includes a subset of methods provided by <code>EntityManager</code> and additional helper methods, such as <code>persistFlushFind</code>.</p></li></ul></li><li><p><strong>Transactional Tests:</strong></p><ul><li><p>Tests annotated with <code>@DataJpaTest</code> are transactional by default, and transactions are rolled back after each test method to ensure a clean state.</p></li></ul></li><li><p><strong>Limitations:</strong></p><ul><li><p><code>@DataJpaTest</code> is specifically designed for testing JPA components and does not support NoSQL components or <code>JdbcTemplate</code>.</p></li></ul></li></ol>",
                "answers": [
                    "<p>It can be used for testing both JPA components and NoSQL components.</p>",
                    "<p>It can be used for testing JdbcTemplate</p>",
                    "<p>It auto-configures a TestEntityManager bean</p>",
                    "<p>TestEntityManager provides all methods that are provided by EntityManager and more.</p>",
                    "<p>If an embedded database is on the classpath, it will be used to configure a DataSource by default</p>"
                ]
            },
            "correct_response": [
                "c",
                "e"
            ],
            "section": "Spring Data JPA",
            "question_plain": "Which two statements are true regarding the @DataJpaTest annotation in Spring Boot?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571779,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>Spring Boot will find and load property files in which of the following?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Externalized Configuration](https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.external-config)"
                ],
                "feedbacks": [
                    "<p><strong>Incorrect</strong>, Spring Boot does not look for a properties file matching the name of the class annotated with <code>@SpringBootApplication</code>. Instead, it looks for standard property files like <code>application.properties</code> or <code>application.yml</code>.</p>",
                    "<p><strong>Correct</strong>, Spring Boot will automatically load <code>application.properties</code> or <code>application.yaml</code> from the classpath root, <code>src/main/resources</code>, or the <code>config/</code> directory. These are the primary configuration files for a Spring Boot application.</p>",
                    "<p><strong>Incorrect</strong>, Spring Boot does not look for files named <code>env.properties</code> or <code>env.yml</code> by default. These file names are not part of Spring Boot's standard configuration file locations.</p>",
                    "<p><strong>Incorrect</strong>, while Spring Boot supports a <code>config/</code> directory for configuration files, it does not look for <code>config.properties</code> or <code>config.yml</code> specifically. Instead, it looks for <code>application.properties</code> or <code>application.yml</code> in the <code>config/</code> directory.</p>"
                ],
                "explanation": "<p><strong>Default Locations for Configuration Files in Spring Boot:</strong><br>Spring Boot provides a flexible mechanism for externalized configuration. By default, it looks for configuration files named <code>application.properties</code> or <code>application.yml</code> in the following locations (in order of precedence):</p><ol><li><p><code><strong>/config</strong></code><strong> directory in the current directory:</strong></p><ul><li><p>Example: <code>./config/application.properties</code></p></li></ul></li><li><p><strong>Current directory:</strong></p><ul><li><p>Example: <code>./application.properties</code></p></li></ul></li><li><p><code><strong>config</strong></code><strong> directory in the classpath:</strong></p><ul><li><p>Example: <code>classpath:/config/application.properties</code></p></li></ul></li><li><p><strong>Classpath root:</strong></p><ul><li><p>Example: <code>classpath:/application.properties</code></p></li></ul></li></ol><p><strong>Key Points:</strong></p><ol><li><p><strong>File Names:</strong></p><ul><li><p>By default, Spring Boot looks for <code>application.properties</code> or <code>application.yml</code> files. These are the standard configuration file names.</p></li></ul></li><li><p><strong>File Locations:</strong></p><ul><li><p>Spring Boot searches for configuration files in specific locations, such as the classpath root, the <code>config/</code> directory, or the current working directory.</p></li></ul></li><li><p><strong>Custom File Names:</strong></p><ul><li><p>If you need to use a custom file name, you can specify it using the <code>spring.config.name</code> property.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">java -Dspring.config.name=myconfig -jar myapp.jar</pre></li></ul></li><li><p><strong>Custom File Locations:</strong></p><ul><li><p>You can also specify a custom location for configuration files using the <code>spring.config.location</code> property.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">java -Dspring.config.location=classpath:/custom/ -jar myapp.jar</pre></li></ul></li></ol><p><strong>Example Usage:</strong></p><p><strong>Default </strong><code><strong>application.properties</strong></code><strong> File:</strong></p><pre class=\"prettyprint linenums\">server.port=8081\nspring.datasource.url=jdbc:mysql://localhost:3306/mydb\nspring.datasource.username=root\nspring.datasource.password=secret</pre><ul><li><p><strong>Location:</strong> <code>src/main/resources/application.properties</code></p></li><li><p><strong>Purpose:</strong> Configures the server port and database connection properties.</p></li></ul><p><strong>Custom Configuration File:</strong></p><pre class=\"prettyprint linenums\">java -Dspring.config.name=customconfig -jar myapp.jar</pre><ul><li><p><strong>File Name:</strong> <code>customconfig.properties</code></p></li><li><p><strong>Location:</strong> Classpath root or current directory.</p></li></ul><p><strong>Best Practices for Spring Boot Configuration:</strong></p><ol><li><p><strong>Use </strong><code><strong>application.properties</strong></code><strong> or </strong><code><strong>application.yml</strong></code><strong>:</strong></p><ul><li><p>Stick to the standard file names unless there is a specific need for custom names.</p></li></ul></li><li><p><strong>Organize Configuration Files:</strong></p><ul><li><p>Use the <code>config/</code> directory to keep configuration files organized and separate from other resources.</p></li></ul></li><li><p><strong>Externalize Sensitive Data:</strong></p><ul><li><p>Avoid hardcoding sensitive information (e.g., passwords) in configuration files. Use environment variables or encrypted configuration files.</p></li></ul></li><li><p><strong>Leverage Profiles:</strong></p><ul><li><p>Use Spring profiles to manage environment-specific configurations (e.g., <code>application-dev.properties</code>, <code>application-prod.yml</code>).</p></li></ul></li></ol>",
                "answers": [
                    "<p>A *.properties file matching the name of the class annotated with @SpringBootApplication</p>",
                    "<p>application.properties or application.yaml, usually located in the classpath root</p>",
                    "<p>env.properties or env.yml, usually located in the classpath root</p>",
                    "<p>config.properties or config.yml, usually located in the classpath root</p>"
                ]
            },
            "correct_response": [
                "b"
            ],
            "section": "Spring Boot",
            "question_plain": "Spring Boot will find and load property files in which of the following?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571781,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>Which statement about @TestPropertySource annotation is true?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework - @TestPropertySource Documentation](https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/test/context/TestPropertySource.html)",
                    "[Spring Boot Testing Guide](https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.testing)"
                ],
                "feedbacks": [
                    "<p><strong>Incorrect</strong>, properties loaded from <code>@TestPropertySource</code> have a higher precedence than Java system properties in Spring Boot's property resolution hierarchy. This means that properties defined in <code>@TestPropertySource</code> can override Java system properties.</p>",
                    "<p><strong>Incorrect</strong>, properties defined in <code>@PropertySource</code> are still loaded into the Spring <code>Environment</code>. <code>@TestPropertySource</code> adds additional properties or overrides existing ones but does not block properties from <code>@PropertySource</code>.</p>",
                    "<p><strong>Incorrect</strong>, <code>@TestPropertySource</code> loads properties files relative to the classpath by default, not the root of the project.</p>",
                    "<p><strong>Correct</strong>, inlined properties defined using the <code>properties</code> attribute of <code>@TestPropertySource</code> take precedence over properties defined in property files. This allows you to override specific properties for testing purposes.</p>"
                ],
                "explanation": "<p><strong>What is </strong><code><strong>@TestPropertySource</strong></code><strong>?</strong><br><code>@TestPropertySource</code> is a Spring annotation used in test classes to configure property sources specifically for testing. It allows you to load properties from external files or define inlined properties that override existing properties in the Spring <code>Environment</code>.</p><p><strong>Spring Boot PropertySource Order:</strong></p><p>Spring Boot uses a well-defined <strong>PropertySource order</strong> to resolve configuration properties. Later sources in the order can override earlier ones. The following is the order of precedence for property sources in Spring Boot:</p><ol><li><p><strong>Default Properties:</strong></p><ul><li><p>Set programmatically using <code>SpringApplication.setDefaultProperties(Map)</code>.</p></li></ul></li><li><p><code><strong>@PropertySource</strong></code><strong> Annotations:</strong></p><ul><li><p>Properties defined in <code>@PropertySource</code> annotations on <code>@Configuration</code> classes.</p></li></ul></li><li><p><strong>Config Data (e.g., </strong><code><strong>application.properties</strong></code><strong> or </strong><code><strong>application.yml</strong></code><strong>):</strong></p><ul><li><p><strong>Packaged Inside the Jar:</strong></p><ul><li><p><code>application.properties</code> or <code>application.yml</code> inside your jar.</p></li><li><p>Profile-specific configuration files (e.g., <code>application-{profile}.properties</code>) inside your jar.</p></li></ul></li><li><p><strong>Outside the Jar:</strong></p><ul><li><p><code>application.properties</code> or <code>application.yml</code> outside your jar.</p></li><li><p>Profile-specific configuration files (e.g., <code>application-{profile}.properties</code>) outside your jar.</p></li></ul></li></ul></li><li><p><strong>RandomValuePropertySource:</strong></p><ul><li><p>Provides properties like <code>random.int</code>, <code>random.long</code>, etc., for generating random values.</p></li></ul></li><li><p><strong>OS Environment Variables:</strong></p><ul><li><p>Environment variables defined in the operating system.</p></li></ul></li><li><p><strong>Java System Properties:</strong></p><ul><li><p>Properties set using <code>System.getProperties()</code> (e.g., <code>-Dproperty=value</code>).</p></li></ul></li><li><p><strong>JNDI Attributes:</strong></p><ul><li><p>Properties from the JNDI context (e.g., <code>java:comp/env</code>).</p></li></ul></li><li><p><strong>Servlet Context and Config Parameters:</strong></p><ul><li><p>Properties from <code>ServletContext</code> and <code>ServletConfig</code>.</p></li></ul></li><li><p><code><strong>SPRING_APPLICATION_JSON</strong></code><strong>:</strong></p><ul><li><p>Inline JSON properties embedded in an environment variable or system property.</p></li></ul></li><li><p><strong>Command-Line Arguments:</strong></p><ul><li><p>Properties passed as command-line arguments (e.g., <code>--property=value</code>).</p></li></ul></li><li><p><strong>Properties in Tests:</strong></p><ul><li><p><code><strong>properties</strong></code><strong> Attribute in Test Annotations:</strong></p><ul><li><p>Properties defined in annotations like <code>@SpringBootTest</code>.</p></li></ul></li><li><p><code><strong>@DynamicPropertySource</strong></code><strong>:</strong></p><ul><li><p>Dynamically defined properties in tests.</p></li></ul></li><li><p><code><strong>@TestPropertySource</strong></code><strong>:</strong></p><ul><li><p>Properties loaded from <code>@TestPropertySource</code> in test classes.</p></li></ul></li></ul></li><li><p><strong>Devtools Global Settings:</strong></p><ul><li><p>Properties from the <code>$HOME/.config/spring-boot</code> directory when DevTools is active.</p></li></ul></li></ol><p><strong>Key Features of </strong><code><strong>@TestPropertySource</strong></code><strong>:</strong></p><ol><li><p><strong>Custom Property Sources for Tests:</strong></p><ul><li><p><code>@TestPropertySource</code> allows you to specify custom property files or inlined properties for use in test cases.</p></li></ul></li><li><p><strong>Relative to Classpath:</strong></p><ul><li><p>By default, properties files specified in <code>@TestPropertySource</code> are loaded relative to the classpath.</p></li></ul></li><li><p><strong>Inlined Properties:</strong></p><ul><li><p>You can define inlined properties using the <code>properties</code> attribute of <code>@TestPropertySource</code>. These properties override properties loaded from other sources.</p></li></ul></li><li><p><strong>Precedence:</strong></p><ul><li><p>Properties loaded from <code>@TestPropertySource</code> have a higher precedence than most other sources, including Java system properties.</p></li></ul></li></ol><p><strong>Example Usage:</strong></p><p><strong>Using </strong><code><strong>@TestPropertySource</strong></code><strong> to Load a Properties File:</strong></p><pre class=\"prettyprint linenums\">java@TestPropertySource(locations = \"classpath:test.properties\")\n@SpringBootTest\npublic class MyServiceTest {\n\n    @Value(\"${my.property}\")\n    private String myProperty;\n\n    @Test\n    public void testProperty() {\n        assertEquals(\"testValue\", myProperty);\n    }\n}\n</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The <code>test.properties</code> file is loaded from the classpath.</p></li><li><p>The property <code>my.property</code> is resolved from <code>test.properties</code>.</p></li></ul></li></ul><p><strong>Using Inlined Properties with </strong><code><strong>@TestPropertySource</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">java@TestPropertySource(properties = {\"my.property=inlineValue\"})\n@SpringBootTest\npublic class MyServiceTest {\n\n    @Value(\"${my.property}\")\n    private String myProperty;\n\n    @Test\n    public void testInlinedProperty() {\n        assertEquals(\"inlineValue\", myProperty);\n    }\n}\n</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The inlined property <code>my.property=inlineValue</code> overrides any other source for <code>my.property</code>.</p></li></ul></li></ul><p><strong>Best Practices for Using </strong><code><strong>@TestPropertySource</strong></code><strong>:</strong></p><ol><li><p><strong>Use for Test-Specific Properties:</strong></p><ul><li><p>Use <code>@TestPropertySource</code> to define properties that are specific to your test cases.</p></li></ul></li><li><p><strong>Prefer Inlined Properties for Simplicity:</strong></p><ul><li><p>Use the <code>properties</code> attribute for small, test-specific overrides instead of creating separate property files.</p></li></ul></li><li><p><strong>Combine with </strong><code><strong>@SpringBootTest</strong></code><strong>:</strong></p><ul><li><p>Use <code>@TestPropertySource</code> with <code>@SpringBootTest</code> to customize the environment for integration tests.</p></li></ul></li></ol>",
                "answers": [
                    "<p>Java system properties have higher precedence than the properties loaded from @TestPropertySource</p>",
                    "<p>Properties defined @PropertySource are not loaded if @TestPropertySource is used</p>",
                    "<p>@TestPropertySource annotation loads a properties file relative to the root of the project by default</p>",
                    "<p>Inlined properties defined in @TestPropertySource can be used to override properties defined in property files</p>"
                ]
            },
            "correct_response": [
                "d"
            ],
            "section": "Testing",
            "question_plain": "Which statement about @TestPropertySource annotation is true?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571783,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>Refer to the code</p><pre class=\"prettyprint linenums\">@RestController\npublic class OrderController {\n...\n@PutMapping(\"/store/orders/{id}\")\nvoid update(@PathVariable String id, @RequestBody Order order){\n    ...\n    }\n}</pre><p>How can a response status code be set for No Content (204)?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework - @ResponseStatus Documentation](https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/web/bind/annotation/ResponseStatus.html)",
                    "[Baeldung - Spring ResponseEntity](https://www.baeldung.com/spring-response-entity)",
                    "[RESTful HTTP Status Codes](https://restfulapi.net/http-status-codes/)"
                ],
                "feedbacks": [
                    "<p><strong>Correct</strong>, the <code>@ResponseStatus</code> annotation can be used to set a specific HTTP status code for a handler method. In this case, annotating the <code>update()</code> method with <code>@ResponseStatus(HttpStatus.NO_CONTENT)</code> will set the response status to <code>204 No Content</code>.</p><ul><li><p>Example:</p><pre class=\"prettyprint linenums\">@PutMapping(\"/store/orders/{id}\")\n@ResponseStatus(HttpStatus.NO_CONTENT)\nvoid update(@PathVariable String id, @RequestBody Order order) {\n    // Update logic here\n}</pre></li></ul>",
                    "<p><strong>Incorrect</strong>, the <code>update()</code> method can return <code>void</code> if the response status is explicitly set using <code>@ResponseStatus</code>. Returning a <code>ResponseEntity</code> is an alternative approach but not mandatory.</p>",
                    "<p><strong>Incorrect</strong>, the <code>@PutMapping</code> annotation does not have an attribute to set the HTTP status code. The HTTP method and URL mapping are its only responsibilities.</p>",
                    "<p><strong>Incorrect</strong>, <code>@ResponseEntity</code> is not an annotation. <code>ResponseEntity</code> is a class used to build and return HTTP responses programmatically but is not used as an annotation.</p>"
                ],
                "explanation": "<p><strong>How to Set HTTP Status Codes in Spring MVC:</strong><br>Spring MVC provides multiple ways to set the HTTP status code for a response:</p><ol><li><p><strong>Using </strong><code><strong>@ResponseStatus</strong></code><strong>:</strong></p><ul><li><p>The <code>@ResponseStatus</code> annotation can be applied to a handler method to set a specific HTTP status code for the response.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">java@ResponseStatus(HttpStatus.NO_CONTENT)\n@PutMapping(\"/store/orders/{id}\")\nvoid update(@PathVariable String id, @RequestBody Order order) {\n    // Update logic here\n}\n</pre></li></ul></li><li><p><strong>Using </strong><code><strong>ResponseEntity</strong></code><strong>:</strong></p><ul><li><p>The <code>ResponseEntity</code> class allows you to build a response programmatically, including setting the status code, headers, and body.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">java@PutMapping(\"/store/orders/{id}\")\nResponseEntity&lt;Void&gt; update(@PathVariable String id, @RequestBody Order order) {\n    // Update logic here\n    return ResponseEntity.noContent().build(); // Sets 204 No Content\n}\n</pre></li></ul></li><li><p><strong>Default Behavior:</strong></p><ul><li><p>If no status code is explicitly set, Spring will use the default status code for the HTTP method:</p><ul><li><p><code>200 OK</code> for <code>GET</code>, <code>POST</code>, <code>PUT</code>, and <code>DELETE</code>.</p></li><li><p><code>204 No Content</code> for <code>DELETE</code> methods that return <code>void</code>.</p></li></ul></li></ul></li></ol><p><strong>Example Usage:</strong></p><p><strong>Using </strong><code><strong>@ResponseStatus</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">java@RestController\npublic class OrderController {\n\n    @PutMapping(\"/store/orders/{id}\")\n    @ResponseStatus(HttpStatus.NO_CONTENT)\n    void update(@PathVariable String id, @RequestBody Order order) {\n        // Update logic here\n    }\n}\n</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The <code>@ResponseStatus(HttpStatus.NO_CONTENT)</code> annotation ensures that the response status code is <code>204 No Content</code>.</p></li><li><p>The method can return <code>void</code>, as no response body is required for a <code>204</code> response.</p></li></ul></li></ul><p><strong>Using </strong><code><strong>ResponseEntity</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">java@RestController\npublic class OrderController {\n\n    @PutMapping(\"/store/orders/{id}\")\n    ResponseEntity&lt;Void&gt; update(@PathVariable String id, @RequestBody Order order) {\n        // Update logic here\n        return ResponseEntity.noContent().build(); // Sets 204 No Content\n    }\n}\n</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The <code>ResponseEntity</code> class is used to programmatically set the response status to <code>204 No Content</code>.</p></li><li><p>This approach provides more flexibility, such as adding headers or returning a different status code based on conditions.</p></li></ul></li></ul><p><strong>Best Practices for Setting HTTP Status Codes:</strong></p><ol><li><p><strong>Use </strong><code><strong>@ResponseStatus</strong></code><strong> for Simplicity:</strong></p><ul><li><p>Use <code>@ResponseStatus</code> when the HTTP status code is fixed and does not depend on runtime conditions.</p></li></ul></li><li><p><strong>Use </strong><code><strong>ResponseEntity</strong></code><strong> for Flexibility:</strong></p><ul><li><p>Use <code>ResponseEntity</code> when you need to dynamically set the status code, add headers, or include a response body.</p></li></ul></li><li><p><strong>Follow RESTful Standards:</strong></p><ul><li><p>Return appropriate HTTP status codes based on the operation performed:</p><ul><li><p><code>200 OK</code> for successful responses with a body.</p></li><li><p><code>201 Created</code> for resource creation.</p></li><li><p><code>204 No Content</code> for successful responses without a body.</p></li><li><p><code>404 Not Found</code> for missing resources.</p></li><li><p><code>400 Bad Request</code> for invalid input.</p></li></ul></li></ul></li></ol>",
                "answers": [
                    "<p>Annotate the update() handler method with @ResponseStatus(HttpStatus.NO_CONTENT)</p>",
                    "<p>The update() handler method cannot return a void type, it must return ResponseEntity type</p>",
                    "<p>Annotate the update handler method with @PutMapping(\"/store/orders/{id}\", HttpStatus.NO_CONTENT)</p>",
                    "<p>Annotate the update() handler method with @ResponseEntity(204)</p>"
                ]
            },
            "correct_response": [
                "a"
            ],
            "section": "Spring MVC",
            "question_plain": "Refer to the code@RestController\npublic class OrderController {\n...\n@PutMapping(\"/store/orders/{id}\")\nvoid update(@PathVariable String id, @RequestBody Order order){\n    ...\n    }\n}How can a response status code be set for No Content (204)?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571785,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>Which statement describes the propagation behaviour of Propagation.REQUIRES_NEW annotation?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Understanding PROPAGATION_REQUIRES_NEW](https://docs.spring.io/spring-framework/reference/data-access/transaction/declarative/tx-propagation.html#tx-propagation-requires_new)"
                ],
                "feedbacks": [
                    "<p><strong>Incorrect</strong>, this describes the behavior of <code>Propagation.NESTED</code>, not <code>Propagation.REQUIRES_NEW</code>. <code>Propagation.NESTED</code> creates a nested transaction within the existing transaction, but it does not suspend the outer transaction.</p>",
                    "<p><strong>Incorrect</strong>, this describes the behavior of <code>Propagation.MANDATORY</code>. <code>Propagation.REQUIRES_NEW</code> does not join an existing transaction; instead, it always starts a new transaction.</p>",
                    "<p><strong>Incorrect</strong>, <code>Propagation.REQUIRES_NEW</code> does not throw an exception if an active transaction exists. Instead, it suspends the existing transaction and starts a new one.</p>",
                    "<ol><li><p><strong>Correct</strong>, <code>Propagation.REQUIRES_NEW</code> always starts a new transaction. If an active transaction exists, it is suspended for the duration of the new transaction and resumed afterward.</p></li></ol><p><br></p>"
                ],
                "explanation": "<p><strong>What is </strong><code><strong>Propagation.REQUIRES_NEW</strong></code><strong> in Spring?</strong><br>In Spring, the <code>@Transactional</code> annotation supports different propagation behaviors, which determine how transactions are managed when a method is called within the context of an existing transaction. <code>Propagation.REQUIRES_NEW</code> is one of these propagation behaviors.</p><p><strong>Behavior of </strong><code><strong>Propagation.REQUIRES_NEW</strong></code><strong>:</strong></p><ol><li><p><strong>Always Starts a New Transaction:</strong></p><ul><li><p>A new transaction is always started, regardless of whether an active transaction exists.</p></li></ul></li><li><p><strong>Suspends Existing Transactions:</strong></p><ul><li><p>If an active transaction exists, it is suspended before the new transaction starts. The existing transaction is resumed after the new transaction completes.</p></li></ul></li><li><p><strong>Independent Commit/Rollback:</strong></p><ul><li><p>The new transaction is independent of the existing transaction. Its commit or rollback does not affect the outer transaction.</p></li></ul></li><li><p><strong>No Exception for Missing Transactions:</strong></p><ul><li><p>If no active transaction exists, <code>Propagation.REQUIRES_NEW</code> simply starts a new transaction.</p></li></ul></li></ol><p><strong>Example Usage:</strong></p><p><strong>Code Example with </strong><code><strong>Propagation.REQUIRES_NEW</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">@Service\npublic class OrderService {\n\n    @Transactional\n    public void processOrder() {\n        // Outer transaction starts here\n        System.out.println(\"Processing order...\");\n        paymentService.processPayment(); // Calls a method with REQUIRES_NEW\n        System.out.println(\"Order processed.\");\n        // Outer transaction ends here\n    }\n}\n\n@Service\npublic class PaymentService {\n\n    @Transactional(propagation = Propagation.REQUIRES_NEW)\n    public void processPayment() {\n        // New transaction starts here\n        System.out.println(\"Processing payment...\");\n        // New transaction ends here\n    }\n}\n</pre><p><strong>Behavior:</strong></p><ol><li><p>The <code>processOrder()</code> method starts a transaction (outer transaction).</p></li><li><p>When <code>processPayment()</code> is called, the outer transaction is suspended, and a new transaction is started.</p></li><li><p>After <code>processPayment()</code> completes, the new transaction is committed or rolled back independently, and the outer transaction is resumed.</p></li></ol><p><strong>Best Practices for Using </strong><code><strong>Propagation.REQUIRES_NEW</strong></code><strong>:</strong></p><ol><li><p><strong>Use for Independent Operations:</strong></p><ul><li><p>Use <code>Propagation.REQUIRES_NEW</code> for operations that must be executed in their own transaction, regardless of the state of the outer transaction (e.g., logging, auditing, or sending notifications).</p></li></ul></li><li><p><strong>Avoid Overuse:</strong></p><ul><li><p>Avoid overusing <code>Propagation.REQUIRES_NEW</code>, as frequent suspension and resumption of transactions can impact performance.</p></li></ul></li><li><p><strong>Handle Exceptions Carefully:</strong></p><ul><li><p>Be cautious when handling exceptions in methods with <code>Propagation.REQUIRES_NEW</code>, as the rollback of the new transaction does not affect the outer transaction.</p></li></ul></li><li><p><strong>Monitor Transaction Boundaries:</strong></p><ul><li><p>Ensure that methods with <code>Propagation.REQUIRES_NEW</code> are properly annotated and do not unintentionally affect the outer transaction.</p></li></ul></li></ol><p><br></p>",
                "answers": [
                    "<p>Runs in a nested transaction if an active transaction exists, throws an exception if an active transaction does not exist.</p>",
                    "<p>Joins a transaction if one already exists; throws an exception if an active transaction does not exist</p>",
                    "<p>Starts a new transaction but throws an exception if an active transaction already exists</p>",
                    "<p>Starts a new transaction; If an active transaction already exists, it is suspended.</p>"
                ]
            },
            "correct_response": [
                "d"
            ],
            "section": "Spring Data",
            "question_plain": "Which statement describes the propagation behaviour of Propagation.REQUIRES_NEW annotation?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571787,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>Refer to the code</p><p><code>ClientService service = applicationContext.getBean(ClientService.class);</code></p><p>It is a Java code fragment from a Spring application. Which statement is true with regard to the above example?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework - ApplicationContext API](https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context/ApplicationContext.html)",
                    "[Baeldung - Spring getBean Examples](https://www.baeldung.com/spring-getbean)"
                ],
                "feedbacks": [
                    "<p><strong>Correct</strong>, when <code>getBean(Class&lt;T&gt; requiredType)</code> is called, Spring searches for a bean of the specified type (<code>ClientService</code> in this case) in the application context. The bean's id or name is irrelevant in this case, as the lookup is performed by type.</p>",
                    "<p><strong>Incorrect</strong>, the <code>getBean(Class&lt;T&gt; requiredType)</code> method does not require casting because it uses generics to infer the return type automatically.</p>",
                    "<p><strong>Incorrect</strong>, the <code>getBean(Class&lt;T&gt; requiredType)</code> method looks up beans by type, not by name or id. If multiple beans of the same type exist, an exception will be thrown unless further disambiguation is provided.</p>",
                    "<p><strong>Incorrect</strong>, specifying the bean id is not required when using the <code>getBean(Class&lt;T&gt; requiredType)</code> method. Spring performs the lookup based on the type of the bean.</p>"
                ],
                "explanation": "<p><strong>What is </strong><code><strong>applicationContext.getBean(Class&lt;T&gt; requiredType)</strong></code><strong>?</strong><br>The <code>getBean(Class&lt;T&gt; requiredType)</code> method is part of Spring's <code>ApplicationContext</code>. It is used to retrieve a bean from the application context by its type. This method is type-safe and does not require explicit casting.</p><p><strong>Key Features of </strong><code><strong>getBean(Class&lt;T&gt; requiredType)</strong></code><strong>:</strong></p><ol><li><p><strong>Type-Based Lookup:</strong></p><ul><li><p>The method retrieves a bean based on its type, not its name or id.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">ClientService service = applicationContext.getBean(ClientService.class);</pre></li></ul></li><li><p><strong>Automatic Type Inference:</strong></p><ul><li><p>The method uses generics to infer the return type, so no explicit casting is required.</p></li></ul></li><li><p><strong>Throws Exception for Multiple Beans:</strong></p><ul><li><p>If multiple beans of the same type exist, Spring will throw a <code>NoUniqueBeanDefinitionException</code> unless further disambiguation is provided (e.g., using qualifiers).</p></li></ul></li><li><p><strong>Throws Exception for Missing Beans:</strong></p><ul><li><p>If no bean of the specified type exists, Spring will throw a <code>NoSuchBeanDefinitionException</code>.</p></li></ul></li><li><p><strong>Alternative Methods:</strong></p><ul><li><p>You can also retrieve beans by name or id using <code>getBean(String name)</code> or by both name and type using <code>getBean(String name, Class&lt;T&gt; requiredType)</code>.</p></li></ul></li></ol><p><strong>Example Usage:</strong></p><p><strong>1. Retrieving a Bean by Type:</strong></p><pre class=\"prettyprint linenums\">@Configuration\npublic class AppConfig {\n\n    @Bean\n    public ClientService clientService() {\n        return new ClientService();\n    }\n}\n\npublic class MainApp {\n    public static void main(String[] args) {\n        ApplicationContext context = new AnnotationConfigApplicationContext(AppConfig.class);\n        ClientService service = context.getBean(ClientService.class); // Lookup by type\n        service.performAction();\n    }\n}</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The <code>getBean(Class&lt;T&gt; requiredType)</code> method retrieves the bean of type <code>ClientService</code>.</p></li><li><p>No explicit casting is required.</p></li></ul></li></ul><p><strong>2. Handling Multiple Beans of the Same Type:</strong></p><pre class=\"prettyprint linenums\">@Configuration\npublic class AppConfig {\n\n    @Bean\n    public ClientService clientService1() {\n        return new ClientService();\n    }\n\n    @Bean\n    public ClientService clientService2() {\n        return new ClientService();\n    }\n}\n\npublic class MainApp {\n    public static void main(String[] args) {\n        ApplicationContext context = new AnnotationConfigApplicationContext(AppConfig.class);\n        // This will throw NoUniqueBeanDefinitionException because multiple beans of type ClientService exist\n        ClientService service = context.getBean(ClientService.class);\n    }\n}</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>If multiple beans of the same type exist, Spring throws a <code>NoUniqueBeanDefinitionException</code>.</p></li><li><p>To resolve this, you can use qualifiers or retrieve the bean by name and type.</p></li></ul></li></ul><p><strong>3. Retrieving a Bean by Name and Type:</strong></p><pre class=\"prettyprint linenums\">ClientService service = applicationContext.getBean(\"clientService1\", ClientService.class);</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>This method retrieves the bean by both name and type, avoiding ambiguity when multiple beans of the same type exist.</p></li></ul></li></ul><p><strong>Best Practices for Using </strong><code><strong>getBean</strong></code><strong>:</strong></p><ol><li><p><strong>Prefer Dependency Injection:</strong></p><ul><li><p>Instead of manually retrieving beans using <code>getBean</code>, rely on Spring's dependency injection for better testability and maintainability.</p></li></ul></li><li><p><strong>Handle Multiple Beans:</strong></p><ul><li><p>Use qualifiers or bean names to disambiguate when multiple beans of the same type exist.</p></li></ul></li><li><p><strong>Avoid Overusing </strong><code><strong>getBean</strong></code><strong>:</strong></p><ul><li><p>Overusing <code>getBean</code> can lead to tightly coupled code. Use it only when necessary, such as in dynamic or programmatic scenarios.</p></li></ul></li><li><p><strong>Use Type-Safe Methods:</strong></p><ul><li><p>Always prefer <code>getBean(Class&lt;T&gt; requiredType)</code> over <code>getBean(String name)</code> for type safety.</p></li></ul></li></ol><p><br></p>",
                "answers": [
                    "<p>It will return a bean of the type ClientService regardless of its id or name;</p>",
                    "<p>This syntax is invalid because the result of the getBean() method call should be cast to ClientService</p>",
                    "<p>It will return a bean called ClientService regardless of its id or name</p>",
                    "<p>This syntax is invalid because the bean id must be specified as a method parameter</p>"
                ]
            },
            "correct_response": [
                "a"
            ],
            "section": "Spring Core",
            "question_plain": "Refer to the codeClientService service = applicationContext.getBean(ClientService.class);It is a Java code fragment from a Spring application. Which statement is true with regard to the above example?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571789,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Given an <code>ApplicationContext</code> containing three bean definitions of type <code>Foo</code> with bean IDs <code>foo1</code>, <code>foo2</code>, and <code>foo3</code>, which <code>@Autowired</code> scenarios are valid?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring - Using @Autowired](https://docs.spring.io/spring-framework/reference/core/beans/annotation-config/autowired.html)",
                    "[Baeldung - Guide to Spring @Autowired](https://www.baeldung.com/spring-autowire)",
                    "[Javadoc - Qualifier](https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/factory/annotation/Qualifier.html)"
                ],
                "feedbacks": [
                    "<p><strong>Correct</strong>, this is valid because <code>@Qualifier(\"foo3\")</code> explicitly specifies which bean to inject. The <code>@Qualifier</code> annotation works with <code>@Autowired</code> to resolve ambiguity when multiple beans of the same type exist.</p>",
                    "<p><strong>Correct</strong>, Spring uses the variable name (<code>foo2</code>) to resolve which bean to inject. However, this approach is <strong>not recommended</strong> because it relies on implicit behavior.</p>",
                    "<p><strong>Correct</strong>, this is valid because <code>@Qualifier(\"foo1\")</code> explicitly specifies which bean to inject into the method parameter.</p>",
                    "<p><strong>Incorrect</strong>, this is ambiguous because there are multiple beans of type <code>Foo</code> in the <code>ApplicationContext</code>. Without <code>@Qualifier</code>, Spring cannot determine which bean to inject and will throw a <code>NoUniqueBeanDefinitionException</code>.</p>",
                    "<p><strong>Incorrect</strong>, this is valid only if there is exactly one bean of type <code>Foo</code> in the <code>ApplicationContext</code>. However, if there are multiple beans of the same type (as in this case), this will result in a <code>NoUniqueBeanDefinitionException</code>. To make this valid, you would need to use <code>@Qualifier</code> to specify the bean.</p>",
                    "<p><strong>Correct</strong>, Spring uses the parameter name (<code>foo2</code>) to resolve which bean to inject. However, this approach is <strong>not recommended</strong> because it relies on implicit behavior.</p>"
                ],
                "explanation": "<p><strong>Key Concepts:</strong></p><ol><li><p><code><strong>@Autowired</strong></code><strong> – Dependency Injection by Type:</strong></p><ul><li><p><code>@Autowired</code> is a Spring annotation that allows automatic dependency injection into fields, constructors, or setter methods.</p></li><li><p>By default, Spring resolves dependencies based on the <strong>type</strong> of the bean. If there is exactly one bean of the required type in the <code>ApplicationContext</code>, it will be injected.</p></li><li><p>If there are <strong>multiple beans of the same type</strong>, Spring will throw a <code>NoUniqueBeanDefinitionException</code> unless:</p><ul><li><p>You use the <code>@Qualifier</code> annotation to explicitly specify which bean to inject.</p></li><li><p>The variable name or method parameter name matches the bean name.</p></li></ul></li></ul></li></ol><ol><li><p><code><strong>@Qualifier</strong></code><strong> – Explicitly Specifying the Bean:</strong></p><ul><li><p><code>@Qualifier</code> allows you to explicitly specify which bean to inject when multiple beans of the same type exist.</p></li><li><p><code>@Qualifier</code> can be used together with <code>@Autowired</code> in fields, constructors, and methods.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">java@Autowired\n@Qualifier(\"foo1\")\nprivate Foo foo; // Injects the bean with ID \"foo1\"\n</pre></li></ul></li></ol><ol><li><p><strong>Variable and Parameter Names – Implicit Matching:</strong></p><ul><li><p>If there are multiple beans of the same type, Spring can use the <strong>name of the variable</strong> (for fields) or the <strong>name of the parameter</strong> (for methods) to resolve which bean to inject.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">java@Autowired\nprivate Foo foo2; // Injects the bean with ID \"foo2\", if it exists\n</pre><pre class=\"prettyprint linenums\">java@Autowired\npublic void setFoo(Foo foo3) {\n    this.foo = foo3; // Injects the bean with ID \"foo3\", if it exists\n}\n</pre></li><li><p><strong>NOTE:</strong> Relying on variable or parameter names is <strong>not recommended</strong>, as it introduces implicit dependencies that can be harder to maintain. Instead, use <code>@Qualifier</code> to explicitly specify the bean name.</p></li></ul></li></ol><ol><li><p><strong>Spring Behavior with Multiple Beans of the Same Type:</strong></p><ul><li><p>If there are multiple beans of the same type and:</p><ul><li><p><strong>You do not use </strong><code><strong>@Qualifier</strong></code><strong> or matching variable/parameter names:</strong> Spring will throw a <code>NoUniqueBeanDefinitionException</code>.</p></li><li><p><strong>The variable/parameter name matches a bean name:</strong> Spring will inject the matching bean.</p></li><li><p><strong>You use </strong><code><strong>@Qualifier</strong></code><strong>:</strong> Spring will inject the bean specified in the <code>@Qualifier</code>.</p></li></ul></li></ul></li></ol><ol><li><p><strong>Best Practices:</strong></p><ul><li><p><strong>Always Use </strong><code><strong>@Qualifier</strong></code><strong>:</strong> Explicitly specify which bean to inject to avoid ambiguity and improve code readability.</p></li><li><p><strong>Avoid Relying on Variable/Parameter Names:</strong> While Spring can use variable or parameter names to resolve beans, this is not recommended as it introduces implicit behavior.</p></li><li><p><strong>Ensure Unique Bean Types:</strong> If possible, avoid defining multiple beans of the same type unless necessary.</p></li></ul></li></ol><p><strong>Example Scenarios:</strong></p><ol><li><p><strong>Injection with </strong><code><strong>@Qualifier</strong></code><strong>:</strong></p><ul><li><p>If you have three beans of type <code>Foo</code> with names <code>foo1</code>, <code>foo2</code>, and <code>foo3</code>, you can use <code>@Qualifier</code> to explicitly specify which bean to inject:</p><pre class=\"prettyprint linenums\">java@Autowired\n@Qualifier(\"foo1\")\nprivate Foo foo; // Injects the bean with ID \"foo1\"\n</pre></li></ul></li><li><p><strong>Injection with Variable Name Matching:</strong></p><ul><li><p>If the variable name matches the bean name, Spring will inject the matching bean:</p><pre class=\"prettyprint linenums\">java@Autowired\nprivate Foo foo2; // Injects the bean with ID \"foo2\"\n</pre></li></ul></li><li><p><strong>Injection with Parameter Name Matching:</strong></p><ul><li><p>If the parameter name of a method matches the bean name, Spring will inject the matching bean:</p><pre class=\"prettyprint linenums\">java@Autowired\npublic void setFoo(Foo foo3) {\n    this.foo = foo3; // Injects the bean with ID \"foo3\"\n}\n</pre></li></ul></li><li><p><strong>Injection Without </strong><code><strong>@Qualifier</strong></code><strong> or Matching Names:</strong></p><ul><li><p>If you do not use <code>@Qualifier</code> or matching variable/parameter names, and there are multiple beans of the same type, Spring will throw an exception:</p><pre class=\"prettyprint linenums\">java@Autowired\nprivate Foo foo; // NoUniqueBeanDefinitionException\n</pre></li></ul></li></ol><p><strong>Summary:</strong></p><ul><li><p><code><strong>@Autowired</strong></code> injects dependencies based on the <strong>type</strong> of the bean.</p></li><li><p>If there are multiple beans of the same type:</p><ul><li><p>Use <code><strong>@Qualifier</strong></code> to explicitly specify which bean to inject.</p></li><li><p>Spring can use <strong>variable names</strong> (for fields) or <strong>parameter names</strong> (for methods) to resolve beans, but this is <strong>not recommended</strong>.</p></li></ul></li><li><p><strong>Best Practices:</strong></p><ul><li><p>Always use <code>@Qualifier</code> to ensure clarity and avoid ambiguity.</p></li><li><p>Avoid relying on variable/parameter names to resolve beans, as this introduces implicit dependencies.</p></li></ul></li></ul><p><br></p>",
                "answers": [
                    "<p>@Autowired @Qualifier(\"foo3\") Foo foo;</p>",
                    "<p>@Autowired private Foo foo2;</p>",
                    "<p>@Autowired public void setFoo(@Qualifier(\"foo1\") Foo foo) {...}</p>",
                    "<p>@Autowired public void setFoo(Foo foo) {...}</p>",
                    "<p>@Autowired private Foo foo;</p>",
                    "<p>@Autowired public void setFoo(Foo foo2) {...}</p>"
                ]
            },
            "correct_response": [
                "a",
                "c",
                "f",
                "b"
            ],
            "section": "Spring Core",
            "question_plain": "Given an ApplicationContext containing three bean definitions of type Foo with bean IDs foo1, foo2, and foo3, which @Autowired scenarios are valid?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571791,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>In which three ways are Security filters used in Spring Security?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Security Reference - Filter Chain](https://docs.spring.io/spring-security/reference/servlet/architecture.html#servlet-securityfilterchain)",
                    "[Baeldung - Spring Security Filters](https://www.baeldung.com/spring-security-custom-filter)"
                ],
                "feedbacks": [
                    "<p><strong>Incorrect</strong>, security filters are not responsible for managing application users. User management is typically handled by a <code>UserDetailsService</code> or an external identity provider, not by security filters.</p>",
                    "<p><strong>Correct</strong>, security filters are used to drive the authentication process by intercepting requests, extracting credentials (e.g., username and password), and passing them to an <code>AuthenticationManager</code> for validation.</p>",
                    "<p><strong>Correct</strong>, Spring Security includes filters like <code>LogoutFilter</code> to handle logout functionality, such as invalidating sessions and clearing authentication data.</p>",
                    "<p><strong>Correct</strong>, security filters enforce authorization by checking whether the authenticated user has the required permissions or roles to access a specific resource. This is typically handled by filters like <code>FilterSecurityInterceptor</code>.</p>",
                    "<p><strong>Incorrect</strong>, security filters in Spring Security are not designed to provide risk governance. Risk governance is a broader concept that involves policies, procedures, and tools beyond the scope of security filters.</p>",
                    "<p><strong>Incorrect</strong>, security filters do not handle data encryption. Encryption is typically handled at the transport layer (e.g., HTTPS) or by specific cryptographic libraries, not by Spring Security filters.</p>"
                ],
                "explanation": "<p><strong>What are Security Filters in Spring Security?</strong><br>Spring Security uses a chain of filters to secure web applications. These filters intercept HTTP requests and perform various security-related tasks, such as authentication, authorization, and session management. Each filter in the chain has a specific responsibility and works together to enforce security policies.</p><p><strong>Key Responsibilities of Security Filters:</strong></p><ol><li><p><strong>Authentication:</strong></p><ul><li><p>Filters like <code>UsernamePasswordAuthenticationFilter</code> and <code>BasicAuthenticationFilter</code> handle authentication by extracting credentials from the request and validating them using an <code>AuthenticationManager</code>.</p></li></ul></li><li><p><strong>Authorization:</strong></p><ul><li><p>Filters like <code>FilterSecurityInterceptor</code> enforce access control by checking whether the authenticated user has the required permissions or roles to access a resource.</p></li></ul></li><li><p><strong>Logout:</strong></p><ul><li><p>Filters like <code>LogoutFilter</code> handle logout functionality by invalidating sessions, clearing authentication data, and redirecting users to a logout success URL.</p></li></ul></li><li><p><strong>Session Management:</strong></p><ul><li><p>Filters like <code>SessionManagementFilter</code> ensure that session-related security policies are enforced, such as preventing session fixation attacks.</p></li></ul></li><li><p><strong>CSRF Protection:</strong></p><ul><li><p>Filters like <code>CsrfFilter</code> protect against Cross-Site Request Forgery (CSRF) attacks by validating CSRF tokens in state-changing requests.</p></li></ul></li><li><p><strong>Request Validation:</strong></p><ul><li><p>Filters like <code>SecurityContextPersistenceFilter</code> ensure that security context information (e.g., authentication details) is preserved across requests.</p></li></ul></li></ol><p><strong>Example Usage:</strong></p><p><strong>1. Authentication with </strong><code><strong>UsernamePasswordAuthenticationFilter</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">java@Bean\npublic SecurityFilterChain filterChain(HttpSecurity http) throws Exception {\n    http\n        .authorizeRequests()\n        .anyRequest().authenticated()\n        .and()\n        .formLogin(); // Enables UsernamePasswordAuthenticationFilter\n    return http.build();\n}</pre><ul><li><p>This configuration enables <code>UsernamePasswordAuthenticationFilter</code> to handle form-based login authentication.</p></li></ul><p><strong>2. Authorization with </strong><code><strong>FilterSecurityInterceptor</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">java@Bean\npublic SecurityFilterChain filterChain(HttpSecurity http) throws Exception {\n    http\n        .authorizeRequests()\n        .antMatchers(\"/admin/**\").hasRole(\"ADMIN\") // Enforces access control\n        .anyRequest().authenticated();\n    return http.build();\n}</pre><ul><li><p>This configuration uses <code>FilterSecurityInterceptor</code> to enforce access control for specific URL patterns.</p></li></ul><p><strong>3. Logout with </strong><code><strong>LogoutFilter</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">java@Bean\npublic SecurityFilterChain filterChain(HttpSecurity http) throws Exception {\n    http\n        .logout()\n        .logoutUrl(\"/logout\")\n        .logoutSuccessUrl(\"/login?logout\")\n        .invalidateHttpSession(true)\n        .clearAuthentication(true);\n    return http.build();\n}</pre><ul><li><p>This configuration enables <code>LogoutFilter</code> to handle logout functionality.</p></li></ul><p><strong>Best Practices for Security Filters:</strong></p><ol><li><p><strong>Understand the Filter Chain:</strong></p><ul><li><p>Learn how Spring Security's filter chain works and the responsibilities of each filter.</p></li></ul></li><li><p><strong>Customize as Needed:</strong></p><ul><li><p>Customize the filter chain only when necessary. Use Spring Security's default configuration for common use cases.</p></li></ul></li><li><p><strong>Avoid Overloading Filters:</strong></p><ul><li><p>Do not overload filters with additional responsibilities. Keep them focused on their specific tasks.</p></li></ul></li><li><p><strong>Use HTTPS for Encryption:</strong></p><ul><li><p>Rely on HTTPS for transport-layer encryption instead of trying to implement encryption in application-level filters.</p></li></ul></li><li><p><strong>Test Security Configuration:</strong></p><ul><li><p>Regularly test your security configuration to ensure that the filters are working as expected.</p></li></ul></li></ol><p><br></p>",
                "answers": [
                    "<p>To manage application users</p>",
                    "<p>To drive authentication</p>",
                    "<p>To provide a logout capability</p>",
                    "<p>To enforce authorization (access control)</p>",
                    "<p>To provide risk governance</p>",
                    "<p>To encrypt data</p>"
                ]
            },
            "correct_response": [
                "b",
                "c",
                "d"
            ],
            "section": "Spring Security",
            "question_plain": "In which three ways are Security filters used in Spring Security?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571793,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<pre class=\"prettyprint linenums\">@Configuration\npublic class MyConfig {\n\n    @Bean\n    public AccountRepository accountRepository(){\n        return new JdbcAccountRepository();\n    }\n\n    @Bean\n    public TransferService transferService(){\n        TransferServiceImpl service = new TransferServiceImpl();\n        service.setAccountRepository(accountRepository());\n        return service;\n    }\n\n    @Bean\n    public AccountService accountService(){\n        return new AccountServiceImpl(accountRepository());\n    }\n}</pre><p>Based on the default Spring behavior, choose the correct answer:</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework - @Bean Annotation](https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context/annotation/Bean.html)",
                    "[Baeldung - Spring @Configuration and @Bean](https://www.baeldung.com/spring-bean)"
                ],
                "feedbacks": [
                    "<p><strong>Correct</strong>, by default, Spring beans are singleton-scoped. Even though the <code>accountRepository()</code> method is called multiple times, Spring ensures that only one instance of the <code>AccountRepository</code> bean is created and shared across the application context.</p>",
                    "<p><strong>Incorrect</strong>, Spring ensures that only one instance of a singleton-scoped bean is created, regardless of how many times the <code>@Bean</code> method is called. The calls to <code>accountRepository()</code> are intercepted by Spring's proxy mechanism, which ensures the same instance is returned.</p>",
                    "<p><strong>Incorrect</strong>, while the <code>accountRepository()</code> method is called multiple times, Spring ensures that the same singleton instance is returned each time, so only one bean is instantiated.</p>",
                    "<p><strong>Incorrect</strong>, Spring's default behavior for singleton-scoped beans ensures that only one instance of the <code>AccountRepository</code> bean is created, regardless of how many times the method is called.</p>"
                ],
                "explanation": "<p><strong>How Spring Handles </strong><code><strong>@Bean</strong></code><strong> Methods:</strong><br>In Spring, methods annotated with <code>@Bean</code> are managed by the Spring container. When a method annotated with <code>@Bean</code> is called, Spring intercepts the call and ensures that the same instance of the bean is returned, provided the bean's scope is <code>singleton</code> (which is the default scope).</p><p><strong>Key Points:</strong></p><ol><li><p><strong>Singleton Scope (Default):</strong></p><ul><li><p>By default, beans in Spring are singleton-scoped, meaning only one instance of the bean is created and shared across the application context.</p></li><li><p>Even if the <code>@Bean</code> method is called multiple times, Spring ensures that the same instance is returned.</p></li></ul></li><li><p><strong>Intercepted Method Calls:</strong></p><ul><li><p>When a <code>@Bean</code> method is called within the same configuration class, Spring intercepts the call and returns the existing bean instance instead of creating a new one.</p></li></ul></li><li><p><strong>Why Only One Instance is Created:</strong></p><ul><li><p>Spring uses a proxy mechanism for <code>@Configuration</code> classes. When a method annotated with <code>@Bean</code> is called, the proxy checks whether the bean has already been created. If it has, the existing instance is returned; otherwise, a new instance is created.</p></li></ul></li></ol><p><strong>Example to Illustrate:</strong></p><p><strong>Configuration Class:</strong></p><pre class=\"prettyprint linenums\">java@Configuration\npublic class MyConfig {\n\n    @Bean\n    public AccountRepository accountRepository() {\n        System.out.println(\"Creating AccountRepository bean...\");\n        return new JdbcAccountRepository();\n    }\n\n    @Bean\n    public TransferService transferService() {\n        TransferServiceImpl service = new TransferServiceImpl();\n        service.setAccountRepository(accountRepository());\n        return service;\n    }\n\n    @Bean\n    public AccountService accountService() {\n        return new AccountServiceImpl(accountRepository());\n    }\n}</pre><p><strong>Main Application:</strong></p><pre class=\"prettyprint linenums\">javapublic class MainApp {\n    public static void main(String[] args) {\n        AnnotationConfigApplicationContext context = new AnnotationConfigApplicationContext(MyConfig.class);\n\n        TransferService transferService = context.getBean(TransferService.class);\n        AccountService accountService = context.getBean(AccountService.class);\n\n        context.close();\n    }\n}</pre><p><strong>Output:</strong></p><pre class=\"prettyprint linenums\">Creating AccountRepository bean...</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The <code>accountRepository()</code> method is called multiple times (once in <code>transferService()</code> and once in <code>accountService()</code>), but the output shows that the <code>AccountRepository</code> bean is created only once.</p></li><li><p>This demonstrates that Spring ensures a single instance of the bean is created and shared.</p></li></ul></li></ul><p><strong>Best Practices for </strong><code><strong>@Bean</strong></code><strong> Methods:</strong></p><ol><li><p><strong>Rely on Spring's Singleton Behavior:</strong></p><ul><li><p>Trust Spring's default behavior to manage singleton-scoped beans. Avoid manually caching or reusing bean instances.</p></li></ul></li><li><p><strong>Use Dependency Injection:</strong></p><ul><li><p>Let Spring handle dependency injection instead of manually calling <code>@Bean</code> methods within your configuration class.</p></li></ul></li><li><p><strong>Avoid Circular Dependencies:</strong></p><ul><li><p>Be cautious when calling <code>@Bean</code> methods within the same configuration class to avoid circular dependencies.</p></li></ul></li><li><p><strong>Understand Bean Scopes:</strong></p><ul><li><p>If you need multiple instances of a bean, explicitly configure the bean with a prototype scope using <code>@Scope(\"prototype\")</code>.</p></li></ul></li></ol><p><br></p>",
                "answers": [
                    "<p>One AccountRepository bean will be instantiated since the default scope is singleton.</p>",
                    "<p>Many AccountRepository beans will be instantiated, depending how often accountRepository(), transferService() and accountService() are called</p>",
                    "<p>Two AccountRepository beans will be instantiated as the accountRepository() method will be called two times.</p>",
                    "<p>Three AccountRepository beans will be instantiated as the accountRepository() method will be called three times.</p>"
                ]
            },
            "correct_response": [
                "a"
            ],
            "section": "Spring Core",
            "question_plain": "@Configuration\npublic class MyConfig {\n\n    @Bean\n    public AccountRepository accountRepository(){\n        return new JdbcAccountRepository();\n    }\n\n    @Bean\n    public TransferService transferService(){\n        TransferServiceImpl service = new TransferServiceImpl();\n        service.setAccountRepository(accountRepository());\n        return service;\n    }\n\n    @Bean\n    public AccountService accountService(){\n        return new AccountServiceImpl(accountRepository());\n    }\n}Based on the default Spring behavior, choose the correct answer:",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571795,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Refer to the code</p><pre class=\"prettyprint linenums\">@Bean\n@ConditionalOnBean(name =\"dataSource\")\npublic JdbcTemplate jdbcTemplate(DataSource dataSource){\n    return new JdbcTemplate(dataSource);\n}</pre><p>The above code shows a conditional @Bean method for the creation of a JdbcTemplate bean.</p><p>Which two statements correctly describe the code behavior?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot - @ConditionalOnBean Documentation](https://docs.spring.io/spring-boot/docs/current/api/org/springframework/boot/autoconfigure/condition/ConditionalOnBean.html)",
                    "[Spring - Condition Annotations](https://docs.spring.io/spring-boot/reference/features/developing-auto-configuration.html#features.developing-auto-configuration.condition-annotations)"
                ],
                "feedbacks": [
                    "<p><strong>Incorrect</strong>, replacing <code>@ConditionalOnBean</code> with <code>@ConditionalOnMissingBean</code> would change the behavior entirely. <code>@ConditionalOnMissingBean</code> ensures that the bean is created only if a <code>DataSource</code> bean does not already exist, which is the opposite of the current behavior.</p>",
                    "<p><strong>Correct</strong>, using <code>@ConditionalOnBean(DataSource.class)</code> checks for the presence of a <code>DataSource</code> bean by type rather than by name. This is generally more flexible and avoids issues caused by bean name mismatches.</p>",
                    "<p><strong>Incorrect</strong>, <code>@ConditionalOnBean</code> requires that a bean named <code>dataSource</code> must already exist in the application context. The presence of the <code>DataSource</code> class in the classpath alone is not sufficient.</p>",
                    "<p><strong>Correct</strong>, the <code>@ConditionalOnBean(name = \"dataSource\")</code> annotation ensures that the <code>JdbcTemplate</code> bean is created only if a bean named <code>dataSource</code> already exists in the application context.</p>",
                    "<p><strong>Incorrect</strong>, the <code>@Bean</code> annotation is required to define the <code>JdbcTemplate</code> bean. Removing it would prevent the bean from being registered in the application context.</p>"
                ],
                "explanation": "<p><strong>What is </strong><code><strong>@ConditionalOnBean</strong></code><strong>?</strong><br>The <code>@ConditionalOnBean</code> annotation is used to conditionally register a bean in the Spring application context based on the presence of one or more specific beans. It ensures that the annotated <code>@Bean</code> method or configuration class is only processed if the specified bean(s) exist.</p><p><strong>Key Features of </strong><code><strong>@ConditionalOnBean</strong></code><strong>:</strong></p><ol><li><p><strong>Bean Presence Check:</strong></p><ul><li><p>The annotation checks for the presence of a specific bean in the application context.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">@ConditionalOnBean(name = \"dataSource\")</pre></li></ul></li><li><p><strong>Name vs. Type:</strong></p><ul><li><p>You can specify the bean by name (<code>name = \"beanName\"</code>) or by type (<code>value = BeanClass.class</code>).</p></li></ul></li><li><p><strong>Common Use Case:</strong></p><ul><li><p>It is commonly used in auto-configuration classes to conditionally register beans based on the presence of other beans.</p></li></ul></li></ol><p><strong>Example Usage:</strong></p><p><strong>1. Using </strong><code><strong>@ConditionalOnBean</strong></code><strong> with a Bean Name:</strong></p><pre class=\"prettyprint linenums\">java@Bean\n@ConditionalOnBean(name = \"dataSource\")\npublic JdbcTemplate jdbcTemplate(DataSource dataSource) {\n    return new JdbcTemplate(dataSource);\n}</pre><ul><li><p><strong>Behavior:</strong></p><ul><li><p>The <code>JdbcTemplate</code> bean is created only if a bean named <code>dataSource</code> exists in the application context.</p></li></ul></li></ul><p><strong>2. Using </strong><code><strong>@ConditionalOnBean</strong></code><strong> with a Bean Type:</strong></p><pre class=\"prettyprint linenums\">java@Bean\n@ConditionalOnBean(DataSource.class)\npublic JdbcTemplate jdbcTemplate(DataSource dataSource) {\n    return new JdbcTemplate(dataSource);\n}</pre><ul><li><p><strong>Behavior:</strong></p><ul><li><p>The <code>JdbcTemplate</code> bean is created if any <code>DataSource</code> bean exists in the application context, regardless of its name.</p></li></ul></li></ul><p><strong>3. Using </strong><code><strong>@ConditionalOnMissingBean</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">java@Bean\n@ConditionalOnMissingBean(DataSource.class)\npublic DataSource defaultDataSource() {\n    return new HikariDataSource(); // Default DataSource\n}</pre><ul><li><p><strong>Behavior:</strong></p><ul><li><p>The <code>defaultDataSource</code> bean is created only if no <code>DataSource</code> bean exists in the application context.</p></li></ul></li></ul><p><strong>Best Practices for Conditional Beans:</strong></p><ol><li><p><strong>Prefer Type-Based Conditions:</strong></p><ul><li><p>Use <code>@ConditionalOnBean(Class)</code> instead of <code>@ConditionalOnBean(name = \"beanName\")</code> to avoid issues caused by bean name mismatches.</p></li></ul></li><li><p><strong>Use </strong><code><strong>@ConditionalOnMissingBean</strong></code><strong> for Defaults:</strong></p><ul><li><p>Use <code>@ConditionalOnMissingBean</code> to define default beans that should only be created if no other bean of the same type exists.</p></li></ul></li><li><p><strong>Document Conditional Logic:</strong></p><ul><li><p>Clearly document the conditions under which a bean will be created to avoid confusion for other developers.</p></li></ul></li><li><p><strong>Avoid Overusing Conditions:</strong></p><ul><li><p>Use conditional annotations judiciously to keep the configuration simple and maintainable.</p></li></ul></li></ol><p><br></p>",
                "answers": [
                    "<p>@ConditionalOnBean(name=\"dataSource\") should be replaced with @ConditionalOnMissingBean(DataSource.class) for greater flexibility</p>",
                    "<p>@ConditionalOnBean(name=\"dataSource\") should be replaced with @ConditionalOnBean(DataSource.class) for greater flexibility</p>",
                    "<p>A JdbcTemplate bean will be created when the DataSource class is in the classpath but there is no DataSource bean.</p>",
                    "<p>A JdbcTemplate bean will be created when a bean named dataSource has already been created.</p>",
                    "<p>The @Bean annotation should be removed</p>"
                ]
            },
            "correct_response": [
                "b",
                "d"
            ],
            "section": "Spring Boot",
            "question_plain": "Refer to the code@Bean\n@ConditionalOnBean(name =\"dataSource\")\npublic JdbcTemplate jdbcTemplate(DataSource dataSource){\n    return new JdbcTemplate(dataSource);\n}The above code shows a conditional @Bean method for the creation of a JdbcTemplate bean.Which two statements correctly describe the code behavior?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571797,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<pre class=\"prettyprint linenums\">public class ClientServiceImpl implements ClientService {\n\n    @Transactional(propagation=Propagation.REQUIRED)\n    public void update1(){\n        update2();\n    }\n\n    @Transactional(propagation=Propagation.REQUIRES_NEW)\n    public void update2(){\n\n    }\n}</pre><p>Assume that the application is using Spring transaction management which uses Spring AOP internally. Choose the statement that describes what is happening when the update1 method is called?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework - Transaction Management](https://docs.spring.io/spring-framework/docs/current/reference/html/data-access.html#transaction-declarative)",
                    "[Spring Framework - Transaction Propagation](https://docs.spring.io/spring-framework/reference/data-access/transaction/declarative/tx-propagation.html)"
                ],
                "feedbacks": [
                    "<p><strong>Correct</strong>, since <code>update2()</code> is called directly from within the same class, the call does not go through the Spring AOP proxy. As a result, the <code>@Transactional</code> annotation on <code>update2()</code> is ignored, and <code>update2()</code> executes within the same transaction initiated by <code>update1()</code>.</p>",
                    "<p><strong>Incorrect</strong>, while <code>Propagation.REQUIRES_NEW</code> normally starts a new transaction, it requires the method call to go through the Spring AOP proxy. Since <code>update2()</code> is called directly from <code>update1()</code> within the same class, the proxy is bypassed, and no new transaction is started.</p>",
                    "<p><strong>Incorrect</strong>, no exception is thrown. Spring allows nested transactions or new transactions, depending on the propagation behavior. However, in this case, <code>update2()</code> does not start a new transaction because the proxy is bypassed.</p>",
                    "<p><strong>Incorrect</strong>, <code>Propagation.REQUIRES_NEW</code> does not use an existing transaction. It always suspends the current transaction and starts a new one. However, in this case, <code>update2()</code> does not start a new transaction because it is called directly and bypasses the proxy.</p>"
                ],
                "explanation": "<p><strong>How Spring Transaction Management Works with AOP:</strong><br>Spring transaction management relies on proxies created by Spring AOP. These proxies intercept method calls and apply transactional behavior based on the annotations (e.g., <code>@Transactional</code>) and their configuration. For the transactional behavior to be applied, the method call must go through the proxy.</p><p><strong>Key Points About the Code:</strong></p><ol><li><p><strong>Direct Method Calls Bypass the Proxy:</strong></p><ul><li><p>In the code, <code>update2()</code> is called directly from <code>update1()</code> within the same class. This direct call bypasses the Spring AOP proxy, so the <code>@Transactional</code> annotation on <code>update2()</code> is ignored.</p></li></ul></li><li><p><strong>Effect of </strong><code><strong>Propagation.REQUIRES_NEW</strong></code><strong>:</strong></p><ul><li><p>Normally, <code>Propagation.REQUIRES_NEW</code> suspends the current transaction (if any) and starts a new transaction. However, this behavior only applies if the method is called through the proxy.</p></li></ul></li><li><p><strong>Single Transaction:</strong></p><ul><li><p>Since the proxy is bypassed, the <code>@Transactional</code> annotation on <code>update2()</code> has no effect. Both <code>update1()</code> and <code>update2()</code> execute within the same transaction initiated by <code>update1()</code>.</p></li></ul></li></ol><p><strong>Example to Illustrate the Behavior:</strong></p><p><strong>Code Example:</strong></p><pre class=\"prettyprint linenums\">java@Service\npublic class ClientServiceImpl implements ClientService {\n\n    @Transactional(propagation = Propagation.REQUIRED)\n    public void update1() {\n        System.out.println(\"update1: Transaction started\");\n        update2();\n        System.out.println(\"update1: Transaction ended\");\n    }\n\n    @Transactional(propagation = Propagation.REQUIRES_NEW)\n    public void update2() {\n        System.out.println(\"update2: Transaction started\");\n        System.out.println(\"update2: Transaction ended\");\n    }\n}\n</pre><p><strong>Main Application:</strong></p><pre class=\"prettyprint linenums\">java@SpringBootApplication\npublic class MainApp {\n\n    public static void main(String[] args) {\n        ApplicationContext context = SpringApplication.run(MainApp.class, args);\n        ClientService clientService = context.getBean(ClientService.class);\n\n        clientService.update1();\n    }\n}\n</pre><p><strong>Output:</strong></p><pre class=\"prettyprint linenums\">update1: Transaction started\nupdate2: Transaction started\nupdate2: Transaction ended\nupdate1: Transaction ended</pre><p><strong>Key Points:</strong></p><ul><li><p>The <code>update2()</code> method is called directly from <code>update1()</code>, so it does not go through the proxy.</p></li><li><p>The <code>@Transactional</code> annotation on <code>update2()</code> is ignored, and both methods execute within the same transaction.</p></li></ul><p><strong>Best Practices for Transaction Management:</strong></p><ol><li><p><strong>Avoid Direct Method Calls:</strong></p><ul><li><p>Avoid calling transactional methods directly from within the same class. Instead, call them from another bean or use <code>self-injection</code> to ensure the call goes through the proxy.</p></li></ul><pre class=\"prettyprint linenums\">java@Autowired\nprivate ClientService self;\n\n@Transactional(propagation = Propagation.REQUIRED)\npublic void update1() {\n    self.update2(); // Ensures the call goes through the proxy\n}\n</pre></li><li><p><strong>Understand Proxy Limitations:</strong></p><ul><li><p>Be aware of how Spring AOP proxies work and their limitations, such as the inability to intercept direct method calls.</p></li></ul></li><li><p><strong>Use AspectJ for Advanced Scenarios:</strong></p><ul><li><p>If you need transactional behavior for direct method calls, consider using AspectJ instead of Spring AOP. AspectJ weaves the transactional behavior directly into the bytecode.</p></li></ul></li><li><p><strong>Test Transactional Behavior:</strong></p><ul><li><p>Test your transactional configuration to ensure that the expected behavior (e.g., propagation, rollback) is applied correctly.</p></li></ul></li></ol><p><br></p>",
                "answers": [
                    "<p>There is only one transaction initiated by update1() because the call to update2() does not go through the proxy</p>",
                    "<p>There are 2 transactions because REQUIRES_NEW always run in a new transaction</p>",
                    "<p>An exception is thrown as another transaction cannot be started within an existing transaction</p>",
                    "<p>There is only one transaction because REQUIRES_NEW will use an active transaction if one already exists.</p>"
                ]
            },
            "correct_response": [
                "a"
            ],
            "section": "Spring Data",
            "question_plain": "public class ClientServiceImpl implements ClientService {\n\n    @Transactional(propagation=Propagation.REQUIRED)\n    public void update1(){\n        update2();\n    }\n\n    @Transactional(propagation=Propagation.REQUIRES_NEW)\n    public void update2(){\n\n    }\n}Assume that the application is using Spring transaction management which uses Spring AOP internally. Choose the statement that describes what is happening when the update1 method is called?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571799,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<pre class=\"prettyprint linenums\">@Configuration\npublic class AppConfig {\n    \n    @Bean\n    public ClientService clientService() {\n    return new ClientServiceImpl();\n    }\n}</pre><p>What is the id/name of the declared bean in this Java configuration class?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework - @Bean Annotation](https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/context/annotation/Bean.html)",
                    "[Baeldung - Spring Beans and Bean Names](https://www.baeldung.com/spring-bean-names)"
                ],
                "feedbacks": [
                    "<p><strong>Incorrect</strong>, the name of the bean is not derived from the return type or the implementation class (<code>ClientServiceImpl</code>). By default, the bean name is the same as the name of the method annotated with <code>@Bean</code>.</p>",
                    "<p><strong>Incorrect</strong>, the name of the bean is not derived from the interface or the type (<code>ClientService</code>). By default, the bean name is the same as the name of the method annotated with <code>@Bean</code>.</p>",
                    "<p><strong>Incorrect</strong>, the name of the bean is not derived from the implementation class (<code>ClientServiceImpl</code>). By default, the bean name is the same as the name of the method annotated with <code>@Bean</code>.</p>",
                    "<p><strong>Correct</strong>, by default, the name of the bean is the same as the name of the method annotated with <code>@Bean</code>. In this case, the method name is <code>clientService</code>, so the bean name will also be <code>clientService</code>.</p>"
                ],
                "explanation": "<p><strong>How Spring Determines Bean Names in Java Configuration:</strong><br>In Spring, when using Java-based configuration with <code>@Bean</code>, the default name of the bean is the name of the method annotated with <code>@Bean</code>. This behavior ensures that the bean name is explicitly tied to the method name in the configuration class.</p><p><strong>Key Points:</strong></p><ol><li><p><strong>Default Bean Name:</strong></p><ul><li><p>The default name of the bean is the name of the method annotated with <code>@Bean</code>.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">java@Bean\npublic MyService myService() {\n    return new MyServiceImpl();\n}\n</pre><ul><li><p>The bean name will be <code>myService</code>.</p></li></ul></li></ul></li><li><p><strong>Custom Bean Name:</strong></p><ul><li><p>You can specify a custom name for the bean by providing a name to the <code>@Bean</code> annotation.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">java@Bean(name = \"customService\")\npublic MyService myService() {\n    return new MyServiceImpl();\n}\n</pre><ul><li><p>The bean name will be <code>customService</code>.</p></li></ul></li></ul></li><li><p><strong>Case Sensitivity:</strong></p><ul><li><p>Bean names are case-sensitive in Spring. For example, <code>clientService</code> and <code>ClientService</code> would be treated as different beans.</p></li></ul></li><li><p><strong>Avoid Bean Name Conflicts:</strong></p><ul><li><p>If two beans are declared with the same name, Spring will throw a <code>BeanDefinitionStoreException</code> unless one of the beans is explicitly marked as a primary bean using <code>@Primary</code>.</p></li></ul></li></ol><p><strong>Example Usage:</strong></p><p><strong>Accessing the Bean by Name:</strong></p><pre class=\"prettyprint linenums\">java@Configuration\npublic class AppConfig {\n\n    @Bean\n    public ClientService clientService() {\n        return new ClientServiceImpl();\n    }\n}\n</pre><p><strong>Main Application:</strong></p><pre class=\"prettyprint linenums\">javapublic class MainApp {\n    public static void main(String[] args) {\n        ApplicationContext context = new AnnotationConfigApplicationContext(AppConfig.class);\n\n        // Accessing the bean by name\n        ClientService service = (ClientService) context.getBean(\"clientService\");\n        service.performAction();\n    }\n}\n</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The bean is registered with the name <code>clientService</code>, which is the name of the method annotated with <code>@Bean</code>.</p></li><li><p>You can retrieve the bean by its name (<code>\"clientService\"</code>) or by its type (<code>ClientService.class</code>).</p></li></ul></li></ul><p><strong>Best Practices for Bean Naming:</strong></p><ol><li><p><strong>Use Descriptive Method Names:</strong></p><ul><li><p>Use method names that clearly describe the purpose of the bean. This makes it easier to understand the configuration and avoids naming conflicts.</p></li></ul></li><li><p><strong>Specify Custom Names When Necessary:</strong></p><ul><li><p>If you need to use a specific name for a bean, use the <code>name</code> attribute of the <code>@Bean</code> annotation.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">java@Bean(name = \"customClientService\")\npublic ClientService clientService() {\n    return new ClientServiceImpl();\n}\n</pre></li></ul></li><li><p><strong>Avoid Name Conflicts:</strong></p><ul><li><p>Ensure that no two beans have the same name in the application context unless one is explicitly marked as <code>@Primary</code>.</p></li></ul></li><li><p><strong>Follow Naming Conventions:</strong></p><ul><li><p>Use camelCase for bean names to follow standard Java conventions.</p></li></ul></li></ol><p><br></p>",
                "answers": [
                    "<p>clientServiceImpl</p>",
                    "<p>ClientService</p>",
                    "<p>ClientServiceImpl</p>",
                    "<p>clientService</p>"
                ]
            },
            "correct_response": [
                "d"
            ],
            "section": "Spring Core",
            "question_plain": "@Configuration\npublic class AppConfig {\n    \n    @Bean\n    public ClientService clientService() {\n    return new ClientServiceImpl();\n    }\n}What is the id/name of the declared bean in this Java configuration class?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571801,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>Which strategy is correct for configuring Spring Security to intercept particular URLs?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Authorize HttpServletRequests](https://docs.spring.io/spring-security/reference/servlet/authorization/authorize-http-requests.html#page-title)"
                ],
                "feedbacks": [
                    "<p><strong>Incorrect</strong>, Spring Security does not obtain URLs directly from Spring MVC controllers. URL interception and access control are configured explicitly in the Spring Security configuration, typically using <code>authorizeRequests()</code> and request matchers.</p>",
                    "<p><strong>Correct</strong>, Spring Security processes URL patterns in the order they are defined. The most specific rules should be defined first, followed by the least specific rules, as the first matching rule is applied.</p>",
                    "<p><strong>Incorrect</strong>, the order of URL patterns matters in Spring Security. The most specific rules must come first because Spring Security applies the first matching rule it encounters. Placing the least specific rules first would prevent more specific rules from being applied.</p>",
                    "<p><strong>Incorrect</strong>, Spring Security does not use a special properties file to specify URL patterns for interception. URL patterns and access rules are typically configured programmatically in a <code>SecurityFilterChain</code> or using XML configuration.</p>"
                ],
                "explanation": "<p><strong>How Spring Security Intercepts URLs:</strong><br>Spring Security uses a filter chain to intercept and secure HTTP requests. URL patterns and access rules are configured explicitly in the security configuration, typically using the <code>authorizeRequests()</code> method in Java-based configuration.</p><p><strong>Key Points About URL Interception in Spring Security:</strong></p><ol><li><p><strong>Order of Rules Matters:</strong></p><ul><li><p>Spring Security processes URL patterns in the order they are defined. The first matching rule is applied, and subsequent rules are ignored for the same request.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">javahttp.authorizeRequests()\n    .antMatchers(\"/admin/**\").hasRole(\"ADMIN\") // Most specific rule\n    .antMatchers(\"/**\").permitAll();          // Least specific rule\n</pre></li></ul></li><li><p><strong>Most Specific Rules First:</strong></p><ul><li><p>Define the most specific rules first (e.g., <code>/admin/**</code>) and the least specific rules last (e.g., <code>/**</code>). This ensures that specific rules are applied before general ones.</p></li></ul></li><li><p><strong>Request Matchers:</strong></p><ul><li><p>Spring Security provides several request matchers for specifying URL patterns:</p><ul><li><p><code>antMatchers</code>: Uses Ant-style path patterns (e.g., <code>/admin/**</code>).</p></li><li><p><code>regexMatchers</code>: Uses regular expressions for URL patterns.</p></li><li><p><code>mvcMatchers</code>: Matches Spring MVC-style patterns.</p></li></ul></li></ul></li><li><p><strong>Programmatic Configuration:</strong></p><ul><li><p>URL patterns and access rules are typically configured programmatically in a <code>SecurityFilterChain</code> bean.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">java@Bean\npublic SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\n    http.authorizeRequests()\n        .antMatchers(\"/admin/**\").hasRole(\"ADMIN\")\n        .antMatchers(\"/user/**\").hasRole(\"USER\")\n        .antMatchers(\"/**\").permitAll()\n        .and()\n        .formLogin();\n    return http.build();\n}\n</pre></li></ul></li><li><p><strong>No Special Properties File:</strong></p><ul><li><p>Spring Security does not use a special properties file for URL interception. All configurations are done programmatically or via XML.</p></li></ul></li></ol><p><strong>Example Usage:</strong></p><p><strong>Securing Specific URLs:</strong></p><pre class=\"prettyprint linenums\">java@Bean\npublic SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\n    http.authorizeRequests()\n        .antMatchers(\"/admin/**\").hasRole(\"ADMIN\") // Only users with ADMIN role can access /admin/**\n        .antMatchers(\"/user/**\").hasRole(\"USER\")   // Only users with USER role can access /user/**\n        .antMatchers(\"/**\").permitAll()           // All other URLs are accessible to everyone\n        .and()\n        .formLogin();                             // Enables form-based login\n    return http.build();\n}\n</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The <code>/admin/**</code> pattern is the most specific and is defined first.</p></li><li><p>The <code>/user/**</code> pattern is less specific and is defined after <code>/admin/**</code>.</p></li><li><p>The <code>/**</code> pattern is the least specific and is defined last.</p></li></ul></li></ul><p><strong>Best Practices for URL Interception:</strong></p><ol><li><p><strong>Define Specific Rules First:</strong></p><ul><li><p>Always define the most specific URL patterns first and the least specific patterns last.</p></li></ul></li><li><p><strong>Use Role-Based Access Control:</strong></p><ul><li><p>Use roles (e.g., <code>hasRole(\"ADMIN\")</code>) to enforce access control for specific URL patterns.</p></li></ul></li><li><p><strong>Test Your Configuration:</strong></p><ul><li><p>Test your security configuration to ensure that the rules are applied in the correct order.</p></li></ul></li><li><p><strong>Avoid Overlapping Patterns:</strong></p><ul><li><p>Be cautious when defining overlapping URL patterns to avoid unintended behavior.</p></li></ul></li><li><p><strong>Use HTTPS:</strong></p><ul><li><p>Ensure that sensitive URLs (e.g., <code>/admin/**</code>) are accessed over HTTPS for secure communication.</p></li></ul></li></ol><p><br></p>",
                "answers": [
                    "<p>Spring Security can obtain URLs from Spring MVC controllers, the Spring Security configuration just needs a reference to the controller to be protected</p>",
                    "<p>The URLs can be specified via configuration (using authorizeRequests() and request matchers), with the most specific rule first and the least specific last</p>",
                    "<p>The URLs can be specified via configuration (using authorizeRequests() and request matchers), with the least specific rule first and the most specific last</p>",
                    "<p>The URLs are specified in a special properties file, used by Spring Security</p>"
                ]
            },
            "correct_response": [
                "b"
            ],
            "section": "Spring Security",
            "question_plain": "Which strategy is correct for configuring Spring Security to intercept particular URLs?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571803,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which three statements are advantages of using Spring's Dependency Injection?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Dependency Injection](https://docs.spring.io/spring-framework/reference/core/beans/dependencies/factory-collaborators.html)",
                    "[Spring Framework - @Autowired Annotation](https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/factory/annotation/Autowired.html)"
                ],
                "feedbacks": [
                    "<p><strong>Incorrect</strong>, dependency injection is specifically designed to reduce tight coupling between components by externalizing the management of dependencies. It promotes loose coupling instead.</p>",
                    "<p><strong>Correct</strong>, one of the key advantages of dependency injection is that it facilitates loose coupling by allowing components to depend on abstractions rather than concrete implementations. Dependencies are injected at runtime, making the code more modular and testable.</p>",
                    "<p><strong>Correct</strong>, Spring allows configuration to be externalized (e.g., in XML, Java configuration, or properties files), which centralizes the management of dependencies and makes the application easier to configure and maintain.</p>",
                    "<p><strong>Correct</strong>, Spring's dependency injection allows the container to manage dependencies externally, meaning components do not need to create or manage their own dependencies. This improves modularity and separation of concern</p>",
                    "<p><strong>Incorrect</strong>, dependency injection decouples behavior from construction, which can sometimes make code harder to trace because dependencies are injected externally (e.g., by the Spring container) instead of being explicitly instantiated in the code.</p>",
                    "<ol><li><p><strong>Incorrect</strong>, dependency injection does not inherently reduce start-up time. In fact, it may slightly increase start-up time due to the overhead of creating and wiring beans in the Spring container.</p></li></ol><p><br></p>"
                ],
                "explanation": "<p><strong>What is Dependency Injection in Spring?</strong><br>Dependency Injection (DI) is a design pattern used in Spring to achieve loose coupling between components. Instead of components creating their own dependencies, the Spring container injects the required dependencies into the components at runtime. This allows for better modularity, easier testing, and centralized configuration.</p><p><strong>Key Advantages of Dependency Injection:</strong></p><ol><li><p><strong>Loose Coupling:</strong></p><ul><li><p>DI facilitates loose coupling by allowing components to depend on abstractions (e.g., interfaces) rather than concrete implementations.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">javapublic class OrderService {\n    private PaymentService paymentService;\n\n    // Dependency is injected via constructor\n    public OrderService(PaymentService paymentService) {\n        this.paymentService = paymentService;\n    }\n}\n</pre></li></ul></li><li><p><strong>Centralized Configuration:</strong></p><ul><li><p>Dependencies can be defined in a centralized configuration file (e.g., XML, Java-based configuration, or properties files), making it easier to manage and modify dependencies without changing the code.</p></li></ul></li><li><p><strong>External Dependency Management:</strong></p><ul><li><p>The Spring container manages the lifecycle and dependencies of beans, allowing components to focus on their core responsibilities.</p></li></ul></li><li><p><strong>Improved Testability:</strong></p><ul><li><p>DI makes it easier to test components in isolation by allowing mock dependencies to be injected during testing.</p></li></ul></li><li><p><strong>Modularity and Reusability:</strong></p><ul><li><p>Components can be reused in different contexts because they are not tightly coupled to specific implementations.</p></li></ul></li></ol><p><strong>Example Usage:</strong></p><p><strong>1. Constructor-Based Dependency Injection:</strong></p><pre class=\"prettyprint linenums\">java@Configuration\npublic class AppConfig {\n\n    @Bean\n    public PaymentService paymentService() {\n        return new CreditCardPaymentService();\n    }\n\n    @Bean\n    public OrderService orderService(PaymentService paymentService) {\n        return new OrderService(paymentService);\n    }\n}</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The <code>PaymentService</code> bean is injected into the <code>OrderService</code> bean via the constructor.</p></li><li><p>Dependencies are managed externally by the Spring container.</p></li></ul></li></ul><p><strong>2. Field-Based Dependency Injection:</strong></p><pre class=\"prettyprint linenums\">java@Component\npublic class OrderService {\n\n    @Autowired\n    private PaymentService paymentService;\n\n    public void processOrder() {\n        paymentService.processPayment();\n    }\n}</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The <code>PaymentService</code> dependency is injected into the <code>OrderService</code> bean via the <code>@Autowired</code> annotation.</p></li></ul></li></ul><p><strong>Best Practices for Dependency Injection:</strong></p><ol><li><p><strong>Prefer Constructor Injection:</strong></p><ul><li><p>Constructor injection is preferred over field injection because it ensures that dependencies are provided at the time of object creation and makes the class easier to test.</p></li></ul></li><li><p><strong>Avoid Overusing Field Injection:</strong></p><ul><li><p>Field injection can make components harder to test and less flexible. Use constructor or setter injection instead.</p></li></ul></li><li><p><strong>Define Dependencies in Configuration:</strong></p><ul><li><p>Use centralized configuration (e.g., Java-based configuration) to define dependencies, making them easier to manage and modify.</p></li></ul></li><li><p><strong>Use Interfaces for Abstractions:</strong></p><ul><li><p>Depend on interfaces rather than concrete implementations to promote loose coupling and flexibility.</p></li></ul></li><li><p><strong>Test with Mock Dependencies:</strong></p><ul><li><p>Use mock dependencies during testing to isolate components and test them independently.</p></li></ul></li></ol><p><br></p>",
                "answers": [
                    "<p>Dependency injection creates tight coupling between components</p>",
                    "<p>Dependency injection facilitates loose coupling between components</p>",
                    "<p>Configuration can be externalized and centralized in small set of files</p>",
                    "<p>Dependencies between application components can be managed external to the components</p>",
                    "<p>Dependency Injection can make code easier to trace because it couples behavior with construction</p>",
                    "<p>Dependency injection reduces the start-up time of an application</p>"
                ]
            },
            "correct_response": [
                "b",
                "c",
                "d"
            ],
            "section": "Spring Core",
            "question_plain": "Which three statements are advantages of using Spring's Dependency Injection?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571805,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which three types of objects can be returned from a JdbcTemplate query?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework - JdbcTemplate Documentation](https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/jdbc/core/JdbcTemplate.html)",
                    "[Baeldung - Spring JdbcTemplate](https://www.baeldung.com/spring-jdbc-jdbctemplate)"
                ],
                "feedbacks": [
                    "<p><strong>Incorrect</strong>, <code>JdbcTemplate</code> does not directly support returning <code>JSONObject</code> or JSON-specific types. However, you can manually convert query results to JSON using libraries like Jackson or Gson.</p>",
                    "<p><strong>Incorrect</strong>, <code>JdbcTemplate</code> does not directly support returning XML-specific types like <code>XMLObject</code>. If XML is needed, you must process the query result manually or use other libraries to handle XML parsing.</p>",
                    "<p><strong>Correct</strong>, <code>JdbcTemplate</code> can return simple types like <code>int</code>, <code>long</code>, <code>String</code>, etc., when querying single-column results or using row mappers to extract specific values.</p><ul><li><p>Example:</p><pre class=\"prettyprint linenums\">javaint count = jdbcTemplate.queryForObject(\"SELECT COUNT(*) FROM users\", Integer.class);</pre></li></ul>",
                    "<p><strong>Correct</strong>, <code>JdbcTemplate</code> can return <code>Map&lt;String, Object&gt;</code> for each row in the result set, where the keys are column names and the values are the corresponding column values.</p><ul><li><p>Example:</p><pre class=\"prettyprint linenums\">javaList&lt;Map&lt;String, Object&gt;&gt; rows = jdbcTemplate.queryForList(\"SELECT * FROM users\")</pre></li></ul>",
                    "<p><strong>Incorrect</strong>, while <code>Properties</code> is a Java class for managing key-value pairs, it is not a standard return type for <code>JdbcTemplate</code> queries. You would need to manually map query results to a <code>Properties</code> object if needed.</p>",
                    "<p><strong>Correct</strong>, <code>JdbcTemplate</code> can return user-defined types (custom objects) by using a <code>RowMapper</code> to map rows from the result set to instances of the custom class.</p><ul><li><p>Example:</p><pre class=\"prettyprint linenums\">javaList&lt;User&gt; users = jdbcTemplate.query(\"SELECT * FROM users\", new BeanPropertyRowMapper&lt;&gt;(User.class));</pre></li></ul>"
                ],
                "explanation": "<p><strong>Explanation:</strong></p><p><strong>What is </strong><code><strong>JdbcTemplate</strong></code><strong>?</strong><br><code>JdbcTemplate</code> is a Spring utility class that simplifies database access and eliminates much of the boilerplate code required for JDBC operations. It provides methods for querying and updating data, as well as executing SQL statements.</p><p><strong>Key Return Types for </strong><code><strong>JdbcTemplate</strong></code><strong> Queries:</strong></p><ol><li><p><strong>Simple Types:</strong></p><ul><li><p><code>JdbcTemplate</code> can return simple types like <code>int</code>, <code>long</code>, <code>String</code>, etc., when querying single-column results.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">javaString name = jdbcTemplate.queryForObject(\"SELECT name FROM users WHERE id = ?\", new Object[]{1}, String.class);\n</pre></li></ul></li><li><p><strong>Generic Maps:</strong></p><ul><li><p><code>JdbcTemplate</code> can return rows as <code>Map&lt;String, Object&gt;</code>, where the keys are column names and the values are the corresponding column values.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">javaList&lt;Map&lt;String, Object&gt;&gt; rows = jdbcTemplate.queryForList(\"SELECT * FROM users\");\n</pre></li></ul></li><li><p><strong>User-Defined Types:</strong></p><ul><li><p><code>JdbcTemplate</code> can return custom objects by using a <code>RowMapper</code> or <code>BeanPropertyRowMapper</code> to map rows from the result set to instances of the custom class.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">javaList&lt;User&gt; users = jdbcTemplate.query(\"SELECT * FROM users\", new BeanPropertyRowMapper&lt;&gt;(User.class));\n</pre></li></ul></li></ol><p><strong>Example Usage:</strong></p><p><strong>1. Returning Simple Types:</strong></p><pre class=\"prettyprint linenums\">javaint userCount = jdbcTemplate.queryForObject(\"SELECT COUNT(*) FROM users\", Integer.class);\nSystem.out.println(\"Number of users: \" + userCount);\n</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The query returns a single value (<code>COUNT(*)</code>), which is mapped to an <code>Integer</code>.</p></li></ul></li></ul><p><strong>2. Returning Generic Maps:</strong></p><pre class=\"prettyprint linenums\">javaList&lt;Map&lt;String, Object&gt;&gt; users = jdbcTemplate.queryForList(\"SELECT * FROM users\");\nfor (Map&lt;String, Object&gt; row : users) {\n    System.out.println(\"ID: \" + row.get(\"id\") + \", Name: \" + row.get(\"name\"));\n}\n</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>Each row in the result set is represented as a <code>Map&lt;String, Object&gt;</code>.</p></li><li><p>The keys in the map correspond to column names, and the values correspond to column values.</p></li></ul></li></ul><p><strong>3. Returning User-Defined Types:</strong></p><pre class=\"prettyprint linenums\">javapublic class User {\n    private int id;\n    private String name;\n\n    // Getters and setters\n}\n\nList&lt;User&gt; users = jdbcTemplate.query(\"SELECT * FROM users\", new BeanPropertyRowMapper&lt;&gt;(User.class));\nfor (User user : users) {\n    System.out.println(\"ID: \" + user.getId() + \", Name: \" + user.getName());\n}\n</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The <code>BeanPropertyRowMapper</code> maps each row in the result set to an instance of the <code>User</code> class.</p></li><li><p>The column names in the result set must match the field names in the <code>User</code> class.</p></li></ul></li></ul><p><strong>Best Practices for Using </strong><code><strong>JdbcTemplate</strong></code><strong>:</strong></p><ol><li><p><strong>Use RowMappers for Custom Objects:</strong></p><ul><li><p>Use <code>RowMapper</code> or <code>BeanPropertyRowMapper</code> to map query results to custom objects.</p></li></ul></li><li><p><strong>Handle Null Values:</strong></p><ul><li><p>Be cautious when working with nullable database columns. Use appropriate null checks in your code.</p></li></ul></li><li><p><strong>Use Parameterized Queries:</strong></p><ul><li><p>Always use parameterized queries to prevent SQL injection attacks.</p></li></ul></li><li><p><strong>Leverage Query Methods:</strong></p><ul><li><p>Use <code>queryForObject</code> for single-row results, <code>queryForList</code> for multiple rows, and <code>query</code> for custom mappings.</p></li></ul></li><li><p><strong>Test Query Results:</strong></p><ul><li><p>Test your queries to ensure they return the expected results, especially when using custom mappers.</p></li></ul></li></ol><p><br></p>",
                "answers": [
                    "<p>JSONObject</p>",
                    "<p>XMLObject</p>",
                    "<p>Simple types (int, long, String, etc)</p>",
                    "<p>Generic Maps</p>",
                    "<p>Properties</p>",
                    "<p>User defined types</p>"
                ]
            },
            "correct_response": [
                "c",
                "d",
                "f"
            ],
            "section": "Spring Data JPA",
            "question_plain": "Which three types of objects can be returned from a JdbcTemplate query?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571807,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are true regarding Spring Security?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Security Reference - Overview](https://docs.spring.io/spring-security/reference/index.html)",
                    "[Baeldung - Spring Security Authentication Providers](https://www.baeldung.com/spring-security-authentication-provider)"
                ],
                "feedbacks": [
                    "<p><strong>Correct</strong>, Spring Security supports multiple authentication mechanisms, including databases, LDAP, in-memory authentication, and custom authentication providers. This flexibility allows developers to integrate with various identity management systems.</p>",
                    "<p><strong>Incorrect</strong>, Spring Security is not tied to the Java EE Security specification. It is a flexible and customizable security framework that goes beyond the Java EE Security specification, offering additional features like method-level security, OAuth2 support, and more.</p>",
                    "<p><strong>Incorrect</strong>, <code>permitAll()</code> does not bypass Spring Security completely. It allows unrestricted access to specific resources while still keeping Spring Security active for other parts of the application.</p>",
                    "<p><strong>Correct</strong>, Spring Security supports method-level security using annotations such as <code>@PreAuthorize</code>, <code>@PostAuthorize</code>, <code>@Secured</code>, and <code>@RolesAllowed</code>. This allows fine-grained access control on individual methods.</p>",
                    "<p><strong>Incorrect</strong>, Spring Security does not require a JAAS policy file. While Spring Security can integrate with JAAS, it is not mandatory and is rarely used in modern Spring applications.</p>"
                ],
                "explanation": "<p><strong>What is Spring Security?</strong><br>Spring Security is a powerful and customizable framework for securing Java applications. It provides authentication, authorization, and other security-related features for both web and method-level security.</p><p><strong>Key Features of Spring Security:</strong></p><ol><li><p><strong>Authentication:</strong></p><ul><li><p>Spring Security supports multiple authentication mechanisms, including:</p><ul><li><p>Database-based authentication</p></li><li><p>LDAP-based authentication</p></li><li><p>In-memory authentication</p></li><li><p>Custom authentication providers</p></li></ul></li></ul></li><li><p><strong>Authorization:</strong></p><ul><li><p>Authorization can be configured at the URL level (via <code>HttpSecurity</code>) or method level (via annotations like <code>@PreAuthorize</code>).</p></li></ul></li><li><p><strong>Flexible Configuration:</strong></p><ul><li><p>Spring Security is not tied to any specific security specification like Java EE Security. It provides a flexible and customizable security model.</p></li></ul></li><li><p><strong>Annotations for Method-Level Security:</strong></p><ul><li><p>Spring Security supports annotations for method-level security:</p><ul><li><p><code>@PreAuthorize</code>: Pre-authorization checks before method execution.</p></li><li><p><code>@PostAuthorize</code>: Post-authorization checks after method execution.</p></li><li><p><code>@Secured</code>: Role-based access control.</p></li><li><p><code>@RolesAllowed</code>: Role-based access control (from JSR-250).</p></li></ul></li></ul></li><li><p><strong>Integration with External Systems:</strong></p><ul><li><p>Spring Security can integrate with external identity providers using protocols like OAuth2, OpenID Connect, and SAML.</p></li></ul></li><li><p><strong>PermitAll Configuration:</strong></p><ul><li><p>The <code>permitAll()</code> method in the security configuration allows unrestricted access to specific resources but does not disable Spring Security entirely.</p></li></ul></li></ol><p><strong>Example Usage:</strong></p><p><strong>1. Configuring Authentication with a Database:</strong></p><pre class=\"prettyprint linenums\">@Bean\npublic SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {\n    http\n        .authorizeRequests()\n        .antMatchers(\"/public/**\").permitAll()\n        .anyRequest().authenticated()\n        .and()\n        .formLogin();\n    return http.build();\n}\n\n@Bean\npublic UserDetailsService userDetailsService(DataSource dataSource) {\n    return new JdbcUserDetailsManager(dataSource); // Uses a database for authentication\n}</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>Authentication is configured to use a database.</p></li><li><p>The <code>/public/**</code> URL pattern is accessible to everyone, but all other requests require authentication.</p></li></ul></li></ul><p><strong>2. Method-Level Security:</strong></p><pre class=\"prettyprint linenums\">@Service\npublic class BankService {\n\n    @PreAuthorize(\"hasRole('ADMIN')\")\n    public void approveLoan() {\n        // Only users with the ADMIN role can call this method\n    }\n\n    @Secured(\"ROLE_USER\")\n    public void viewAccount() {\n        // Only users with the USER role can call this method\n    }\n}</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The <code>@PreAuthorize</code> annotation is used to enforce access control before the <code>approveLoan()</code> method is executed.</p></li><li><p>The <code>@Secured</code> annotation is used to enforce role-based access control for the <code>viewAccount()</code> method.</p></li></ul></li></ul><p><strong>Best Practices for Using Spring Security:</strong></p><ol><li><p><strong>Use Annotations for Method-Level Security:</strong></p><ul><li><p>Use annotations like <code>@PreAuthorize</code> and <code>@Secured</code> to enforce fine-grained access control at the method level.</p></li></ul></li><li><p><strong>Centralize URL Security Configuration:</strong></p><ul><li><p>Use <code>HttpSecurity</code> to define URL-level security rules in a centralized configuration.</p></li></ul></li><li><p><strong>Leverage Multiple Authentication Mechanisms:</strong></p><ul><li><p>Use the authentication mechanism that best suits your application, such as database-based authentication, LDAP, or OAuth2.</p></li></ul></li><li><p><strong>Test Security Configuration:</strong></p><ul><li><p>Regularly test your security configuration to ensure that access control rules are applied correctly.</p></li></ul></li><li><p><strong>Avoid Hardcoding Roles:</strong></p><ul><li><p>Use externalized configuration for roles and permissions to make the application easier to maintain.</p></li></ul></li></ol><p><br></p>",
                "answers": [
                    "<p>Authentication data can be accessed using a variety of different mechanisms, including databases and LDAP</p>",
                    "<p>It provides a strict implementation of the Java EE Security specification</p>",
                    "<p>In the authorization configuration the usage of permitAll(() allows bypassing Spring Security completely</p>",
                    "<p>Access control can be configured at the method level</p>",
                    "<p>A special Java Authentication and Authorization Service (JAAS) policy file needs to be configured</p>"
                ]
            },
            "correct_response": [
                "a",
                "d"
            ],
            "section": "Spring Security",
            "question_plain": "Which two statements are true regarding Spring Security?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571809,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are correct regarding Spring Boot auto-configuration?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot - Auto-Configuration](https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.developing-auto-configuration)"
                ],
                "feedbacks": [
                    "<p><strong>Incorrect</strong>, Spring Boot auto-configuration can apply when a bean is missing <strong>or</strong> when specific conditions are met. For example, <code>@ConditionalOnMissingBean</code> ensures that auto-configuration applies only when a specific bean is not already defined. However, auto-configuration can also apply based on other conditions, such as the presence of a class or property.</p>",
                    "<p><strong>Incorrect</strong>, auto-configuration is applied after user-defined beans are registered. This ensures that user-defined beans take precedence over auto-configured beans.</p>",
                    "<p><strong>Correct</strong>, Spring Boot auto-configuration classes are listed in the <code>META-INF/spring.factories</code> file, which is processed to determine which auto-configuration classes should be applied.</p>",
                    "<p><strong>Correct</strong>, auto-configuration classes use <code>@Conditional</code> annotations (e.g., <code>@ConditionalOnClass</code>, <code>@ConditionalOnBean</code>) to determine whether the configuration should be applied based on the presence or absence of specific classes, beans, or properties.</p>",
                    "<p><strong>Incorrect</strong>, auto-configuration is generally designed to apply when a bean is <strong>missing</strong>, not when it is already present. If a bean is already defined, auto-configuration will not override it.</p>"
                ],
                "explanation": "<p><strong>What is Spring Boot Auto-Configuration?</strong><br>Spring Boot's auto-configuration simplifies application setup by automatically configuring beans based on the classpath, environment, and other conditions. It eliminates the need for manual configuration for common use cases, while still allowing developers to override the defaults when needed.</p><p><strong>Key Features of Auto-Configuration:</strong></p><ol><li><p><strong>Triggered by Classpath and Context:</strong></p><ul><li><p>Auto-configuration is triggered based on the presence of specific classes, beans, or properties in the application context or classpath.</p></li></ul></li><li><p><strong>Uses </strong><code><strong>@Conditional</strong></code><strong> Annotations:</strong></p><ul><li><p>Auto-configuration classes use <code>@Conditional</code> annotations (e.g., <code>@ConditionalOnClass</code>, <code>@ConditionalOnBean</code>, <code>@ConditionalOnMissingBean</code>) to determine whether the configuration should be applied.</p></li></ul></li><li><p><strong>Defined in </strong><code><strong>META-INF/spring.factories</strong></code><strong>:</strong></p><ul><li><p>Auto-configuration classes are listed in the <code>META-INF/spring.factories</code> file, which is processed by Spring Boot at runtime.</p></li></ul></li><li><p><strong>Does Not Override User-Defined Beans:</strong></p><ul><li><p>Auto-configuration is applied only when a specific bean is missing in the application context. If a user-defined bean is present, it takes precedence over the auto-configured bean.</p></li></ul></li><li><p><strong>Customizable and Extensible:</strong></p><ul><li><p>Developers can customize or disable auto-configuration using <code>@EnableAutoConfiguration</code>, <code>@SpringBootApplication</code>, or the <code>spring.autoconfigure.exclude</code> property.</p></li></ul></li></ol><p><strong>Example Usage:</strong></p><p><strong>1. Auto-Configuration with </strong><code><strong>@ConditionalOnMissingBean</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">@Configuration\n@ConditionalOnMissingBean(SomeLibraryService.class)\npublic class SomeLibraryAutoConfiguration {\n\n    @Bean\n    public SomeLibraryService someLibraryService() {\n        return new SomeLibraryService();\n    }\n}</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The <code>SomeLibraryAutoConfiguration</code> class is applied only if no <code>SomeLibraryService</code> bean is already defined in the application context.</p></li><li><p>If a user-defined <code>SomeLibraryService</code> bean is present, the auto-configuration will not create a new bean.</p></li></ul></li></ul><p><strong>2. Auto-Configuration with </strong><code><strong>@ConditionalOnBean</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">@Configuration\n@ConditionalOnBean(DataSource.class)\npublic class JdbcTemplateAutoConfiguration {\n\n    @Bean\n    public JdbcTemplate jdbcTemplate(DataSource dataSource) {\n        return new JdbcTemplate(dataSource);\n    }\n}</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The <code>JdbcTemplateAutoConfiguration</code> class is applied only if a <code>DataSource</code> bean is present in the application context.</p></li><li><p>This ensures that the <code>JdbcTemplate</code> bean is created only when a <code>DataSource</code> is available.</p></li></ul></li></ul><p><strong>3. Disabling Auto-Configuration:</strong></p><pre class=\"prettyprint linenums\">spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration</pre><ul><li><p><strong>Key Points:</strong></p><ul><li><p>The <code>DataSourceAutoConfiguration</code> class is excluded from the auto-configuration process.</p></li><li><p>This is useful when you want to manually configure a data source.</p></li></ul></li></ul><p><strong>Best Practices for Auto-Configuration:</strong></p><ol><li><p><strong>Understand Default Behavior:</strong></p><ul><li><p>Familiarize yourself with the default auto-configuration classes provided by Spring Boot to avoid unnecessary manual configuration.</p></li></ul></li><li><p><strong>Override When Necessary:</strong></p><ul><li><p>Define your own beans to override default auto-configuration when specific customizations are required.</p></li></ul></li><li><p><strong>Disable Unnecessary Auto-Configuration:</strong></p><ul><li><p>Exclude auto-configuration classes that are not relevant to your application to improve performance and reduce complexity.</p></li></ul></li><li><p><strong>Test Auto-Configuration:</strong></p><ul><li><p>Test your application's behavior with and without auto-configuration to ensure that the desired beans are being created.</p></li></ul></li></ol><p><br></p>",
                "answers": [
                    "<p>Auto-configuration could apply when a bean is missing but not when bean is present</p>",
                    "<p>Auto-configuration is applied before user-defined bean have been registered</p>",
                    "<p>Auto-configuration is applied by processing candidates listed in META-INF/spring.factories</p>",
                    "<p>Auto-configuration uses @Conditional annotations to constrain when it should apply</p>",
                    "<p>Auto-configuration could apply when a bean is present but not when a bean is missing</p>"
                ]
            },
            "correct_response": [
                "c",
                "d"
            ],
            "section": "Spring Boot",
            "question_plain": "Which two statements are correct regarding Spring Boot auto-configuration?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571811,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are true regarding Spring Boot Testing?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot - Testing](https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.testing)",
                    "[Baeldung - Spring Boot Testing with @SpringBootTest](https://www.baeldung.com/spring-boot-testing)"
                ],
                "feedbacks": [
                    "<p>Incorrect, Spring Boot <strong>reuses</strong> the <code>ApplicationContext</code> between tests as long as the context configuration remains the same. The context will only be recreated if the <code>@DirtiesContext</code> annotation is used or if there are changes to the configuration that affect the context.</p>",
                    "<p>Correct, if no specific configuration classes are provided, <code>@SpringBootTest</code> will look for a single class annotated with <code>@SpringBootConfiguration</code> (or its meta-annotation, <code>@SpringBootApplication</code>). If multiple configuration classes are found, an exception will be thrown unless explicitly specified.</p>",
                    "<p>Incorrect, there is no annotation named <code>@TestApplicationContext</code> in Spring Boot. To define additional beans or configurations for tests, you should use the <code>@TestConfiguration</code> annotation or provide configuration classes in the <code>@SpringBootTest</code> annotation.</p>",
                    "<p>Incorrect, test methods in a <code>@SpringBootTest</code> class are <strong>not transactional by default</strong>. To enable transactional behavior, you must explicitly annotate the class or methods with <code>@Transactional</code>.</p>",
                    "<p>Correct, <code>@SpringBootTest</code> is designed for integration testing as it loads the full <code>ApplicationContext</code> and allows testing the application in a fully initialized environment.</p>"
                ],
                "explanation": "<p><code><strong>@SpringBootTest</strong></code><strong> Annotation in Spring Boot:</strong><br>The <code>@SpringBootTest</code> annotation is a powerful annotation in Spring Boot used for integration testing. It loads the full <code>ApplicationContext</code> and provides a fully initialized environment for testing.</p><p><strong>Key Features of </strong><code><strong>@SpringBootTest</strong></code><strong>:</strong></p><ol><li><p><strong>Reusing Application Context:</strong></p><ul><li><p>By default, Spring Boot <strong>reuses</strong> the <code>ApplicationContext</code> between tests as long as the context configuration remains the same.</p></li><li><p>The context will only be recreated in the following cases:</p><ul><li><p>The <code>@DirtiesContext</code> annotation is used.</p></li><li><p>There are changes to the configuration that affect the context.</p></li></ul></li></ul></li><li><p><strong>Default Configuration Class:</strong></p><ul><li><p>If no configuration classes are specified, <code>@SpringBootTest</code> looks for a single class annotated with <code>@SpringBootConfiguration</code> (or <code>@SpringBootApplication</code>).</p></li><li><p>If multiple configuration classes exist, an exception will be thrown unless explicitly specified.</p></li></ul></li><li><p><strong>Transactional Behavior:</strong></p><ul><li><p>Test methods in a <code>@SpringBootTest</code> class are <strong>not transactional by default</strong>.</p></li><li><p>To enable transactional behavior, annotate the class or methods with <code>@Transactional</code>.</p></li></ul></li><li><p><strong>Customizing the Context:</strong></p><ul><li><p>Additional beans or configurations can be defined using the <code>@TestConfiguration</code> annotation or by specifying configuration classes in the <code>@SpringBootTest</code> annotation.</p></li></ul></li><li><p><strong>Caching Application Context:</strong></p><ul><li><p>Spring Boot caches the <code>ApplicationContext</code> to avoid recreating it for every test.</p></li></ul></li></ol>",
                "answers": [
                    "<p>Test methods annotated with @SpringBootTest will recreate the ApplicationContext</p>",
                    "<p>@SpringBootTest without any configuration classes expects there is only one class annotated with @SpringBootConfiguration in the application</p>",
                    "<p>@TestApplicationContext is used to define additional beans or customizations for a test</p>",
                    "<p>Test methods in a @SpringBootTest class are transactional by default</p>",
                    "<p>@SpringBootTest is typically used for integration testing</p>"
                ]
            },
            "correct_response": [
                "b",
                "e"
            ],
            "section": "Spring Boot",
            "question_plain": "Which two statements are true regarding Spring Boot Testing?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571813,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two options are valid optional attributes for Spring's @Transactional annotation?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework Reference Documentation - Transaction Management](https://docs.spring.io/spring-framework/docs/current/reference/html/data-access.html#transaction-declarative)",
                    "[Spring Boot Documentation - Data Access](https://docs.spring.io/spring-boot/docs/current/reference/htmlsingle/#boot-features-sql)",
                    "[Baeldung - Spring Transaction Management](https://www.baeldung.com/transaction-configuration-with-jpa-and-spring)"
                ],
                "feedbacks": [
                    "<p>This attribute specifies the transaction isolation level (e.g., READ_COMMITTED, REPEATABLE_READ). It determines how the transaction should be isolated from other concurrent transactions.<br><em>Example:</em></p><pre class=\"prettyprint linenums\">@Transactional(isolation = Isolation.READ_COMMITTED)\npublic void performTask() {\n    // business logic here\n}</pre><p>This attribute is valid because it is part of the standard configuration options provided by Spring for controlling transactional behavior.</p>",
                    "<p>This attribute defines the transactional propagation behavior (e.g., REQUIRED, SUPPORTS, NESTED). It instructs Spring how to handle transactions when a transactional method is called from another transactional context.<br><em>Example:</em></p><pre class=\"prettyprint linenums\">@Transactional(propagation = Propagation.REQUIRED)\npublic void executeOperation() {\n    // business logic here\n}\n</pre><p>This attribute is valid and widely used to manage the boundaries and nested behavior of transactions in Spring.</p>",
                    "<p>This option is not a valid attribute in the @Transactional annotation. There is no such attribute defined in Spring's transaction management API.</p>",
                    "<p>While Spring supports nested transactions via the Propagation.NESTED setting, there is no attribute named \"nestedTransaction.\" The correct approach is to set propagation to NESTED if nested transactions are desired.</p>",
                    "<p>This option is not a valid attribute either. Spring’s @Transactional annotation provides a \"readOnly\" attribute to optimize transactions for read operations, but \"readWrite\" does not exist.</p>"
                ],
                "explanation": "<p>Transaction management in Spring is a critical aspect of enterprise application development. Spring provides both declarative and programmatic transaction management, with the declarative approach being preferred for its simplicity and maintainability.</p><p><strong>Key Concepts:</strong></p><ul><li><p><strong>Declarative Transaction Management:</strong><br>By using the @Transactional annotation, developers can demarcate transactional boundaries declaratively. This approach abstracts the complexity of manual transaction management and integrates seamlessly with Spring’s AOP (Aspect-Oriented Programming) support.</p></li><li><p><strong>Attributes of @Transactional:</strong><br>The @Transactional annotation comes with several attributes that allow fine-tuning of transactional behavior:</p><ul><li><p><strong>propagation:</strong> Determines how transactions relate to each other. For instance, <code>Propagation.REQUIRED</code> (default) means that the method must run within a transaction, joining an existing one if present. Options like <code>Propagation.REQUIRES_NEW</code> or <code>Propagation.NESTED</code> provide additional control.</p></li><li><p><strong>isolation:</strong> Sets the isolation level for the transaction, controlling how and when the changes made by one transaction become visible to other transactions. Common isolation levels include <code>Isolation.READ_COMMITTED</code> and <code>Isolation.REPEATABLE_READ</code>.</p></li><li><p><strong>timeout:</strong> Specifies the maximum time a transaction is allowed to run before it is automatically rolled back.</p></li><li><p><strong>readOnly:</strong> A boolean flag that, when set to true, hints that the transaction should be optimized for read operations. This can improve performance by avoiding unnecessary locks.</p></li><li><p><strong>rollbackFor / noRollbackFor:</strong> These attributes allow developers to specify which exception types should trigger a rollback or not.</p></li></ul></li></ul><p><strong>Example Usage:</strong><br>Consider a service layer where transactional behavior is essential for data integrity:</p><pre class=\"prettyprint linenums\">@Service\npublic class OrderService {\n\n    @Transactional(\n        propagation = Propagation.REQUIRED,\n        isolation = Isolation.READ_COMMITTED,\n        timeout = 30,\n        readOnly = false\n    )\n    public void placeOrder(Order order) {\n        // business logic for placing an order\n        // e.g., validate order, update inventory, process payment\n    }\n}</pre><p>In this example, the <code>@Transactional</code> annotation ensures that all operations within the <code>placeOrder</code> method are executed within a single transaction. The settings specify that the transaction must join an existing one or create a new one (<code>Propagation.REQUIRED</code>), use the READ_COMMITTED isolation level to avoid dirty reads, enforce a timeout of 30 seconds, and indicate that the transaction is not read-only (as it involves write operations).</p><p><strong>Benefits of Using @Transactional:</strong></p><ul><li><p><strong>Consistency and Data Integrity:</strong> Ensures that a series of operations either complete successfully or are entirely rolled back in the event of a failure.</p></li><li><p><strong>Simplified Error Handling:</strong> Automatic rollback capabilities reduce the need for explicit error handling in business logic.</p></li><li><p><strong>Declarative Approach:</strong> Improves code readability and maintainability by separating transaction management from business logic.</p></li></ul><p><strong>Advanced Topics:</strong></p><ul><li><p><strong>Nested Transactions:</strong> While not supported directly via a separate attribute, nested transactions can be managed using <code>Propagation.NESTED</code>. This allows a transaction to be nested within another, which can be useful for isolating a subset of operations within a larger transactional context.</p></li><li><p><strong>Programmatic Transaction Management:</strong> In cases where finer control is needed, developers can use Spring’s <code>PlatformTransactionManager</code> to manage transactions programmatically, though this approach is more verbose and error-prone compared to declarative transactions.</p></li></ul>",
                "answers": [
                    "<p>isolation</p>",
                    "<p>propagation</p>",
                    "<p>writeOnly</p>",
                    "<p>nestedTransaction</p>",
                    "<p>readWrite</p>"
                ]
            },
            "correct_response": [
                "a",
                "b"
            ],
            "section": "Spring Data JPA",
            "question_plain": "Which two options are valid optional attributes for Spring's @Transactional annotation?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571815,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>In Spring, each bean instance is assigned a scope. What is the default scope of a Spring bean?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework Bean Scopes Documentation](https://docs.spring.io/spring-framework/docs/current/reference/html/core.html#beans-factory-scopes)",
                    "[Baeldung - Spring Bean Scopes](https://www.baeldung.com/spring-bean-scopes)"
                ],
                "feedbacks": [
                    "<p>The <code>session</code> scope is not the default scope in Spring. It is a specialized scope used in web applications where a new bean instance is created for each HTTP session. Since <code>session</code> scope is only relevant in web applications, it cannot be the default.</p>",
                    "<p>Similar to <code>session</code>, the <code>request</code> scope applies only to web applications, where a new bean instance is created for each HTTP request. The <code>request</code> scope is useful for beans that should not be shared across different HTTP requests.</p>",
                    "<p>This is the correct answer. In Spring, the default scope of a bean is <code>singleton</code>, meaning that a single instance of the bean is created and shared across the entire Spring container. If no scope is specified, Spring automatically assumes <code>singleton</code>.<br><em>Example:</em></p><pre class=\"prettyprint linenums\">@Component\npublic class SingletonBean {\n    public SingletonBean() {\n        System.out.println(\"Singleton instance created\");\n    }\n}\n</pre><p>Regardless of how many times this bean is injected into different components, only <strong>one instance</strong> is created.</p>",
                    "<p>The <code>prototype</code> scope is another available option but is <strong>not the default</strong>. In this scope, a new instance of the bean is created <strong>every time it is requested</strong> from the Spring container.</p><pre class=\"prettyprint linenums\">@Scope(\"prototype\")\n@Component\npublic class PrototypeBean {\n    public PrototypeBean() {\n        System.out.println(\"New Prototype instance created\");\n    }\n}</pre><p>Unlike <code>singleton</code>, prototype beans do not get stored in the application context after creation.</p>"
                ],
                "explanation": "<p>In the Spring Framework, a <strong>bean scope</strong> defines the lifecycle and visibility of a bean within the application context. Different scopes allow for flexibility in managing bean instances, depending on the needs of the application.</p><p><strong>1. Default Scope: Singleton</strong></p><p>The default scope for a Spring bean is <strong>singleton</strong>. This means that Spring creates exactly <strong>one instance</strong> of the bean per Spring container, and every request for the bean returns the same instance.</p><pre class=\"prettyprint linenums\">@Component\npublic class SingletonBean {\n    public SingletonBean() {\n        System.out.println(\"SingletonBean instance created\");\n    }\n}\n</pre><p>When this bean is used multiple times, only <strong>one instance</strong> is created and shared:</p><pre class=\"prettyprint linenums\">@Autowired\nprivate SingletonBean singletonBean1;\n\n@Autowired\nprivate SingletonBean singletonBean2;\n</pre><p>Here, <code>singletonBean1</code> and <code>singletonBean2</code> will refer to <strong>the same object</strong>.</p><p><strong>2. Other Scopes in Spring:</strong></p><ul><li><p><strong>Prototype:</strong> Each request for the bean creates a <strong>new instance</strong>. This is useful for stateful objects.</p><pre class=\"prettyprint linenums\">@Scope(\"prototype\")\n@Component\npublic class PrototypeBean {\n    public PrototypeBean() {\n        System.out.println(\"PrototypeBean instance created\");\n    }\n}\n</pre><p>Now, every time we inject or request this bean, a <strong>new instance</strong> is created.</p></li><li><p><strong>Request:</strong> Used in web applications. A new bean instance is created per <strong>HTTP request</strong>.</p><pre class=\"prettyprint linenums\">@Scope(\"request\")\n@Component\npublic class RequestBean {\n}\n</pre><p>This is useful for handling request-specific data in web applications.</p></li><li><p><strong>Session:</strong> Creates a new bean instance for each <strong>user session</strong> in a web application.</p><pre class=\"prettyprint linenums\">@Scope(\"session\")\n@Component\npublic class SessionBean {\n}\n</pre><p>This is useful for maintaining user session-specific state.</p></li><li><p><strong>Application:</strong> The bean is shared across the entire <strong>ServletContext</strong> (application-wide scope).</p></li><li><p><strong>WebSocket:</strong> A bean exists for the duration of a WebSocket session.</p></li></ul><p><strong>3. Choosing the Right Scope:</strong></p><ul><li><p>Use <strong>singleton</strong> for stateless services or utility classes (recommended default).</p></li><li><p>Use <strong>prototype</strong> if you need a new instance every time (e.g., objects with state).</p></li><li><p>Use <strong>request/session/application</strong> scopes in web applications when managing user or request-specific state.</p></li></ul><p><br></p>",
                "answers": [
                    "<p>session</p>",
                    "<p>request</p>",
                    "<p>singleton</p>",
                    "<p>prototype</p>"
                ]
            },
            "correct_response": [
                "c"
            ],
            "section": "Spring Core",
            "question_plain": "In Spring, each bean instance is assigned a scope. What is the default scope of a Spring bean?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571817,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are true regarding the RestTemplate class?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Official Documentation – RestTemplate](https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/web/client/RestTemplate.html)",
                    "[Spring Guide – Accessing REST services with RestTemplate](https://spring.io/guides/gs/consuming-rest/)",
                    "[Baeldung – Guide to RestTemplate](https://www.baeldung.com/rest-template)"
                ],
                "feedbacks": [
                    "<p>The <code>RestTemplate</code> class is designed to simplify communication with RESTful web services. It provides methods such as <code>getForObject()</code>, <code>postForEntity()</code>, <code>exchange()</code>, and <code>delete()</code> to handle HTTP requests and responses conveniently.<br><em>Example:</em></p><pre class=\"prettyprint linenums\">RestTemplate restTemplate = new RestTemplate();\nString response = restTemplate.getForObject(\"https://api.example.com/data\", String.class);\n</pre><p>This statement is <strong>correct</strong> as <code>RestTemplate</code> is primarily intended for REST <strong>clients</strong>, not services.</p>",
                    "<p><code>RestTemplate</code> automatically serializes Java objects to JSON (or other formats) and deserializes HTTP responses into Java objects using <code>MessageConverters</code>.<br><em>Example:</em></p><pre class=\"prettyprint linenums\">public class User {\n    private String name;\n    private int age;\n    // getters and setters\n}\n\nRestTemplate restTemplate = new RestTemplate();\nUser user = restTemplate.getForObject(\"https://api.example.com/user/1\", User.class);\n</pre><p>Here, Spring automatically converts the JSON response to a <code>User</code> object.<br>This statement is <strong>correct</strong> because <code>RestTemplate</code> supports automatic object mapping using <code>HttpMessageConverters</code>.</p>",
                    "<p>This statement is <strong>incorrect</strong> because <code>RestTemplate</code> does allow sending HTTP requests with custom headers using the <code>exchange()</code> method.<br><em>Example of sending a request with a custom header:</em></p><pre class=\"prettyprint linenums\">HttpHeaders headers = new HttpHeaders();\nheaders.set(\"Authorization\", \"Bearer token\");\nHttpEntity&lt;String&gt; entity = new HttpEntity&lt;&gt;(headers);\n\nResponseEntity&lt;String&gt; response = restTemplate.exchange(\n    \"https://api.example.com/protected-resource\",\n    HttpMethod.GET,\n    entity,\n    String.class\n);\n</pre><p>This contradicts the statement, making it incorrect.</p>",
                    "<p>While <code>RestTemplate</code> is useful for consuming RESTful services, it does <strong>not</strong> provide features for writing RESTful services (i.e., it is not a framework for creating REST APIs, such as Spring MVC).<br>A REST service in Spring is typically implemented using <code>@RestController</code> and <code>@RequestMapping</code>, not <code>RestTemplate</code>.<br><em>Example of a RESTful service (which does not involve </em><code><em>RestTemplate</em></code><em>):</em></p><pre class=\"prettyprint linenums\">@RestController\n@RequestMapping(\"/users\")\npublic class UserController {\n    @GetMapping(\"/{id}\")\n    public User getUser(@PathVariable Long id) {\n        return new User(\"John Doe\", 30);\n    }\n}\n</pre><p>This makes the statement <strong>incorrect</strong>.</p>",
                    "<p><code>RestTemplate</code> follows a <strong>synchronous</strong> and <strong>blocking</strong> execution model, meaning each HTTP request blocks the calling thread until the response is received.<br>If non-blocking behavior is needed, <code>WebClient</code> (introduced in Spring 5 as part of Spring WebFlux) should be used instead.<br><em>Example using WebClient for non-blocking requests:</em></p><pre class=\"prettyprint linenums\">WebClient webClient = WebClient.create(\"https://api.example.com\");\n\nMono&lt;String&gt; response = webClient.get()\n    .uri(\"/data\")\n    .retrieve()\n    .bodyToMono(String.class);\n</pre><p>Since <code>RestTemplate</code> does not support asynchronous non-blocking operations, this statement is <strong>incorrect</strong>.</p>"
                ],
                "explanation": "<p><code>RestTemplate</code> is a synchronous HTTP client in Spring that simplifies REST API communication by providing high-level methods for HTTP interactions. It abstracts the complexity of making HTTP requests and processing responses, making it easier for developers to work with RESTful services.</p><p><strong>Key Features of </strong><code><strong>RestTemplate</strong></code><strong>:</strong></p><ul><li><p>Supports standard HTTP methods: <code>GET</code>, <code>POST</code>, <code>PUT</code>, <code>DELETE</code>, <code>PATCH</code></p></li><li><p>Automatically converts HTTP responses into Java objects using <code>HttpMessageConverters</code></p></li><li><p>Allows setting custom headers and authentication</p></li><li><p>Handles request/response serialization and deserialization</p></li></ul><p><strong>Common Methods in </strong><code><strong>RestTemplate</strong></code><strong>:</strong></p><ul><li><p><strong>GET Requests:</strong></p><pre class=\"prettyprint linenums\">RestTemplate restTemplate = new RestTemplate();\nString response = restTemplate.getForObject(\"https://api.example.com/data\", String.class);\n</pre></li><li><p><strong>POST Requests:</strong></p><pre class=\"prettyprint linenums\">User newUser = new User(\"Alice\", 25);\nResponseEntity&lt;User&gt; response = restTemplate.postForEntity(\"https://api.example.com/users\", newUser, User.class);\n</pre></li><li><p><strong>Sending Requests with Custom Headers:</strong></p><pre class=\"prettyprint linenums\">HttpHeaders headers = new HttpHeaders();\nheaders.setContentType(MediaType.APPLICATION_JSON);\nHttpEntity&lt;User&gt; requestEntity = new HttpEntity&lt;&gt;(newUser, headers);\n\nResponseEntity&lt;User&gt; response = restTemplate.exchange(\n    \"https://api.example.com/users\",\n    HttpMethod.POST,\n    requestEntity,\n    User.class\n);\n</pre></li><li><p><strong>DELETE Requests:</strong></p><pre class=\"prettyprint linenums\">restTemplate.delete(\"https://api.example.com/users/1\");\n</pre></li></ul><p><strong>When to Use </strong><code><strong>RestTemplate</strong></code><strong> vs. </strong><code><strong>WebClient</strong></code><strong>?</strong></p><ul><li><p><strong>Use </strong><code><strong>RestTemplate</strong></code> if you need a simple, synchronous HTTP client in traditional Spring MVC applications.</p></li><li><p><strong>Use </strong><code><strong>WebClient</strong></code> if you need an <strong>asynchronous</strong> and <strong>non-blocking</strong> HTTP client for reactive programming in Spring WebFlux.</p></li></ul><p><strong>Deprecation Notice:</strong></p><p>While <code>RestTemplate</code> is still widely used, <strong>Spring recommends using </strong><code><strong>WebClient</strong></code><strong> for new applications</strong> as <code>RestTemplate</code> will be deprecated in future Spring versions.</p><p><br></p>",
                "answers": [
                    "<p>It provides convenience methods for writing REST clients</p>",
                    "<p>It automatically supports sending and receiving Java objects</p>",
                    "<p>Sending an HTTP request with a custom header is not possible when using RestTemplate</p>",
                    "<p>It provides convenience methods for writing REST services</p>",
                    "<p>It supports asynchronous non-blocking model</p>"
                ]
            },
            "correct_response": [
                "a",
                "b"
            ],
            "section": "Spring Web",
            "question_plain": "Which two statements are true regarding the RestTemplate class?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571819,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which statements are correct when @SpringBootApplication is annotated on a class?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Documentation – @SpringBootApplication](https://docs.spring.io/spring-boot/docs/current/reference/html/using.html#using.boot-application)",
                    "[Spring Boot Auto-Configuration Explained](https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.developing-auto-configuration)",
                    "[Baeldung – Guide to @SpringBootApplication](https://www.baeldung.com/spring-boot-annotations)"
                ],
                "feedbacks": [
                    "<p>This statement is <strong>incorrect</strong> because methods annotated with <code>@Bean</code> in a class with <code>@SpringBootApplication</code> are <strong>not ignored</strong>. The <code>@SpringBootApplication</code> annotation enables component scanning and configuration, which includes detecting <code>@Bean</code> methods.<br><em>Example:</em></p><pre class=\"prettyprint linenums\">@SpringBootApplication\npublic class MyApplication {\n    public static void main(String[] args) {\n        SpringApplication.run(MyApplication.class, args);\n    }\n\n    @Bean\n    public RestTemplate restTemplate() {\n        return new RestTemplate();\n    }\n}\n</pre><p>The <code>restTemplate()</code> method will be registered as a bean, proving that <code>@Bean</code> methods are <strong>not ignored</strong>.</p>",
                    "<p>This is <strong>incorrect</strong> because <code>@SpringBootApplication</code> does not override or ignore other annotations on the class. Additional annotations (e.g., <code>@EnableScheduling</code>, <code>@PropertySource</code>) will still function normally.<br><em>Example:</em></p><pre class=\"prettyprint linenums\">@SpringBootApplication\n@EnableScheduling\npublic class MyApplication {\n}\n</pre><p>Here, <code>@EnableScheduling</code> will still work, demonstrating that other annotations <strong>are not ignored</strong>.</p>",
                    "<p>This is <strong>correct</strong>. The <code>@SpringBootApplication</code> annotation includes <code>@EnableAutoConfiguration</code>, which instructs Spring Boot to automatically configure beans based on classpath dependencies.</p><p><br></p><p><em>Example:</em> If <code>spring-boot-starter-web</code> is present, Spring Boot <strong>automatically</strong> configures an embedded Tomcat server and registers necessary beans.</p>",
                    "<p>This is <strong>correct</strong>. The <code>@SpringBootApplication</code> annotation includes <code>@ComponentScan</code>, which scans for Spring components <strong>starting from the package where the annotated class is located</strong>.<br><em>Example:</em></p><pre class=\"prettyprint linenums\">@SpringBootApplication\npublic class MyApp { }\n</pre><p>If <code>MyApp</code> is in <code>com.example.app</code>, then all components in <code>com.example.app</code> and sub-packages will be scanned automatically.</p>",
                    "<p>This is <strong>incorrect</strong>. Spring Boot applications typically create <strong>a single </strong><code><strong>ApplicationContext</strong></code> per application instance. Multiple contexts can be created manually, but <code>@SpringBootApplication</code> itself does not lead to multiple application contexts.</p>"
                ],
                "explanation": "<p>The <code>@SpringBootApplication</code> annotation is a convenience annotation in Spring Boot that combines three important annotations:</p><pre class=\"prettyprint linenums\">@Target(ElementType.TYPE)\n@Retention(RetentionPolicy.RUNTIME)\n@SpringBootConfiguration\n@EnableAutoConfiguration\n@ComponentScan\npublic @interface SpringBootApplication { }\n</pre><p><strong>Key Components of </strong><code><strong>@SpringBootApplication</strong></code><strong>:</strong></p><ol><li><p><code><strong>@SpringBootConfiguration</strong></code></p><ul><li><p>A specialized version of <code>@Configuration</code>, indicating that this class provides Spring Boot application configuration.</p></li></ul></li><li><p><code><strong>@EnableAutoConfiguration</strong></code></p><ul><li><p>Enables Spring Boot's auto-configuration feature, which configures beans automatically based on dependencies.</p></li></ul></li><li><p><code><strong>@ComponentScan</strong></code></p><ul><li><p>Enables component scanning from the package of the annotated class and its sub-packages.</p></li></ul></li></ol><p><strong>Effect of </strong><code><strong>@SpringBootApplication</strong></code><strong>:</strong></p><ul><li><p>Automatically configures commonly used Spring beans based on the classpath.</p></li><li><p>Starts component scanning from the package where the annotation is placed.</p></li><li><p>Reduces the need for manual configuration in <code>applicationContext.xml</code> or Java-based configuration.</p></li></ul><p><strong>Example Usage:</strong></p><pre class=\"prettyprint linenums\">@SpringBootApplication\npublic class MyApplication {\n    public static void main(String[] args) {\n        SpringApplication.run(MyApplication.class, args);\n    }\n}\n</pre><p>In this example:</p><ul><li><p>Spring Boot automatically configures beans for embedded Tomcat, Spring MVC, and Jackson (if dependencies exist).</p></li><li><p>Components like <code>@Service</code>, <code>@Repository</code>, and <code>@Controller</code> in the same package or sub-packages are automatically detected.</p></li></ul><p><strong>Manually Customizing </strong><code><strong>@SpringBootApplication</strong></code><strong>:</strong></p><p>If necessary, you can override its behavior.</p><ul><li><p><strong>Disable Auto-Configuration for Specific Classes:</strong></p><pre class=\"prettyprint linenums\">@SpringBootApplication(exclude = DataSourceAutoConfiguration.class)\npublic class MyApplication { }\n</pre></li><li><p><strong>Change Component Scan Package:</strong></p><pre class=\"prettyprint linenums\">@SpringBootApplication\n@ComponentScan(\"com.example.custompackage\")\npublic class MyApplication { }</pre></li></ul>",
                "answers": [
                    "<p>Methods in the class annotated with @Bean will be ignored</p>",
                    "<p>All other annotations on the class will be ignored</p>",
                    "<p>It causes Spring Boot to enable auto-configuration by default</p>",
                    "<p>Component scanning will start from the package of the class</p>",
                    "<p>A separate ApplicationContext will be created for each class annotated with @SpringBootApplication</p>"
                ]
            },
            "correct_response": [
                "c",
                "d"
            ],
            "section": "Spring Boot",
            "question_plain": "Which statements are correct when @SpringBootApplication is annotated on a class?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571821,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are true concerning the BeanPostProcessor Extension point?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Official Documentation – BeanPostProcessor](https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/beans/factory/config/BeanPostProcessor.html)",
                    "[Spring Bean Lifecycle and PostProcessors:](https://docs.spring.io/spring-framework/docs/current/reference/html/core.html#beans-factory-extension-bpp)",
                    "[Baeldung – BeanPostProcessor Explained:](https://www.baeldung.com/spring-beanpostprocessor)"
                ],
                "feedbacks": [
                    "<p>This statement is <strong>correct</strong> because Spring allows developers to implement custom <code>BeanPostProcessor</code> classes to modify or customize bean instances after their instantiation but before they are used in the application.<br><em>Example:</em> Implementing a custom <code>BeanPostProcessor</code>:</p><pre class=\"prettyprint linenums\">@Component\npublic class CustomBeanPostProcessor implements BeanPostProcessor {\n    @Override\n    public Object postProcessBeforeInitialization(Object bean, String beanName) {\n        System.out.println(\"Before Initialization: \" + beanName);\n        return bean;\n    }\n\n    @Override\n    public Object postProcessAfterInitialization(Object bean, String beanName) {\n        System.out.println(\"After Initialization: \" + beanName);\n        return bean;\n    }\n}\n</pre><p>This processor modifies beans before and after their initialization.</p>",
                    "<p>This statement is <strong>incorrect</strong> because <code>BeanFactoryPostProcessor</code>s are executed <strong>before</strong> any bean instances are created, while <code>BeanPostProcessor</code>s operate on beans <strong>after they are instantiated</strong> but before they are fully initialized.</p><ul><li><p><strong>BeanFactoryPostProcessor</strong> runs <strong>before</strong> beans are instantiated and is mainly used to modify bean definitions (e.g., <code>PropertySourcesPlaceholderConfigurer</code>).</p></li><li><p><strong>BeanPostProcessor</strong> runs <strong>after</strong> the bean is created but before initialization completes.<br><em>Execution order:</em></p></li></ul><ol><li><p><strong>BeanFactoryPostProcessor</strong> modifies the bean definitions before they are instantiated.</p></li><li><p>The bean instance is created.</p></li><li><p><strong>BeanPostProcessor</strong> processes the bean before and after initialization.</p></li></ol>",
                    "<p>This is <strong>correct</strong> because <code>BeanPostProcessor</code> hooks into the bean lifecycle during <strong>initialization</strong> (i.e., after instantiation but before usage).</p><ul><li><p><code>postProcessBeforeInitialization()</code> is called <strong>before</strong> the <code>@PostConstruct</code> or <code>InitializingBean#afterPropertiesSet()</code> methods.</p></li><li><p><code>postProcessAfterInitialization()</code> is called <strong>after</strong> initialization.</p></li></ul>",
                    "<p>This is <strong>incorrect</strong> because dependency injection occurs <strong>before</strong> <code>BeanPostProcessor</code> methods are executed.</p><ul><li><p>Beans are <strong>instantiated</strong> and <strong>their dependencies are injected</strong> first.</p></li><li><p>Then <code>postProcessBeforeInitialization()</code> and <code>postProcessAfterInitialization()</code> are called.<br><em>Example of lifecycle order:</em></p></li></ul><pre class=\"prettyprint linenums\">Bean Instantiation → Dependency Injection → postProcessBeforeInitialization() → @PostConstruct → InitializingBean.afterPropertiesSet() → postProcessAfterInitialization()\n</pre><p>Since <code>BeanPostProcessor</code> operates after dependencies are injected, this statement is false.</p>",
                    "<p>This is <strong>incorrect</strong> because <code>BeanPostProcessor</code>s can be explicitly ordered using the <code>@Order</code> annotation or by implementing <code>Ordered</code> interface.<br><em>Example:</em></p><pre class=\"prettyprint linenums\">@Component\n@Order(1)\npublic class FirstProcessor implements BeanPostProcessor { ... }\n\n@Component\n@Order(2)\npublic class SecondProcessor implements BeanPostProcessor { ... }\n</pre><p>Here, <code>FirstProcessor</code> will execute <strong>before</strong> <code>SecondProcessor</code>.</p>"
                ],
                "explanation": "<p>The <code>BeanPostProcessor</code> interface in Spring allows for modifying or customizing beans <strong>after</strong> they are instantiated but <strong>before</strong> and <strong>after</strong> initialization. This provides a powerful extension point to modify bean properties dynamically.</p><p><strong>Key Features of </strong><code><strong>BeanPostProcessor</strong></code><strong>:</strong></p><ul><li><p>Works on <strong>all Spring-managed beans</strong> in the application context.</p></li><li><p>Executes <strong>before and after</strong> a bean's initialization.</p></li><li><p>Used for <strong>proxying beans, modifying properties, and lifecycle management</strong>.</p></li><li><p>Commonly used for <strong>AOP (Aspect-Oriented Programming)</strong> and <strong>custom annotations</strong> processing.</p></li></ul><p><strong>Lifecycle of a Bean with </strong><code><strong>BeanPostProcessor</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">plaintext\nCopyEditBean Definition → Instantiation → Dependency Injection → postProcessBeforeInitialization() → @PostConstruct → InitializingBean.afterPropertiesSet() → postProcessAfterInitialization() → Bean Ready for Use\n</pre><p><strong>Implementing a Custom </strong><code><strong>BeanPostProcessor</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">java\nCopyEdit@Component\npublic class CustomBeanPostProcessor implements BeanPostProcessor {\n    @Override\n    public Object postProcessBeforeInitialization(Object bean, String beanName) {\n        if (bean instanceof SomeBean) {\n            System.out.println(\"Before Initialization: Modifying \" + beanName);\n        }\n        return bean;\n    }\n\n    @Override\n    public Object postProcessAfterInitialization(Object bean, String beanName) {\n        if (bean instanceof SomeBean) {\n            System.out.println(\"After Initialization: Modifying \" + beanName);\n        }\n        return bean;\n    }\n}\n</pre><p>This processor will intercept <strong>every bean</strong> and modify it before and after initialization.</p><p><strong>Use Cases of </strong><code><strong>BeanPostProcessor</strong></code><strong>:</strong></p><ul><li><p><strong>Modifying beans dynamically</strong> (e.g., injecting dependencies dynamically).</p></li><li><p><strong>Proxying beans</strong> (e.g., implementing AOP manually).</p></li><li><p><strong>Registering custom initialization logic</strong>.</p></li><li><p><strong>Implementing security checks before bean usage</strong>.</p></li></ul><p><br></p>",
                "answers": [
                    "<p>Custom BeanPostProcessors can be implemented for Spring applications</p>",
                    "<p>BeanPostProcessors are called before the BeanFactoryPostProcessors</p>",
                    "<p>BeanPostProcessors are called during the initialization phase of a bean lifecycle</p>",
                    "<p>BeanPostProcessors are called before the dependencies have been injected</p>",
                    "<p>BeanPostProcessors cannot be ordered in a Spring Boot application</p>"
                ]
            },
            "correct_response": [
                "a",
                "c"
            ],
            "section": "Spring Core",
            "question_plain": "Which two statements are true concerning the BeanPostProcessor Extension point?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571823,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>Which statement defines a pointcut?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring AOP Documentation – Pointcuts](https://docs.spring.io/spring-framework/docs/current/reference/html/core.html#aop-pointcuts)",
                    "[Baeldung – Guide to Spring AOP](https://www.baeldung.com/spring-aop)"
                ],
                "feedbacks": [
                    "<p>This is <strong>incorrect</strong> because a module that encapsulates advices is called an <strong>aspect</strong>, not a pointcut.</p><ul><li><p>In Spring AOP, an <strong>aspect</strong> is a modularized cross-cutting concern (e.g., logging, security).</p></li><li><p>It contains <strong>advice</strong> (code to execute at join points) and <strong>pointcuts</strong> (expressions that select join points).</p></li></ul>",
                    "<p>This is <strong>correct</strong> because a <strong>pointcut</strong> defines where an aspect should be applied by selecting specific join points (e.g., method executions).</p><ul><li><p>Pointcuts are usually written using <strong>AspectJ expressions</strong>.</p></li><li><p>Example of a pointcut selecting all methods in a package:</p><pre class=\"prettyprint linenums\">@Pointcut(\"execution(* com.example.service.*.*(..))\")\npublic void serviceMethods() {}</pre></li></ul>",
                    "<p>This is <strong>incorrect</strong> because this definition describes a <strong>join point</strong>, not a pointcut.</p><ul><li><p>A <strong>join point</strong> represents an execution point in the application (e.g., method call, exception handling, field modification).</p></li><li><p>A <strong>pointcut</strong> selects multiple join points where the advice should run.</p></li></ul>",
                    "<p>This is <strong>incorrect</strong> because code that runs at selected join points is called <strong>advice</strong>, not a pointcut.</p><ul><li><p>A <strong>pointcut</strong> selects join points, but the actual behavior is defined in <strong>advice</strong> (e.g., <code>@Before</code>, <code>@After</code>, <code>@Around</code>).</p></li></ul>"
                ],
                "explanation": "<p><strong>Aspect-Oriented Programming (AOP) Overview</strong></p><p>AOP is used to separate <strong>cross-cutting concerns</strong> (e.g., logging, security, transaction management) from the core business logic.</p><p>The key concepts in Spring AOP are:</p><ul><li><p><strong>Aspect</strong>: A module that contains cross-cutting logic.</p></li><li><p><strong>Advice</strong>: The action taken at a join point (e.g., <code>@Before</code>, <code>@After</code>, <code>@Around</code>).</p></li><li><p><strong>Join Point</strong>: A point in program execution (e.g., method execution).</p></li><li><p><strong>Pointcut</strong>: A <strong>predicate/expression</strong> that selects join points where advice should be applied.</p></li></ul><p><strong>Defining Pointcuts in Spring AOP</strong></p><ul><li><p><strong>Example 1: Selecting all methods in a package</strong></p><pre class=\"prettyprint linenums\">java\nCopyEdit@Pointcut(\"execution(* com.example.service.*.*(..))\")\npublic void serviceMethods() {}\n</pre><p>This pointcut matches <strong>all methods</strong> in <code>com.example.service</code>.</p></li><li><p><strong>Example 2: Selecting methods with a specific annotation</strong></p><pre class=\"prettyprint linenums\">java\nCopyEdit@Pointcut(\"@annotation(org.springframework.transaction.annotation.Transactional)\")\npublic void transactionalMethods() {}\n</pre><p>This selects <strong>methods annotated with </strong><code><strong>@Transactional</strong></code>.</p></li><li><p><strong>Example 3: Combining Pointcuts</strong></p><pre class=\"prettyprint linenums\">java\nCopyEdit@Pointcut(\"execution(* com.example.service.*.*(..))\")\npublic void serviceMethods() {}\n\n@Pointcut(\"@annotation(org.springframework.transaction.annotation.Transactional)\")\npublic void transactionalMethods() {}\n\n@Pointcut(\"serviceMethods() &amp;&amp; transactionalMethods()\")\npublic void transactionalServiceMethods() {}\n</pre><p>This applies advice <strong>only</strong> to service methods that are also transactional.</p></li></ul><p><strong>Applying Advice to a Pointcut</strong></p><ul><li><p>Using <code>@Before</code> advice on a pointcut:</p><pre class=\"prettyprint linenums\">java\nCopyEdit@Aspect\n@Component\npublic class LoggingAspect {\n    @Before(\"execution(* com.example.service.*.*(..))\")\n    public void logBeforeMethod(JoinPoint joinPoint) {\n        System.out.println(\"Executing: \" + joinPoint.getSignature());\n    }\n}\n</pre><p> Here, logging is applied <strong>before every method execution</strong> in the <code>com.example.service</code> package.</p></li></ul><p><br></p>",
                "answers": [
                    "<p> A module that encapsulates advices</p>",
                    "<p>An expression that selects one or more join points</p>",
                    "<p>A point in the execution of a program such as a method call on field assigment</p>",
                    "<p>Code to be executed at each selected join point</p>"
                ]
            },
            "correct_response": [
                "b"
            ],
            "section": "Spring AOP",
            "question_plain": "Which statement defines a pointcut?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571825,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are true regarding constructor injection?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Official Documentation – Constructor Injection:](https://docs.spring.io/spring-framework/docs/current/reference/html/core.html#beans-constructor-injection)",
                    "[Baeldung – Constructor Dependency Injection in Spring](https://www.baeldung.com/constructor-injection-in-spring)",
                    "[Baeldung – Why Is Field Injection Not Recommended?](https://www.baeldung.com/java-spring-field-injection-cons)"
                ],
                "feedbacks": [
                    "<p>This is <strong>correct</strong> because, starting from Spring 4.3, if a class has only <strong>one constructor</strong>, Spring automatically uses it for dependency injection <strong>without requiring </strong><code><strong>@Autowired</strong></code>.<br><em>Example:</em></p><pre class=\"prettyprint linenums\">@Component\npublic class MyService {\n    private final MyRepository myRepository;\n\n    // No @Autowired required since there is only one constructor\n    public MyService(MyRepository myRepository) {\n        this.myRepository = myRepository;\n    }\n}\n</pre><p>If multiple constructors exist, <code>@Autowired</code> must be explicitly used to indicate which one should be used.</p>",
                    "<p>This is <strong>incorrect</strong> because if a class has <strong>multiple constructors</strong>, Spring <strong>does not</strong> automatically know which one to use, and <code>@Autowired</code> must be explicitly declared on one of them.<br><em>Example:</em></p><pre class=\"prettyprint linenums\">@Component\npublic class MyService {\n    private final MyRepository myRepository;\n    private final AnotherDependency anotherDependency;\n\n    // Spring does not know which constructor to use without @Autowired\n    public MyService(MyRepository myRepository) {\n        this.myRepository = myRepository;\n    }\n\n    @Autowired  // Required if there are multiple constructors\n    public MyService(MyRepository myRepository, AnotherDependency anotherDependency) {\n        this.myRepository = myRepository;\n        this.anotherDependency = anotherDependency;\n    }\n}\n</pre><p>Without <code>@Autowired</code>, Spring will throw an exception due to ambiguity.</p>",
                    "<p>This is <strong>correct</strong> because constructor injection promotes <strong>better testability</strong> by allowing dependencies to be injected via constructor parameters instead of relying on Spring’s container.</p><ul><li><p><strong>Advantages of constructor injection for unit testing:</strong></p><ul><li><p>Allows <strong>mocking dependencies easily</strong> without using reflection.</p></li><li><p>Ensures dependencies are always injected (avoiding <code>NullPointerException</code>).</p></li><li><p>Encourages <strong>immutable</strong> dependencies, which is a good design practice.<br><em>Example (constructor injection improves testability):</em></p></li></ul></li></ul><pre class=\"prettyprint linenums\">public class MyService {\n    private final MyRepository myRepository;\n\n    public MyService(MyRepository myRepository) {\n        this.myRepository = myRepository;\n    }\n\n    public String getData() {\n        return myRepository.fetchData();\n    }\n}\n</pre><p><strong>Unit test using constructor injection:</strong></p><pre class=\"prettyprint linenums\">@Test\nvoid testGetData() {\n    MyRepository mockRepository = Mockito.mock(MyRepository.class);\n    Mockito.when(mockRepository.fetchData()).thenReturn(\"Mock Data\");\n\n    MyService myService = new MyService(mockRepository);\n    assertEquals(\"Mock Data\", myService.getData());\n}\n</pre><p>This proves that constructor injection makes unit testing <strong>simpler and more maintainable</strong>.</p><p><br></p>",
                    "<p>This is <strong>incorrect</strong> because constructors can accept multiple parameters for injection.<br><em>Example of multiple dependencies injected via constructor:</em></p><pre class=\"prettyprint linenums\">@Component\npublic class OrderService {\n    private final PaymentService paymentService;\n    private final InventoryService inventoryService;\n\n    public OrderService(PaymentService paymentService, InventoryService inventoryService) {\n        this.paymentService = paymentService;\n        this.inventoryService = inventoryService;\n    }\n}\n</pre><p>This disproves the claim that constructor injection allows only <strong>one</strong> injected value.</p>",
                    "<p>This is <strong>incorrect</strong> because <strong>constructor injection is preferred</strong> for testability.</p><ul><li><p>Field injection makes unit testing harder because dependencies are <strong>private and final</strong>, requiring <strong>reflection</strong> to modify them.</p></li><li><p>Constructor injection avoids reliance on the Spring container, making testing easier.</p></li></ul><p><em>Example of problematic field injection:</em></p><pre class=\"prettyprint linenums\">@Component\npublic class MyService {\n    @Autowired\n    private MyRepository myRepository;  // Hard to mock in tests\n}\n</pre><p><strong>Why this is problematic in unit tests?</strong></p><ul><li><p><strong>Cannot directly inject mocks</strong> without using Spring context.</p></li><li><p><strong>Requires reflection</strong> to modify private fields, making tests more complex.</p></li><li><p>Not easily compatible with constructor-based dependency injection best practices.</p></li></ul>"
                ],
                "explanation": "<p><strong>Dependency Injection (DI) in Spring</strong></p><p>Dependency Injection (DI) is a core concept in Spring that allows <strong>inversion of control (IoC)</strong>, meaning objects do not create their own dependencies but receive them from an external container.</p><p><strong>Types of Dependency Injection in Spring:</strong></p><ul><li><p><strong>Constructor Injection (Recommended)</strong></p><pre class=\"prettyprint linenums\">@Component\npublic class MyService {\n    private final MyRepository myRepository;\n\n    @Autowired\n    public MyService(MyRepository myRepository) {\n        this.myRepository = myRepository;\n    }\n}\n</pre></li><li><p><strong>Setter Injection (Less Preferred)</strong></p><pre class=\"prettyprint linenums\">@Component\npublic class MyService {\n    private MyRepository myRepository;\n\n    @Autowired\n    public void setMyRepository(MyRepository myRepository) {\n        this.myRepository = myRepository;\n    }\n}\n</pre></li><li><p><strong>Field Injection (Not Recommended for Testing)</strong></p><pre class=\"prettyprint linenums\">@Component\npublic class MyService {\n    @Autowired\n    private MyRepository myRepository;\n}\n</pre></li></ul><p><strong>Why Constructor Injection is Preferred?</strong></p><ul><li><p>Encourages <strong>immutable dependencies</strong> (better design).</p></li><li><p><strong>Easier to test</strong> without requiring the Spring container.</p></li><li><p><strong>Avoids issues</strong> with circular dependencies in Spring.</p></li></ul><p><br></p>",
                "answers": [
                    "<p>If there is only one constructor the @Autowired annotation is not required</p>",
                    "<p>Constructor injection can be used with multiple constructors without @Autowired annotation</p>",
                    "<p>Constructor injection is preferred over field injection to support unit testing</p>",
                    "<p>Constructor injection only allows one value to be injected</p>",
                    "<p>Field injection is preferred over constructor injection from a unit testing standpoint</p>"
                ]
            },
            "correct_response": [
                "a",
                "c"
            ],
            "section": "Spring Core",
            "question_plain": "Which two statements are true regarding constructor injection?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571827,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>Which statement is true about REST?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Official Documentation – RESTful APIs with Spring:](https://docs.spring.io/spring-framework/reference/web/webmvc/mvc-controller/ann-methods/responsebody.html)",
                    "[REST API Design Best Practices – Mozilla Developer Network (MDN):](https://developer.mozilla.org/en-US/docs/Learn/Server-side/RESTful_API)",
                    "[RESTful Web Services – Baeldung:](https://www.baeldung.com/rest-api)"
                ],
                "feedbacks": [
                    "<p>This is <strong>incorrect</strong> because REST (<strong>Representational State Transfer</strong>) is an <strong>architectural style</strong>, not a <strong>protocol</strong>.</p><ul><li><p>It is <strong>not a strict set of rules</strong> like HTTP, TCP, or SOAP.</p></li><li><p>REST relies on standard protocols such as <strong>HTTP</strong> for communication but is itself not a protocol.</p></li><li><p>It defines principles like <strong>statelessness, resource-based communication, and uniform interfaces</strong>.</p></li><li><p>Example of a RESTful API request using HTTP:</p><pre class=\"prettyprint linenums\">GET /users/1 HTTP/1.1\nHost: example.com\nAccept: application/json\n</pre></li></ul><p>Since REST is an <strong>architectural style</strong> and not a <strong>protocol</strong>, this statement is false.</p>",
                    "<p>This is <strong>incorrect</strong> because one of the key principles of REST is <strong>statelessness</strong>.</p><ul><li><p>Each request from a client to a server <strong>must contain all necessary information</strong> for processing, and the server <strong>does not store client session state</strong>.</p></li><li><p>If state management is required, it is typically handled <strong>client-side</strong> (e.g., using JWT tokens or cookies).</p></li><li><p>Example: When making multiple RESTful API requests, the server does not remember previous requests:</p><pre class=\"prettyprint linenums\">GET /orders/123 HTTP/1.1\nAuthorization: Bearer abc123token\n</pre><p> Here, the client sends the authentication token <strong>on every request</strong> since the server does not maintain session state.</p></li></ul>",
                    "<p>This statement is <strong>meaningless</strong> in the context of REST principles. REST <strong>follows specific constraints</strong> such as statelessness, resource representation, and a uniform interface.</p><ul><li><p>It is <strong>not relative</strong> but rather <strong>a well-defined architectural style</strong> for building web services.</p></li></ul>",
                    "<p>This is <strong>correct</strong> because RESTful services are designed to be <strong>platform-independent and language-agnostic</strong>, making them highly <strong>interoperable</strong>.</p><ul><li><p>RESTful APIs use <strong>standardized formats</strong> like <strong>JSON, XML, or plain text</strong>, allowing different clients (Java, Python, JavaScript, etc.) to communicate with the same RESTful service.</p></li><li><p>Example: A REST API returning a JSON response:</p><pre class=\"prettyprint linenums\">json\nCopyEdit{\n  \"id\": 1,\n  \"name\": \"John Doe\",\n  \"email\": \"john.doe@example.com\"\n}\n</pre><p> This response can be easily parsed by clients written in different languages.</p></li><li><p>REST’s <strong>interoperability</strong> is one of the key reasons for its widespread adoption in web development.</p></li></ul>"
                ],
                "explanation": "<p>REST is an <strong>architectural style</strong> that defines a set of <strong>constraints</strong> for building scalable and maintainable web services. It is widely used for developing web APIs due to its simplicity and interoperability.</p><p><strong>Key Principles of REST:</strong></p><ul><li><p><strong>Statelessness:</strong> The server does not store client session data; each request must contain all necessary information.</p></li><li><p><strong>Client-Server Architecture:</strong> The client and server are separate entities, improving scalability.</p></li><li><p><strong>Uniform Interface:</strong> REST APIs use standard HTTP methods (<code>GET</code>, <code>POST</code>, <code>PUT</code>, <code>DELETE</code>).</p></li><li><p><strong>Resource-Based:</strong> RESTful APIs represent <strong>resources</strong> using URIs (<code>/users/{id}</code>).</p></li><li><p><strong>Cacheability:</strong> Responses should be cacheable to improve performance.</p></li><li><p><strong>Layered System:</strong> The architecture can include multiple layers (e.g., load balancers, security layers).</p></li></ul><p><strong>Common HTTP Methods in REST:</strong></p><ul><li><p><code>GET</code> – Retrieve a resource.</p></li><li><p><code>POST</code> – Create a new resource.</p></li><li><p><code>PUT</code> – Update an existing resource.</p></li><li><p><code>DELETE</code> – Remove a resource.</p></li></ul><p><strong>Example of a RESTful API Endpoint:</strong></p><pre class=\"prettyprint linenums\">GET /users/1 HTTP/1.1\nHost: api.example.com\nAccept: application/json\n</pre><p>Response:</p><pre class=\"prettyprint linenums\">{\n  \"id\": 1,\n  \"name\": \"John Doe\",\n  \"email\": \"john.doe@example.com\"\n}\n</pre><p><strong>Why REST is Interoperable?</strong></p><ul><li><p>Uses <strong>standard protocols</strong> (HTTP).</p></li><li><p>Supports multiple <strong>data formats</strong> (JSON, XML, etc.).</p></li><li><p>Clients and servers can be implemented in <strong>different languages</strong>.</p></li></ul>",
                "answers": [
                    "<p>REST is a Protocol</p>",
                    "<p>REST is Stateful</p>",
                    "<p>REST is Relative</p>",
                    "<p>REST is Interoperable</p>"
                ]
            },
            "correct_response": [
                "d"
            ],
            "section": "Spring MVC",
            "question_plain": "Which statement is true about REST?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571829,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>What are the two reasons Spring be used to build a Java application?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework Overview – Official Documentation:](https://docs.spring.io/spring-framework/reference/core.html)",
                    "[Spring Boot Features – Official Guide:](https://docs.spring.io/spring-boot/reference/features.html)"
                ],
                "feedbacks": [
                    "<p>This is <strong>incorrect</strong> because Spring <strong>does not handle deployment automation directly</strong>. While <strong>Spring Boot</strong> and <strong>Spring Cloud</strong> provide integrations with cloud platforms (e.g., AWS, Azure, Google Cloud), <strong>actual deployment automation</strong> is handled by external tools like Kubernetes, Docker, or CI/CD pipelines (e.g., Jenkins, GitHub Actions).</p>",
                    "<p>This is <strong>incorrect</strong> because <strong>Spring itself does not provide IDE support</strong>. However, IDEs like IntelliJ IDEA, Eclipse, and VS Code offer <strong>Spring-related plugins</strong> (e.g., Spring Tools Suite - STS) to facilitate development. IDE support comes from third-party tools, not Spring itself.</p>",
                    "<p>This is <strong>correct</strong> because Spring simplifies <strong>infrastructure concerns</strong> such as:</p><ul><li><p><strong>Persistence (Spring Data, JPA, JDBC)</strong> → Abstracts database access.</p></li><li><p><strong>Messaging (Spring Kafka, Spring JMS, Spring AMQP)</strong> → Provides abstraction over messaging systems like Kafka, RabbitMQ.</p></li><li><p><strong>Security (Spring Security)</strong> → Manages authentication and authorization.</p></li><li><p><strong>Caching (Spring Cache)</strong> → Integrates with cache providers (e.g., Redis).<br>Example: Using Spring Data JPA to abstract database access:</p></li></ul><pre class=\"prettyprint linenums\">@Repository\npublic interface UserRepository extends JpaRepository&lt;User, Long&gt; {\n    List&lt;User&gt; findByLastName(String lastName);\n}\n</pre><p>This eliminates the need to write SQL queries manually.</p>",
                    "<p>This is <strong>incorrect</strong> because build automation is handled by tools like <strong>Maven or Gradle</strong>, not Spring. Spring <strong>integrates</strong> with these tools but does not replace them.</p><ul><li><p>Example of a <strong>Spring Boot application build</strong> using Maven:</p><pre class=\"prettyprint linenums\">mvn clean package</pre></li></ul>",
                    "<p>This is <strong>correct</strong> because Spring <strong>manages bean lifecycles and dependency injection</strong> via the <strong>Spring IoC (Inversion of Control) container</strong>.</p><ul><li><p>Spring allows for <strong>constructor, setter, and field injection</strong>, promoting <strong>loose coupling</strong> and <strong>better testability</strong>.</p></li><li><p>Example of dependency injection using Spring:</p><pre class=\"prettyprint linenums\">@Component\npublic class OrderService {\n    private final PaymentService paymentService;\n\n    @Autowired\n    public OrderService(PaymentService paymentService) {\n        this.paymentService = paymentService;\n    }\n}\n</pre><p> Here, Spring <strong>automatically injects</strong> the <code>PaymentService</code> dependency.</p></li></ul>"
                ],
                "explanation": "<p>Spring is a <strong>lightweight, modular, and extensible</strong> framework designed to simplify Java application development. It provides:</p><ul><li><p><strong>Inversion of Control (IoC) and Dependency Injection (DI)</strong> → Eliminates manual dependency management.</p></li><li><p><strong>Abstractions over common infrastructure concerns</strong> (persistence, messaging, caching).</p></li><li><p><strong>Spring Boot for rapid application development</strong> → Eliminates boilerplate code.</p></li><li><p><strong>Spring Cloud for microservices architecture</strong> → Simplifies distributed system development.</p></li><li><p><strong>Security, Testing, and Integration support</strong> → Includes authentication, testing utilities, and external system integrations.</p></li></ul>",
                "answers": [
                    "<p>Spring automated deployment of Java applications to all of the major cloud providers</p>",
                    "<p>Spring provides comprehensive Java IDE support</p>",
                    "<p>Spring provides abstraction over infrastructure such as persistence and messaging</p>",
                    "<p>Spring automates a Java application build</p>",
                    "<p>Spring provides a Dependency Injection container</p>"
                ]
            },
            "correct_response": [
                "c",
                "e"
            ],
            "section": "Spring Core",
            "question_plain": "What are the two reasons Spring be used to build a Java application?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571831,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which three statements are correct regarding the customization of Spring Boot auto-configuration?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot - Auto-Configuration](https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.developing-auto-configuration)",
                    "[Annotation Interface AutoConfigureOrder](https://docs.spring.io/spring-boot/api/java/org/springframework/boot/autoconfigure/AutoConfigureOrder.html)",
                    "[Spring Boot - Custom Auto-Configuration](https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.developing-auto-configuration.custom)"
                ],
                "feedbacks": [
                    "<p>Correct, the <code>@AutoConfigureAfter</code> and <code>@AutoConfigureBefore</code> annotations allow developers to specify the order in which auto-configuration classes are applied. This is useful when certain configurations must be applied before or after others.</p><ul><li><p>Example:</p><pre class=\"prettyprint linenums\">@Configuration\n@AutoConfigureAfter(SomeOtherAutoConfiguration.class)\npublic class MyAutoConfiguration {\n    // Custom auto-configuration logic\n}</pre></li></ul>",
                    "<p>Correct, <code>@AutoConfigureOrder</code> lets you assign a precedence value to an auto-configuration class so Spring Boot applies it in the desired order.</p>",
                    "<p><code><strong>@EnableAutoConfiguration</strong></code><strong> annotation.</strong><br>Correct, specific auto-configuration classes can be disabled by using the <code>exclude</code> attribute on the <code>@EnableAutoConfiguration</code> annotation or its alias, <code>@SpringBootApplication</code>.</p><ul><li><p>Example:</p><pre class=\"prettyprint linenums\">@SpringBootApplication(exclude = {DataSourceAutoConfiguration.class})\npublic class MyApplication {\n    // Application code\n}</pre></li></ul>",
                    "<p>Incorrect, subclassing existing auto-configuration classes is not the recommended way to customize auto-configuration. Instead, developers should create their own configuration classes or use conditional annotations like <code>@ConditionalOnProperty</code>.</p>",
                    "<p>Incorrect, auto-configuration classes should not enable component scanning. Instead, they should define beans programmatically or conditionally using annotations like <code>@ConditionalOnClass</code> or <code>@ConditionalOnMissingBean</code>.</p>"
                ],
                "explanation": "<p>Key Mechanisms for Customizing Auto-Configuration:</p><p><strong>Order Control:</strong></p><ul><li><p>Use <code>@AutoConfigureAfter</code> and <code>@AutoConfigureBefore</code> to specify the relative order in which auto-configuration classes are applied.</p></li><li><p>Use <code>@AutoConfigureOrder</code> to define an explicit numerical order for auto-configuration classes. Lower values have higher priority. This is useful when you need deterministic ordering across multiple custom auto-configurations.</p></li></ul><p><strong>Example:</strong></p><pre class=\"prettyprint linenums\">@AutoConfiguration\n@AutoConfigureOrder(Ordered.HIGHEST_PRECEDENCE)\npublic class MyHighPriorityAutoConfiguration {\n    // Beans defined here are applied before others\n}\n</pre><p><strong>Disabling Auto-Configuration:</strong><br>Use the <code>exclude</code> attribute on the <code>@EnableAutoConfiguration</code> or <code>@SpringBootApplication</code> annotation to disable specific auto-configuration classes.</p><pre class=\"prettyprint linenums\">@SpringBootApplication(exclude = { SecurityAutoConfiguration.class })\npublic class MyApplication {\n    // Application code\n}\n</pre><p><strong>Creating Custom Auto-Configuration:</strong><br>Developers can create custom auto-configuration classes by defining beans programmatically and using conditional annotations like <code>@ConditionalOnClass</code>, <code>@ConditionalOnProperty</code>, or <code>@ConditionalOnMissingBean</code>.</p><p><strong>Avoid Subclassing:</strong><br>Subclassing existing auto-configuration classes is not recommended. Instead, create new configuration classes or use conditional annotations to customize behavior.</p><p><strong>Avoid Component Scanning:</strong><br>Auto-configuration classes should not enable component scanning. Instead, they should define beans explicitly and conditionally.</p>",
                "answers": [
                    "<p>Use the @AutoConfigureAfter or @AutoConfigureBefore annotations to apply configuration in significant order</p>",
                    "<p>Control the order of auto-configuration classes applied with @AutoConfigureOrder</p>",
                    "<p>Disable specific auto-configuration classes by using the exclude attribute on the @EnableAutoConfiguration annotation</p>",
                    "<p>Provide customized auto-configuration by subclassing the provided Spring Boot auto-configuration classes</p>",
                    "<p>Enable component scanning within auto-configuration classes to find necessary components</p>"
                ]
            },
            "correct_response": [
                "a",
                "c",
                "b"
            ],
            "section": "Spring Boot",
            "question_plain": "Which three statements are correct regarding the customization of Spring Boot auto-configuration?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571833,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two options are REST principles?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[RESTful Web Services – Official MDN Guide:](https://developer.mozilla.org/en-US/docs/Glossary/REST)",
                    "[Understanding Statelessness in RESTful APIs: Principles, Benefits, and Best Practices:](https://aditya-sunjava.medium.com/understanding-statelessness-in-restful-apis-principles-benefits-and-best-practices-023c8aa4aa3c)",
                    "[REST API Principles – A Comprehensive Overview:](https://blog.dreamfactory.com/rest-apis-an-overview-of-basic-principles)"
                ],
                "feedbacks": [
                    "<p>This is <strong>incorrect</strong> because REST follows the <strong>stateless</strong> principle, meaning the server <strong>does not maintain any client state</strong> between requests. Each request must contain all the necessary information for processing.</p><ul><li><p>Example:</p><pre class=\"prettyprint linenums\">GET /user/1 HTTP/1.1\nAuthorization: Bearer abc123token\n</pre><p> The server processes this request <strong>without remembering previous interactions</strong>. If the client needs session-like behavior, it should manage state <strong>client-side</strong> (e.g., using tokens, cookies, or local storage).</p></li></ul>",
                    "<p>This is <strong>incorrect</strong> because REST is designed for <strong>loose coupling</strong> between clients and servers.</p><ul><li><p>The client and server interact through <strong>standardized APIs</strong> using <strong>resource URIs, HTTP methods, and media types (JSON, XML, etc.)</strong>, reducing dependencies.</p></li><li><p>RESTful services allow clients to evolve independently of servers, as long as the API contract remains consistent.</p></li></ul>",
                    "<p>This is <strong>incorrect</strong> because caching is a <strong>core principle</strong> of REST.</p><ul><li><p>RESTful services <strong>should</strong> use caching mechanisms to improve performance and reduce server load.</p></li><li><p>HTTP caching is controlled via headers such as <code>Cache-Control</code> and <code>ETag</code>.</p></li><li><p>Example response with caching headers:</p><pre class=\"prettyprint linenums\">HTTP/1.1 200 OK\nCache-Control: max-age=3600\nETag: \"34c5a9c\"\n</pre><p> This allows clients to cache the response for <strong>one hour</strong>, improving efficiency.</p></li></ul>",
                    "<p>This is <strong>correct</strong> because REST enforces <strong>statelessness</strong>, meaning each request is <strong>independent</strong> and contains all necessary information.</p><ul><li><p>There is <strong>no session state</strong> stored on the server, making REST scalable.</p></li><li><p>Statelessness allows REST to work efficiently in <strong>distributed and load-balanced environments</strong>.</p></li></ul>",
                    "<p>This is <strong>correct</strong> because REST uses <strong>standard HTTP mechanisms</strong> to communicate between clients and servers.</p><ul><li><p><strong>HTTP headers</strong> are used for metadata (e.g., content type, authentication, caching).</p></li><li><p><strong>HTTP status codes</strong> inform clients about the result of their request.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">http\nCopyEditHTTP/1.1 201 Created\nLocation: /users/123\n</pre><p> This status code (<code>201 Created</code>) tells the client that the request was successful and provides a reference to the created resource.</p></li></ul>"
                ],
                "explanation": "<p>REST (<strong>Representational State Transfer</strong>) is an architectural style for designing web services that relies on <strong>standard web protocols (HTTP)</strong>. It promotes:</p><ul><li><p><strong>Statelessness:</strong> Each request must contain all necessary information.</p></li><li><p><strong>Client-Server Separation:</strong> The UI and backend are <strong>decoupled</strong>.</p></li><li><p><strong>Uniform Interface:</strong> APIs should follow consistent patterns using HTTP methods.</p></li><li><p><strong>Cacheability:</strong> Clients and intermediaries should cache responses when possible.</p></li><li><p><strong>Layered System:</strong> Requests can pass through intermediaries (e.g., proxies, load balancers).</p></li></ul><p><strong>Key REST Design Practices:</strong></p><ul><li><p>Use <strong>resource URIs</strong>: <code>/users/{id}</code>, <code>/orders/{id}</code></p></li><li><p>Use <strong>HTTP methods properly</strong>:</p><ul><li><p><code>GET</code> → Retrieve a resource</p></li><li><p><code>POST</code> → Create a resource</p></li><li><p><code>PUT</code> → Update a resource</p></li><li><p><code>DELETE</code> → Remove a resource</p></li></ul></li><li><p>Use <strong>appropriate HTTP status codes</strong> (<code>200 OK</code>, <code>404 Not Found</code>, <code>500 Internal Server Error</code>).</p></li></ul><p><strong>Example REST API Interaction:</strong></p><p><strong>Client Request:</strong></p><pre class=\"prettyprint linenums\">http\nCopyEditGET /users/1 HTTP/1.1\nAccept: application/json\n</pre><p><strong>Server Response:</strong></p><pre class=\"prettyprint linenums\">http\nCopyEditHTTP/1.1 200 OK\nContent-Type: application/json\nCache-Control: max-age=3600\n{\n  \"id\": 1,\n  \"name\": \"Alice\",\n  \"email\": \"alice@example.com\"\n}\n</pre><ul><li><p>The response follows REST principles:</p><ul><li><p>Uses <strong>stateless</strong> communication.</p></li><li><p>Uses <strong>standard HTTP headers</strong> (<code>Cache-Control</code>, <code>Content-Type</code>).</p></li><li><p>Uses a <strong>meaningful status code</strong> (<code>200 OK</code>).</p></li></ul></li></ul>",
                "answers": [
                    "<p>REST-ful application servers keep track of the client state</p>",
                    "<p>REST-ful applications favor tight coupling between the clients and the servers</p>",
                    "<p>REST-ful applications cannot use caching</p>",
                    "<p>REST-ful applications use a stateless architecture</p>",
                    "<p>REST-ful application use HTTP headers and status codes as a contract with the clients</p>"
                ]
            },
            "correct_response": [
                "d",
                "e"
            ],
            "section": "Spring MVC",
            "question_plain": "Which two options are REST principles?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571835,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements describe Spring JdbcTemplate?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework Reference Documentation](https://docs.spring.io/spring-framework/reference/data-access/jdbc/core.html)",
                    "[Spring JdbcTemplate Example - DigitalOcean](https://www.digitalocean.com/community/tutorials/spring-jdbctemplate-example)"
                ],
                "feedbacks": [
                    "<p>This statement is correct. <code>JdbcTemplate</code> offers various methods to execute SQL queries, such as <code>queryForObject()</code>, <code>query()</code>, and <code>update()</code>, facilitating interaction with the database.</p>",
                    "<p>This statement is incorrect. <code>JdbcTemplate</code> can perform various database operations, including <code>INSERT</code>, <code>UPDATE</code>, and <code>DELETE</code> statements, allowing for comprehensive data manipulation.</p>",
                    "<p>This statement is incorrect. <code>JdbcTemplate</code> catches <code>SQLException</code> instances and translates them into unchecked exceptions within the <code>org.springframework.dao</code> hierarchy, relieving developers from mandatory exception handling.</p>",
                    "<p>This statement is correct. <code>JdbcTemplate</code> facilitates iteration over <code>ResultSet</code> instances and extraction of returned parameter values, simplifying result set handling.</p>",
                    "<p>This statement is incorrect. <code>JdbcTemplate</code> does not generate SQL statements; it executes SQL provided by the developer. Developers are responsible for writing the SQL queries that <code>JdbcTemplate</code> executes.</p>"
                ],
                "explanation": "<p><code>JdbcTemplate</code> is a central class in Spring's JDBC module that simplifies database interactions. It manages the creation and release of resources, executes SQL queries, updates statements, and handles stored procedure calls. By abstracting the boilerplate code associated with standard JDBC operations, <code>JdbcTemplate</code> allows developers to focus on writing SQL and processing results.</p><p><strong>Key Features of </strong><code><strong>JdbcTemplate</strong></code><strong>:</strong></p><ul><li><p><strong>Resource Management:</strong> Automatically handles the creation and release of database resources, such as connections and statements, reducing the risk of resource leaks.​<a href=\"https://springframework.guru/spring-jdbctemplate-crud-operations/\">springframework.guru+1docs.spring.io+1</a></p></li><li><p><strong>Exception Handling:</strong> Catches <code>SQLException</code> instances and translates them into unchecked exceptions within the <code>org.springframework.dao</code> hierarchy, providing a consistent exception handling mechanism.​</p></li><li><p><strong>Query Execution:</strong> Provides methods like <code>queryForObject()</code>, <code>query()</code>, and <code>update()</code> to execute SQL queries and updates, streamlining database operations.​<a href=\"https://mkyong.com/spring/spring-jdbctemplate-querying-examples/\">mkyong.com+1docs.spring.io+1</a></p></li><li><p><strong>Result Set Processing:</strong> Facilitates iteration over <code>ResultSet</code> instances and extraction of returned parameter values, simplifying data retrieval and processing.​<a href=\"https://docs.spring.io/spring-framework/reference/data-access/jdbc/core.html\">docs.spring.io+2docs.spring.io+2TutorialsPoint+2</a></p></li></ul><p><strong>Example Usage of </strong><code><strong>JdbcTemplate</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">// Querying for a single object\nString sql = \"SELECT name FROM users WHERE id = ?\";\nString name = jdbcTemplate.queryForObject(sql, new Object[]{id}, String.class);\n</pre><p><br></p><p>In this example, <code>queryForObject()</code> executes the SQL query and maps the result to a <code>String</code>. The method handles the <code>ResultSet</code> internally, providing a streamlined approach to data retrieval.</p>",
                "answers": [
                    "<p>The JdbcTemplate provides methods for query execution</p>",
                    "<p>The JdbcTemplate can only perform update but not insert to the database</p>",
                    "<p>All JdbcTemplate&nbsp; methods throw SQLException which are required to handle</p>",
                    "<p>The JdbcTemplate provides the ability to work with result sets</p>",
                    "<p>The JdbcTemplate generates SQL statements</p>"
                ]
            },
            "correct_response": [
                "a",
                "d"
            ],
            "section": "Spring Data JPA",
            "question_plain": "Which two statements describe Spring JdbcTemplate?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571837,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are correct regarding the @EnableAutoConfiguration annotation?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Reference Guide – Auto-configuration:](https://docs.spring.io/spring-boot/reference/using/auto-configuration.html)",
                    "[Spring Boot API Documentation – @EnableAutoConfiguration:](https://docs.spring.io/spring-boot/api/java/org/springframework/boot/autoconfigure/EnableAutoConfiguration.html)"
                ],
                "feedbacks": [
                    "<p>Correct, <code>@EnableAutoConfiguration</code> loads configuration classes via spring.factories, so its effect does not depend on the package of the annotated class.</p>",
                    "<p>This statement is incorrect. Auto-configuration is applied <strong>after</strong> user-defined beans have been registered. This design allows developers to override the default configurations provided by Spring Boot with their custom bean definitions.</p>",
                    "<p>This statement is correct. The <code>@SpringBootApplication</code> annotation is a composite annotation that includes <code>@EnableAutoConfiguration</code>, <code>@ComponentScan</code>, and <code>@Configuration</code>. Therefore, <code>@EnableAutoConfiguration</code> is a meta-annotation within <code>@SpringBootApplication</code>.</p>",
                    "<p>This statement is incorrect. While <code>@SpringBootConfiguration</code> is a specialization of <code>@Configuration</code>, it does not compose <code>@EnableAutoConfiguration</code>. Instead, <code>@EnableAutoConfiguration</code> is directly included in <code>@SpringBootApplication</code>.</p>",
                    "<p>This statement is correct. The <code>@EnableAutoConfiguration</code> annotation guides Spring Boot to automatically configure the application context by inferring and setting up beans based on the application's classpath and defined beans.</p>"
                ],
                "explanation": "<p>The <code>@EnableAutoConfiguration</code> annotation is a pivotal feature in Spring Boot that simplifies the configuration process by automatically setting up the Spring application context. It scans the classpath and attempts to configure beans that are likely needed based on the dependencies present. For example, if <code>spring-boot-starter-web</code> is included in the project, Spring Boot will automatically configure components like Tomcat and Spring MVC.</p><p><strong>Key Aspects of </strong><code><strong>@EnableAutoConfiguration</strong></code><strong>:</strong></p><ul><li><p><strong>Automatic Bean Configuration:</strong> Spring Boot scans the classpath and configures beans based on the dependencies and settings, reducing the need for explicit bean definitions.​</p></li><li><p><strong>Customization and Exclusions:</strong> Developers can exclude specific auto-configuration classes using the <code>exclude</code> or <code>excludeName</code> attributes of the annotation, allowing for customization of the application context. ​<a href=\"https://docs.spring.io/spring-boot/reference/using/auto-configuration.html\">docs.spring.io</a></p></li><li><p><strong>Integration with </strong><code><strong>@SpringBootApplication</strong></code><strong>:</strong> The <code>@SpringBootApplication</code> annotation combines <code>@EnableAutoConfiguration</code>, <code>@ComponentScan</code>, and <code>@Configuration</code>, streamlining the setup of Spring applications.</p></li></ul>",
                "answers": [
                    "<p>It has the same effect regardless of the package of the classes that is annotated with it</p>",
                    "<p>It ensures auto configuration is applied before user defined beans have been registered</p>",
                    "<p>It is a meta annotation on the @SpringBootApplication composed annotation</p>",
                    "<p>It is a meta annotation on the @SpringBootConfiguration composed annotation</p>",
                    "<p>It enables auto-configuration of the ApplicationContext by attempting to guess necessary beans</p>"
                ]
            },
            "correct_response": [
                "c",
                "e",
                "a"
            ],
            "section": "Spring Boot",
            "question_plain": "Which two statements are correct regarding the @EnableAutoConfiguration annotation?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571839,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p><code>public interface CustomerRepository extends CrudRepository&lt;Customer, Long&gt; {}</code></p><p>Which statement is true?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Introduction to Spring Data JPA | Baeldung](https://www.baeldung.com/the-persistence-layer-with-spring-data-jpa)",
                    "[Spring Boot JpaRepository with Example | GeeksforGeeks:](https://www.geeksforgeeks.org/spring-boot-jparepository-with-example/)",
                    "[JpaRepository — How is it implemented? | Level Up Coding:](https://levelup.gitconnected.com/jparepository-how-is-it-implemented-2afc6751b47e)"
                ],
                "feedbacks": [
                    "<p>This statement is incorrect. In Spring Data JPA, you only need to define the repository interface (e.g., <code>CustomerRepository</code>), and Spring automatically provides the implementation at runtime. There's no need for a manual implementation or explicit bean declaration.</p>",
                    "<p>This statement is correct. Spring Data JPA automatically generates implementations for repository interfaces like <code>CustomerRepository</code> at runtime, allowing developers to focus on defining repository methods without providing explicit implementations.</p>",
                    "<p>This statement is incorrect. In Spring Data JPA, repositories are defined as interfaces. Spring generates the necessary implementation classes at runtime, so there's no need to define repository classes manually.</p>",
                    "<p>This statement is incorrect. JPA annotations are used with Spring Data JPA. If you're using Spring Data JDBC, you would use JDBC-specific annotations instead. Mixing JPA annotations with Spring Data JDBC is not appropriate.</p>"
                ],
                "explanation": "<p>Spring Data JPA simplifies data access by reducing boilerplate code required for database interactions. By defining repository interfaces that extend <code>JpaRepository</code> or other relevant interfaces, Spring automatically provides implementations at runtime. This approach allows developers to focus on business logic rather than data access infrastructure.</p><p><strong>Key Features of Spring Data JPA Repositories:</strong></p><ul><li><p><strong>Automatic Implementation:</strong> Spring generates repository implementations at runtime based on the defined interfaces.​</p></li><li><p><strong>Query Methods:</strong> Developers can define query methods in the repository interface, and Spring will automatically implement them based on method names or custom queries.​</p></li><li><p><strong>Custom Implementations:</strong> If needed, developers can provide custom implementations for specific repository methods.​</p></li></ul>",
                "answers": [
                    "<p>A class that implements CustomerRepository must be implemented and declared as Spring Bean</p>",
                    "<p>An implementation of this repository can be automatically generated by Spring Data JPA</p>",
                    "<p>CustomerRepository should be a class, not an interface</p>",
                    "<p>Jpa annotations are required on the Customer Class to successfully use Spring Data JDBC</p>"
                ]
            },
            "correct_response": [
                "b"
            ],
            "section": "Spring Data",
            "question_plain": "public interface CustomerRepository extends CrudRepository&lt;Customer, Long&gt; {}Which statement is true?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571841,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are true regarding bean creation?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework Reference Documentation – Using @Autowired](https://docs.spring.io/spring-framework/reference/core/beans/annotation-config/autowired.html)",
                    "[Understanding @Autowired in Spring: A Comprehensive Guide](https://dev.to/tiuwill/understanding-autowired-in-spring-a-comprehensive-guide-1i6k)",
                    "[How To Simplify Dependency Injection with @Autowired](https://springframework.guru/autowiring-in-spring/)"
                ],
                "feedbacks": [
                    "<p>This statement is correct. Annotating a class with <code>@Component</code> marks it as a candidate for component scanning, allowing Spring to detect and register it as a bean automatically. This process simplifies bean creation by eliminating the need for explicit configuration.</p>",
                    "<p>This statement is incorrect. The <code>@Autowired</code> annotation is used for dependency injection, not for bean creation. It indicates that a dependency should be automatically injected by Spring's dependency injection facilities.</p>",
                    "<p>This statement is incorrect. While <code>@Autowired</code> is used to inject dependencies into methods or fields, it does not create beans. Bean creation is managed through annotations like <code>@Component</code> or <code>@Bean</code>.</p>",
                    "<p>This statement is correct. Defining methods annotated with <code>@Bean</code> within a class annotated with <code>@Configuration</code> allows for explicit creation of beans. This approach provides fine-grained control over bean instantiation and configuration.</p>",
                    "<p>This statement is incorrect. The <code>@Bean</code> annotation is applied to methods, not classes. It indicates that the method will return an object that should be registered as a bean in the Spring application context.</p>"
                ],
                "explanation": "<p>In Spring, beans can be created and managed in various ways, primarily through annotations and configuration classes.</p><ul><li><p><strong>Implicit Bean Creation with </strong><code><strong>@Component</strong></code><strong>:</strong> Annotating a class with <code>@Component</code> makes it a candidate for component scanning. When Spring's component scanner detects this annotation, it automatically registers the class as a bean in the application context. This method promotes a convention-over-configuration approach, reducing the need for explicit bean definitions.​</p></li><li><p><strong>Explicit Bean Creation with </strong><code><strong>@Bean</strong></code><strong>:</strong> Within a class annotated with <code>@Configuration</code>, methods annotated with <code>@Bean</code> define beans explicitly. This approach offers precise control over bean instantiation, initialization, and dependencies, allowing for complex configurations and custom logic during bean creation.​</p></li><li><p><strong>Dependency Injection with </strong><code><strong>@Autowired</strong></code><strong>:</strong> The <code>@Autowired</code> annotation is used to inject dependencies into a bean's properties, methods, or constructors. It allows Spring to resolve and inject collaborating beans into the bean, facilitating loose coupling and enhancing testability. However, <code>@Autowired</code> does not create beans; it only injects existing beans managed by Spring.</p></li></ul>",
                "answers": [
                    "<p>A Spring bean can be implicitly created by annotating the class with @Component and using the component scanner to scan its package</p>",
                    "<p>A Spring bean can be explicitly created by annotating the class with @Autowired</p>",
                    "<p>A Spring bean can be explicitly created by annotating methods or fields by @Autowired</p>",
                    "<p>A Spring bean can be&nbsp; explicitly created using @Bean annotated methods within a Spring configuration class</p>",
                    "<p>A Spring bean can be implicitly created by annotating the class with @Bean and using the component scanner to scan its package</p>"
                ]
            },
            "correct_response": [
                "a",
                "d"
            ],
            "section": "Spring Core",
            "question_plain": "Which two statements are true regarding bean creation?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571843,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are true about Spring Boot and Spring Data JPA?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring @EntityScan vs. @ComponentScan | Baeldung](https://www.baeldung.com/spring-entityscan-vs-componentscan)",
                    "[Configure Hibernate with Spring Boot - HowToDoInJava](https://howtodoinjava.com/spring-boot2/hibernate-configuration-example/)",
                    "[Spring Boot Entity Scanning - DZone](https://dzone.com/articles/spring-boot-entity-scan)"
                ],
                "feedbacks": [
                    "<p>This statement is incorrect. By default, Spring Boot initializes embedded databases during startup, which includes creating schemas and populating data as defined in <code>schema.sql</code> and <code>data.sql</code> files. This behavior ensures that the database is in a known state each time the application starts.</p>",
                    "<p>This statement is incorrect. While Spring Data JPA is a popular choice for interacting with relational databases, it is not the only implementation. Other modules, such as Spring Data JDBC, provide alternative approaches for database interaction.</p>",
                    "<p>This statement is incorrect. Spring Boot allows customization of the packages to scan for JPA entities using the <code>@EntityScan</code> annotation. This annotation specifies the base packages to scan, preventing the need to scan the entire classpath.</p>",
                    "<p>This statement is correct. The <code>@EntityScan</code> annotation allows developers to specify the packages to scan for JPA entities, while <code>spring.jpa.*</code> properties in the application configuration enable customization of various JPA settings, such as the database platform and Hibernate properties.</p>",
                    "<p>This statement is correct. Spring Data JPA allows passing Hibernate-specific properties through the <code>spring.jpa.properties.*</code> namespace in the application configuration. This feature enables fine-tuned customization of Hibernate behavior within a Spring Boot application.</p>"
                ],
                "explanation": "<p>Spring Boot and Spring Data JPA provide flexible mechanisms to customize the behavior of data access layers in applications.</p><ul><li><p><strong>Entity Scanning with </strong><code><strong>@EntityScan</strong></code><strong>:</strong> By default, Spring Boot scans the package of the main application class and its sub-packages for JPA entities. If entities are located outside these packages, the <code>@EntityScan</code> annotation can be used to specify the exact packages to scan, ensuring that all entity classes are detected and managed by the EntityManager. ​<a href=\"https://www.baeldung.com/spring-entityscan-vs-componentscan\">baeldung.com</a></p></li><li><p><strong>Configuring JPA Properties:</strong> Spring Boot allows customization of JPA and Hibernate behavior through properties defined in the application configuration file (e.g., <code>application.properties</code> or <code>application.yml</code>). Properties prefixed with <code>spring.jpa.</code> control various aspects of JPA setup, such as the database platform (<code>spring.jpa.database-platform</code>) and Hibernate-specific settings (<code>spring.jpa.properties.*</code>). This approach provides a centralized and declarative way to adjust the JPA provider's behavior.​</p></li></ul>",
                "answers": [
                    "<p>Embedded Databases (H2, HSQLDB, Derby) are not re-created during the startup</p>",
                    "<p>Spring Data JPA is the only implementation for relational databases</p>",
                    "<p>Scanning of JPA Entities can not be customized the whole classpath is scanned</p>",
                    "<p>@EntityScan and spring.jpa.* properties can be used to customize Spring Data JPA</p>",
                    "<p>Any kind of Hibernate property can be passed to Spring Data JPA like spring.jpa.properties.xxx.</p>"
                ]
            },
            "correct_response": [
                "d",
                "e"
            ],
            "section": "Spring Data JPA",
            "question_plain": "Which two statements are true about Spring Boot and Spring Data JPA?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571845,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two annotations indicate that the transaction for a transactional test method should be committed after the test method has completed?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[@Commit Annotation – Spring Framework Documentation](https://docs.spring.io/spring-framework/reference/testing/annotations/integration-spring/annotation-commit.html)",
                    "[@Rollback Annotation – Spring Framework Documentation](https://docs.spring.io/spring-framework/reference/testing/annotations/integration-spring/annotation-rollback.html)",
                    "[Programmatic Transactions in the Spring TestContext Framework – Baeldung](https://www.baeldung.com/spring-test-programmatic-transactions)"
                ],
                "feedbacks": [
                    "<p>This statement is incorrect. The <code>@Transactional</code> annotation does not have a <code>commit</code> attribute. Transactional behavior, such as commit or rollback, is controlled by annotations like <code>@Commit</code> or <code>@Rollback</code></p>",
                    "<p>This statement is correct. The <code>@Commit</code> annotation indicates that the transaction for a transactional test method should be committed after the test method has completed. It can be used at both the class and method levels to explicitly specify commit behavior.</p>",
                    "<p>This statement is correct. The <code>@Rollback</code> annotation, when set to <code>false</code>, specifies that the transaction for a test method should not be rolled back after completion, effectively committing the transaction. This annotation can also be applied at both the class and method levels.</p>",
                    "<p>This statement is incorrect. The <code>@Sql</code> annotation is used to execute SQL scripts before or after test methods but does not have an <code>alwaysCommit</code> attribute. It does not control transaction commit behavior directly.</p>",
                    "<p>This statement is incorrect. The <code>@SqlMergeMode</code> annotation determines how SQL scripts are merged when multiple <code>@Sql</code> annotations are present but does not influence transaction commit or rollback behavior.</p>"
                ],
                "explanation": "<p>In Spring's testing framework, test methods annotated with <code>@Transactional</code> are executed within a transaction that, by default, is rolled back upon completion. This default behavior ensures that changes made during the test do not affect the actual database state, maintaining test isolation.</p><p>However, there are scenarios where committing the transaction after a test method is desirable, such as when verifying the effects of a commit operation. To achieve this, Spring provides the <code>@Commit</code> and <code>@Rollback</code> annotations:</p><ul><li><p><code><strong>@Commit</strong></code><strong>:</strong> When applied to a test method or class, it instructs the Spring TestContext Framework to commit the transaction after the test method completes. This annotation serves as a clear indicator of the intent to commit transactions in tests. ​<a href=\"https://stackoverflow.com/questions/53063400/commit-internal-transactions-while-debugging-junit-tests-in-spring-boot\">stackoverflow.com</a></p></li><li><p><code><strong>@Rollback(false)</strong></code><strong>:</strong> Setting the <code>value</code> attribute of the <code>@Rollback</code> annotation to <code>false</code> similarly directs the framework to commit the transaction after test execution. While functionally equivalent to <code>@Commit</code>, using <code>@Commit</code> is often preferred for its explicitness. ​</p></li></ul>",
                "answers": [
                    "<p>@Transactional(commit=true)</p>",
                    "<p>@Commit</p>",
                    "<p>@Rollback(false)</p>",
                    "<p>@Sql(alwaysCommit=true)</p>",
                    "<p>@SqlMergeMode(false)</p>"
                ]
            },
            "correct_response": [
                "b",
                "c"
            ],
            "section": "Testing",
            "question_plain": "Which two annotations indicate that the transaction for a transactional test method should be committed after the test method has completed?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571847,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<pre class=\"prettyprint linenums\">@Configuration\n@ConditionalOnClass(HelloService.class)\npublic class HelloAutoConfig {\n\n    @ConditionalOnMissingBean(HelloService.class)\n    @Bean\n    HelloService helloService() {\n        return new TypicalHelloService();\n    }\n}</pre><p>Which two statements are correct regarding the HelloAutoConfig auto-configuration class when it is specified in the META-INF.spring.factories file?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Reference Documentation – Developing Auto-Configuration](https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.developing-auto-configuration)",
                    "[Spring Boot API Documentation – @ConditionalOnClass](https://docs.spring.io/spring-boot/docs/current/api/org/springframework/boot/autoconfigure/condition/ConditionalOnClass.html)",
                    "[Baeldung – Spring Conditional Annotations Explained](https://www.baeldung.com/spring-conditional-annotations)"
                ],
                "feedbacks": [
                    "<p>This statement is correct. The <code>@ConditionalOnMissingBean(HelloService.class)</code> annotation ensures that the <code>helloService()</code> method defines a <code>HelloService</code> bean only if no other bean of the same type is already present in the <code>ApplicationContext</code>. This mechanism allows developers to provide a default bean that can be overridden by user-defined beans.</p>",
                    "<p>This statement is correct. The <code>@ConditionalOnClass(HelloService.class)</code> annotation specifies that the <code>HelloAutoConfig</code> auto-configuration class is applied only if the <code>HelloService</code> class is present on the classpath. This condition prevents the auto-configuration from being applied when the necessary class dependencies are absent.</p>",
                    "<p>This statement is incorrect. The <code>@ConditionalOnMissingBean</code> annotation ensures that the <code>helloService()</code> method defines a <code>HelloService</code> bean only if no other bean of the same type exists in the <code>ApplicationContext</code>. It does not replace existing beans; instead, it allows the creation of a bean only when one is not already present.</p>",
                    "<p>This statement is incorrect. The <code>@ConditionalOnClass(HelloService.class)</code> annotation specifies that the <code>HelloAutoConfig</code> class is applied only when the <code>HelloService</code> class is present on the classpath, not when it is absent.</p>",
                    "<p>This statement is incorrect. The presence of the <code>@ConditionalOnClass(HelloService.class)</code> annotation means that the <code>HelloAutoConfig</code> class, including its <code>helloService()</code> method, is processed only if the <code>HelloService</code> class is available on the classpath. If <code>HelloService.class</code> is absent, the auto-configuration does not apply, and the bean is not created.</p>"
                ],
                "explanation": "<p>Spring Boot's auto-configuration mechanism simplifies application setup by automatically configuring beans based on the application's classpath and defined beans. Conditional annotations play a crucial role in this process:</p><ul><li><p><code><strong>@ConditionalOnClass</strong></code><strong>:</strong> This annotation indicates that a configuration applies only when a specified class is present on the classpath. It helps in conditionally enabling configurations based on the availability of certain classes, ensuring that beans are created only when their dependencies are present. ​</p></li><li><p><code><strong>@ConditionalOnMissingBean</strong></code><strong>:</strong> This annotation allows the definition of a bean only if a bean of the specified type is not already present in the <code>ApplicationContext</code>. It enables developers to provide default bean definitions that can be overridden by user-defined beans, promoting flexibility and customization. ​<a href=\"https://stackoverflow.com/questions/50796810/what-does-the-spring-annotation-conditionalonmissingbean-do\">stackoverflow.com</a></p></li></ul><p>By combining these annotations, developers can create robust and flexible auto-configuration classes that adapt to the application's environment and existing configurations.</p>",
                "answers": [
                    "<p>A HelloService bean will be created from the helloService() method only when there is no other HelloService bean in the ApplicationContext</p>",
                    "<p>This auto-configuration class is used only when the HelloService.class is on the classpath</p>",
                    "<p>A HelloService bean will be created from the helloService() method and will replace existing a HelloService bean in the ApplicationContext</p>",
                    "<p>This auto-configuration class is used only when the HelloService.class is not on the classpath</p>",
                    "<p>A HelloService bean will be created from the helloService() method even if the HelloService.class is not in the classpath</p>"
                ]
            },
            "correct_response": [
                "a",
                "b"
            ],
            "section": "Spring Boot",
            "question_plain": "@Configuration\n@ConditionalOnClass(HelloService.class)\npublic class HelloAutoConfig {\n\n    @ConditionalOnMissingBean(HelloService.class)\n    @Bean\n    HelloService helloService() {\n        return new TypicalHelloService();\n    }\n}Which two statements are correct regarding the HelloAutoConfig auto-configuration class when it is specified in the META-INF.spring.factories file?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571849,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two are required to use transactions on Spring?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework Documentation – Transaction Management](https://docs.spring.io/spring-framework/reference/data-access/transaction.html)",
                    "[Spring @Transactional Annotation – Baeldung](https://www.baeldung.com/transaction-configuration-with-jpa-and-spring)",
                    "[Spring Boot Transactions and Rollbacks – Baeldung](https://www.baeldung.com/spring-transactional-propagation-isolation)"
                ],
                "feedbacks": [
                    "<p>This is incorrect. Spring provides built-in declarative transaction management using <code>@Transactional</code>, and developers do not need to implement <code>TransactionInterceptor</code> manually. Spring's AOP-based transaction management automatically applies transaction handling without requiring direct implementation of transaction interceptors.</p>",
                    "<p>This is correct. The <code>@Transactional</code> annotation is the primary way to enable transaction management in Spring. It can be applied at the class, method, or interface level, ensuring that method calls are executed within a transactional context.</p><p><strong>Example:</strong></p><pre class=\"prettyprint linenums\">@Service\npublic class AccountService {\n    @Transactional\n    public void transferFunds(Account from, Account to, BigDecimal amount) {\n        from.debit(amount);\n        to.credit(amount);\n    }\n}\n</pre><p>Here, the <code>@Transactional</code> annotation ensures that the <code>transferFunds()</code> method runs within a transaction.</p>",
                    "<p>This is correct. The <code>@EnableTransactionManagement</code> annotation enables Spring's annotation-driven transaction management, allowing <code>@Transactional</code> to be processed correctly.</p><p><strong>Example:</strong></p><pre class=\"prettyprint linenums\">@Configuration\n@EnableTransactionManagement\npublic class AppConfig {\n    @Bean\n    public PlatformTransactionManager transactionManager(EntityManagerFactory emf) {\n        return new JpaTransactionManager(emf);\n    }\n}\n</pre><p>Without this, Spring may not recognize the <code>@Transactional</code> annotations, leading to transactions not being properly managed.</p>",
                    "<p>This is incorrect. While Spring's transaction management <strong>internally</strong> uses AOP to wrap transactional methods, developers <strong>do not need</strong> to manually write AOP advice for transactions. The <code>@Transactional</code> annotation and <code>@EnableTransactionManagement</code> handle it automatically.</p>",
                    "<p>This is incorrect.</p><ul><li><p>First, <code>@Transaction</code> does not exist; the correct annotation is <code>@Transactional</code>.</p></li><li><p>Second, a class does <strong>not</strong> need <code>@Service</code> to be transactional—<code>@Transactional</code> can be applied to any Spring-managed bean, such as <code>@Repository</code>, <code>@Component</code>, or <code>@Controller</code>.</p></li></ul>"
                ],
                "explanation": "<p>Spring provides declarative and programmatic transaction management, allowing applications to handle database transactions efficiently.</p><p><strong>Declarative Transaction Management (Recommended)</strong></p><ul><li><p>Uses <code>@Transactional</code> to define transactional boundaries.</p></li><li><p>Requires <code>@EnableTransactionManagement</code> to be enabled in the Spring configuration.</p></li><li><p>Supports multiple transaction managers (<code>JpaTransactionManager</code>, <code>DataSourceTransactionManager</code>).</p></li></ul><p><strong>Example:</strong></p><pre class=\"prettyprint linenums\">@Transactional\npublic void updateAccountBalance(Account account, BigDecimal newBalance) {\n    account.setBalance(newBalance);\n}\n</pre><p><strong>Programmatic Transaction Management (Less Common)</strong></p><ul><li><p>Uses <code>TransactionTemplate</code> or <code>PlatformTransactionManager</code> to manage transactions manually.</p></li></ul><p><strong>Example using </strong><code><strong>TransactionTemplate</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">@Service\npublic class AccountService {\n    private final TransactionTemplate transactionTemplate;\n\n    public AccountService(PlatformTransactionManager transactionManager) {\n        this.transactionTemplate = new TransactionTemplate(transactionManager);\n    }\n\n    public void transferFunds(Account from, Account to, BigDecimal amount) {\n        transactionTemplate.execute(status -&gt; {\n            from.debit(amount);\n            to.credit(amount);\n            return null;\n        });\n    }\n}\n</pre><p><br></p>",
                "answers": [
                    "<p>A class returning a transaction must be implemented TransactionInterceptor interface</p>",
                    "<p>Annotate a class, an interface, or individual methods requiring a transaction with the @Transactional annotation</p>",
                    "<p>Add @EnableTransactionManagement to a Java configuration class</p>",
                    "<p>Write a Spring AOP advice to implement transactional behaviour</p>",
                    "<p>A class must be annotated with @Service and @Transaction</p>"
                ]
            },
            "correct_response": [
                "b",
                "c"
            ],
            "section": "Spring Data JPA",
            "question_plain": "Which two are required to use transactions on Spring?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571851,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are true regarding storing user details in Spring Security?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Security Reference – UserDetailsService](https://docs.spring.io/spring-security/reference/servlet/authentication/passwords/user-details-service.html)",
                    "[Default Password Encoder in Spring Security – Baeldung](https://www.baeldung.com/spring-security-5-default-password-encoder)"
                ],
                "feedbacks": [
                    "<p>Implementing the <code>UserDetailsService</code> interface allows developers to retrieve user information from custom storage mechanisms, such as databases or external services. This customization is essential for integrating Spring Security with existing user data sources.</p>",
                    "<p>User details in Spring Security include not only the username and password but also authorities (roles or permissions). The <code>UserDetails</code> interface provides methods like <code>getAuthorities()</code>, <code>getPassword()</code>, and <code>getUsername()</code> to access this information.</p>",
                    "<p>When a custom <code>UserDetailsService</code> is defined in the application context, Spring Boot does not create a default user. The presence of a custom <code>UserDetailsService</code> bean overrides the default user configuration. ​</p>",
                    "<p>Passwords must indeed be hashed for security; however, the default hashing algorithm in Spring Security is <strong>BCrypt</strong>, not MD5. BCrypt is preferred due to its adaptive strength and resistance to brute-force attacks.</p>",
                    "<p>Spring Security supports storing user details in various locations, including in-memory storage, relational databases (via JDBC), and LDAP directories. This flexibility allows integration with different authentication data sources.</p>"
                ],
                "explanation": "<p>Spring Security provides a flexible framework for managing user authentication and authorization. By implementing the <code>UserDetailsService</code> interface, developers can customize how user information is retrieved, enabling integration with various data sources such as databases, LDAP servers, or in-memory stores. Ensuring that passwords are securely hashed—preferably using algorithms like BCrypt—is crucial for protecting user credentials. Additionally, user details encompass not just usernames and passwords but also authorities, which define the roles and permissions assigned to each user.</p>",
                "answers": [
                    "<p>User details can be stored in custom storage and retrieve them by implementing UserDetailsService interface</p>",
                    "<p>The user details includes username and password but not authorities</p>",
                    "<p>With a custom UserDetailsService defined in the ApplicationContext, Spring Boot still creates the default user</p>",
                    "<p>Passwords must be hashed and the default hashing algorithm is MD5</p>",
                    "<p>User details can be stored in a database, in LDAP, or in memory</p>"
                ]
            },
            "correct_response": [
                "a",
                "e"
            ],
            "section": "Spring Security",
            "question_plain": "Which two statements are true regarding storing user details in Spring Security?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571853,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>What is a Spring Boot starter dependency?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Documentation – Starters Overview](https://docs.spring.io/spring-boot/docs/current/reference/html/using.html#using.build-systems.starters)",
                    "[Baeldung – Introduction to Spring Boot Starters](https://www.baeldung.com/spring-boot-starters)",
                    "[How to Create a Custom Spring Boot Starter – Baeldung](https://www.baeldung.com/spring-boot-custom-starter)"
                ],
                "feedbacks": [
                    "<p>This is incorrect because Spring Boot <strong>starters</strong> are predefined dependencies, not a POM file that must be built. They allow developers to easily manage transitive dependencies and configurations.</p>",
                    "<p>This is incorrect. While <strong>Spring Initializr</strong> allows users to generate a project with predefined dependencies, a <strong>starter dependency</strong> is just a dependency aggregator and not a full project template.</p>",
                    "<p>This is incorrect because Spring Boot starters <strong>do not generate code</strong>. They only provide dependencies and configurations needed for common use cases like web development or database access.</p>",
                    "<p>This is correct. Spring Boot starters <strong>group dependencies</strong> into logical sets (e.g., <code>spring-boot-starter-web</code>, <code>spring-boot-starter-data-jpa</code>) to reduce manual dependency management and ensure compatibility.</p>"
                ],
                "explanation": "<p>Spring Boot starters simplify dependency management in Java applications. They <strong>aggregate multiple dependencies</strong> into a single POM or Gradle entry, ensuring compatibility and reducing boilerplate configuration.</p><p>Spring Boot provides multiple starters for different functionalities. For example, <code>spring-boot-starter-web</code> includes dependencies for building web applications, such as Tomcat, Spring MVC, and Jackson. Similarly, <code>spring-boot-starter-data-jpa</code> includes dependencies for working with JPA and Hibernate.</p><p>Using Spring Boot starters eliminates the need to manually specify each dependency and ensures that the included libraries are tested together for compatibility.</p><p><strong>How Spring Boot Starters Work</strong></p><p>A <strong>starter dependency</strong> is a predefined set of dependencies that automatically pulls in necessary libraries. Instead of manually adding multiple dependencies, a single starter can include everything needed for a specific use case.</p><p>For example, instead of adding dependencies for <strong>Spring MVC, Tomcat, and Jackson</strong> separately, you can include:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;\n&lt;/dependency&gt;\n</pre><p>Spring Boot then automatically resolves and downloads all required libraries.</p><p>Similarly, in <strong>Gradle</strong>, you can use:</p><pre class=\"prettyprint linenums\">dependencies {\n    implementation 'org.springframework.boot:spring-boot-starter-web'\n}\n</pre><p><strong>Common Spring Boot Starters</strong></p><ul><li><p><code>spring-boot-starter-web</code>: Includes dependencies for building web applications using Spring MVC.</p></li><li><p><code>spring-boot-starter-data-jpa</code>: Provides everything needed to use JPA with Hibernate.</p></li><li><p><code>spring-boot-starter-security</code>: Configures Spring Security for authentication and authorization.</p></li><li><p><code>spring-boot-starter-test</code>: Provides testing frameworks such as JUnit, Mockito, and Spring Test.</p></li><li><p><code>spring-boot-starter-actuator</code>: Adds health checks, metrics, and monitoring tools.</p></li></ul><p><strong>Why Use Starters?</strong></p><ol><li><p><strong>Simplifies Dependency Management</strong> – Avoids manually adding multiple related dependencies.</p></li><li><p><strong>Ensures Compatibility</strong> – Provides a curated set of dependencies that work well together.</p></li><li><p><strong>Reduces Boilerplate Code</strong> – Removes unnecessary XML configurations.</p></li><li><p><strong>Standardizes Development</strong> – Offers a common way to set up different modules.</p></li></ol><p><strong>How to Create a Custom Spring Boot Starter?</strong></p><p>If a company or development team frequently uses a set of dependencies, they can create a <strong>custom Spring Boot Starter</strong> to standardize configuration across projects.</p><p>Steps to create a custom starter:</p><ol><li><p><strong>Create a new Maven/Gradle project</strong> and define required dependencies.</p></li><li><p><strong>Provide Auto-Configuration</strong> using <code>@Configuration</code> and <code>@ConditionalOnClass</code>.</p></li><li><p><strong>Package and Publish</strong> the starter to a private or public repository.</p></li></ol><p>Example of an <strong>auto-configured bean</strong> in a custom starter:</p><pre class=\"prettyprint linenums\">@Configuration\n@ConditionalOnClass(MyService.class)\npublic class MyAutoConfiguration {\n    @Bean\n    public MyService myService() {\n        return new MyService();\n    }\n}\n</pre><p>This approach ensures that <code>MyService</code> is only created when it is available on the classpath.</p>",
                "answers": [
                    "<p>A specific POM which you must build to control Spring Boot's opinionated runtime</p>",
                    "<p>A pre-existing model to project you can download and use as the basis of your project</p>",
                    "<p>A setting for specifying which code toy want SpringBoot to generate for you</p>",
                    "<p>An easy way to include multiple, coordinated dependencies related to a specific technology, like web or JDBC</p>"
                ]
            },
            "correct_response": [
                "d"
            ],
            "section": "Spring Boot",
            "question_plain": "What is a Spring Boot starter dependency?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571855,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two options will inject the value of the daily.limit system property?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Documentation – Externalized Configuration](https://docs.spring.io/spring-boot/reference/features/external-config.html#features.external-config)",
                    "[Spring Boot Property Sources – Baeldung](https://www.baeldung.com/properties-with-spring)"
                ],
                "feedbacks": [
                    "<p>This syntax correctly injects the system property or environment variable named <code>daily.limit</code> into a Spring bean.<br>Spring resolves placeholders inside <code>\"${}\"</code> from:</p><ul><li><p>System properties (<code>System.getProperties()</code>)</p></li><li><p>Environment variables</p></li><li><p><code>application.properties</code> or <code>application.yml</code></p></li><li><p><code>@PropertySource</code>-loaded properties<br>Example usage:</p></li></ul><pre class=\"prettyprint linenums\">@Value(\"${daily.limit}\")\nprivate int dailyLimit;</pre>",
                    "<p>This uses <strong>Spring Expression Language (SpEL)</strong> to inject system properties dynamically.</p><ul><li><p><code>systemProperties</code> is a built-in Spring context variable referring to Java system properties.</p></li><li><p>The SpEL syntax allows dynamic evaluations.<br>Example usage:</p></li></ul><pre class=\"prettyprint linenums\">@Value(\"#{systemProperties['daily.limit']}\")\nprivate int dailyLimit;</pre>",
                    "<p>This syntax is <strong>incorrect</strong>.</p><ul><li><p>The <code>$()</code> syntax does not exist in Spring property resolution.</p></li><li><p>Spring only recognizes <code>${}</code> for property placeholders and <code>#{}</code> for SpEL expressions.</p></li></ul>",
                    "<p>This syntax is <strong>incorrect</strong>.</p><ul><li><p>The <code>#()</code> syntax is <strong>not</strong> valid in Spring Expression Language (SpEL).</p></li><li><p>SpEL expressions should use <code>#{}</code>.</p></li></ul>",
                    "<p>This syntax is <strong>incorrect</strong>.</p><ul><li><p><code>#()</code> is <strong>not a valid SpEL syntax</strong>.</p></li><li><p>The correct SpEL syntax is <code>@Value(\"#{systemProperties['daily.limit']}\")</code>.</p></li></ul>"
                ],
                "explanation": "<p>Spring allows injecting system properties and environment variables into beans using the <code>@Value</code> annotation. There are two main approaches:</p><p><strong>1. Using </strong><code><strong>${}</strong></code><strong> for Property Placeholders</strong></p><p>The placeholder notation <code>${}</code> is used to inject values from system properties, environment variables, or property files.</p><pre class=\"prettyprint linenums\">java\nCopyEdit@Value(\"${daily.limit}\")\nprivate int dailyLimit;\n</pre><p>This searches for <code>daily.limit</code> in:</p><ul><li><p><code>application.properties</code> or <code>application.yml</code></p></li><li><p>System properties (<code>System.getProperty()</code>)</p></li><li><p>Environment variables (<code>System.getenv()</code>)</p></li></ul><p>Example <strong>application.properties</strong> entry:</p><pre class=\"prettyprint linenums\">ini\nCopyEditdaily.limit=100\n</pre><p>If the property is not found, Spring can throw an error unless a default value is provided:</p><pre class=\"prettyprint linenums\">java\nCopyEdit@Value(\"${daily.limit:50}\")\nprivate int dailyLimit; // Defaults to 50 if the property is missing\n</pre><p><strong>2. Using </strong><code><strong>#{}</strong></code><strong> for Spring Expression Language (SpEL)</strong></p><p>Spring Expression Language (SpEL) is used for dynamic property resolution.</p><pre class=\"prettyprint linenums\">java\nCopyEdit@Value(\"#{systemProperties['daily.limit']}\")\nprivate int dailyLimit;\n</pre><p>This fetches the system property <strong>at runtime</strong>, making it more dynamic than <code>${}</code>.</p><p>To set a system property, use:</p><pre class=\"prettyprint linenums\">java\nCopyEditSystem.setProperty(\"daily.limit\", \"200\");\n</pre><p>Spring can also retrieve environment variables using SpEL:</p><pre class=\"prettyprint linenums\">java\nCopyEdit@Value(\"#{environment['HOME']}\")\nprivate String homeDir;\n</pre><p><strong>3. Using </strong><code><strong>Environment</strong></code><strong> API for Advanced Property Management</strong></p><p>Spring provides the <code>Environment</code> abstraction to retrieve properties programmatically:</p><pre class=\"prettyprint linenums\">java\nCopyEdit@Autowired\nprivate Environment env;\n\npublic void printDailyLimit() {\n    String dailyLimit = env.getProperty(\"daily.limit\", \"50\");\n    System.out.println(\"Daily Limit: \" + dailyLimit);\n}\n</pre><p>This method is useful when dealing with multiple property sources.</p>",
                "answers": [
                    "<p>@Value(\"${daily.limit}\")\"</p>",
                    "<p>@Value(\"#{systemProperties['daily.limit']}\")</p>",
                    "<p>@Value(\"$(systemProperties.daily.limit)\")</p>",
                    "<p>@Value(\"#(daily.limit)\")</p>",
                    "<p>@Value(\"#(systemProperties.daily.limit)\")</p>"
                ]
            },
            "correct_response": [
                "a",
                "b"
            ],
            "section": "Spring Core",
            "question_plain": "Which two options will inject the value of the daily.limit system property?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571857,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which three statements are correct regarding the Actuator info endpoint?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Actuator Documentation](https://docs.spring.io/spring-boot/docs/current/reference/html/actuator.html)",
                    "[Spring Boot Actuator /Info Endpoint Example - HowToDoInJava](https://howtodoinjava.com/spring-boot/info-endpoint-custom-info/)"
                ],
                "feedbacks": [
                    "<p>The Actuator <code>info</code> endpoint is commonly used to display build information, such as version, description, and other metadata. This data can be populated from sources like the <code>build-info.properties</code> file generated during the build process.</p>",
                    "<p>While Spring Boot Actuator endpoints can be secured to restrict access to authenticated users, this behavior is not specific to the <code>info</code> endpoint alone. It applies to all endpoints, and securing them requires additional configuration.</p>",
                    "<p>The <code>info</code> endpoint is designed to display arbitrary application information. Developers can customize it to expose various details about the application by adding properties prefixed with <code>info.</code> in the <code>application.properties</code> file or by implementing custom <code>InfoContributor</code> beans.</p>",
                    "<p>The <code>info</code> endpoint is read-only and does not provide functionality to modify application properties or configurations at runtime. Its purpose is solely to expose information about the application.</p>",
                    "<p>The <code>/actuator/info</code> endpoint is enabled by default in Spring Boot 2.x, but starting from version 2.5.0 (released in May 2021), it is no longer exposed over HTTP by default and requires explicit configuration using <code>management.endpoints.web.exposure.include=info</code>. Prior to version 2.5.0, both <code>/health</code> and <code>/info</code> were exposed by default without additional configuration.</p>"
                ],
                "explanation": "<p>Spring Boot Actuator provides a set of production-ready features to help monitor and manage applications. One of these features is the <code>info</code> endpoint, which serves as a centralized place to expose various pieces of information about the application.​</p><p><strong>Purpose of the </strong><code><strong>info</strong></code><strong> Endpoint</strong></p><p>The primary purpose of the <code>info</code> endpoint is to provide arbitrary information about the application. This can include:​</p><ul><li><p><strong>Build Information:</strong> Details such as the application version, build time, and other metadata.​</p></li><li><p><strong>Custom Application Data:</strong> Any other information that might be relevant to understand the state or identity of the application.​</p></li></ul><p>This information is particularly useful for monitoring purposes, allowing teams to quickly ascertain details about the deployed application without accessing the codebase or deployment artifacts.​</p><p><strong>Enabling and Configuring the </strong><code><strong>info</strong></code><strong> Endpoint</strong></p><p>To use the <code>info</code> endpoint, you need to include the Spring Boot Actuator dependency in your project. Here's how you can add it:</p><ul><li><p><strong>Maven:</strong></p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-starter-actuator&lt;/artifactId&gt;\n&lt;/dependency&gt;\n</pre></li></ul><p><br></p><ul><li><p><strong>Gradle:</strong></p><pre class=\"prettyprint linenums\">implementation 'org.springframework.boot:spring-boot-starter-actuator'\n</pre></li></ul><p><br></p><p>By default, starting from Spring Boot 2.5.0, the <code>info</code> endpoint is not exposed over HTTP. To expose it, you need to configure your <code>application.properties</code> or <code>application.yml</code> file:​</p><ul><li><p><strong>application.properties:</strong></p><pre class=\"prettyprint linenums\">management.endpoints.web.exposure.include=info</pre></li><li><p>After configuring this, the <code>info</code> endpoint will be accessible at <code>/actuator/info</code>.​</p></li></ul><p><strong>Adding Information to the </strong><code><strong>info</strong></code><strong> Endpoint</strong></p><p>There are several ways to add information to the <code>info</code> endpoint:​</p><ol><li><p><strong>Using </strong><code><strong>application.properties</strong></code><strong> or </strong><code><strong>application.yml</strong></code><strong>:</strong></p><p>You can add static information by prefixing properties with <code>info.</code>. For example:</p><ul><li><p><strong>application.properties:</strong></p><pre class=\"prettyprint linenums\">info.app.name=Sample Application\ninfo.app.version=1.0.0\ninfo.app.description=This is a sample Spring Boot application.\n</pre></li><li><p><strong>application.yml:</strong></p><pre class=\"prettyprint linenums\">info:\n  app:\n    name: Sample Application\n    version: 1.0.0\n    description: This is a sample Spring Boot application.\n</pre></li></ul><p>These properties will be exposed in the <code>info</code> endpoint as JSON:</p><pre class=\"prettyprint linenums\">{\n  \"app\": {\n    \"name\": \"Sample Application\",\n    \"version\": \"1.0.0\",\n    \"description\": \"This is a sample Spring Boot application.\"\n  }\n}\n</pre></li></ol><p><br></p><ol><li><p><strong>Using Build Plugins:</strong></p><p>You can automatically include build information using build plugins:</p><ul><li><p><strong>Maven:</strong> By configuring the Spring Boot Maven plugin, you can generate a <code>build-info.properties</code> file that Actuator uses to populate the <code>info</code> endpoint.​</p><pre class=\"prettyprint linenums\">&lt;plugin&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-maven-plugin&lt;/artifactId&gt;\n    &lt;configuration&gt;\n        &lt;addBuildInfo&gt;true&lt;/addBuildInfo&gt;\n    &lt;/configuration&gt;\n&lt;/plugin&gt;\n</pre></li><li><p><strong>Gradle:</strong> Similarly, in Gradle, you can use the <code>bootBuildInfo</code> task:​</p><pre class=\"prettyprint linenums\">bootBuildInfo {\n    properties {\n        // additional properties\n    }\n}\n</pre></li></ul><p>This setup will include build details like version, artifact, and name in the <code>info</code> endpoint.</p></li><li><p><strong>Implementing </strong><code><strong>InfoContributor</strong></code><strong>:</strong></p><p>For dynamic or more complex information, you can implement the <code>InfoContributor</code> interface:</p><pre class=\"prettyprint linenums\">@Component\npublic class CustomInfoContributor implements</pre></li></ol>",
                "answers": [
                    "<p>Typically it is used to display build or source control information</p>",
                    "<p>It provides configuration options through which only an authenticated user can display application information</p>",
                    "<p>It can be used to display arbitrary application information</p>",
                    "<p>It can be used to change a property value on a running application</p>",
                    "<p>It is not enabled by default</p>"
                ]
            },
            "correct_response": [
                "a",
                "c",
                "e"
            ],
            "section": "Spring Boot Actuator",
            "question_plain": "Which three statements are correct regarding the Actuator info endpoint?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571859,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>Which statement is true?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework Reference - @ActiveProfiles](https://docs.spring.io/spring-framework/reference/testing/annotations/integration-spring/annotation-activeprofiles.html)",
                    "[Spring Framework Javadoc - @ActiveProfiles](https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/test/context/ActiveProfiles.html)"
                ],
                "feedbacks": [
                    "<p>This statement inaccurately describes the functionality of <code>@ActiveProfiles</code>. The annotation is not used to configure the location of properties files or inlined properties; instead, it specifies which bean definition profiles should be active during the loading of an <code>ApplicationContext</code> for integration tests.</p>",
                    "<p>This statement accurately reflects the purpose of the <code>@ActiveProfiles</code> annotation. It is applied at the class level to declare which bean definition profiles should be active when loading an <code>ApplicationContext</code> for integration tests.</p>",
                    "<p>This statement confuses the purpose of <code>@ActiveProfiles</code> with functionality related to recording application events. The <code>@ActiveProfiles</code> annotation does not instruct the Spring TestContext Framework to record application events during test execution; its sole purpose is to specify active bean definition profiles.</p>",
                    "<p>This statement misrepresents the functionality of <code>@ActiveProfiles</code>. The annotation is not used to configure how the Spring TestContext Framework is bootstrapped; rather, it is used to declare which bean definition profiles should be active during the loading of an <code>ApplicationContext</code> for integration tests.​</p>"
                ],
                "explanation": "<p>In Spring, the <code>@ActiveProfiles</code> annotation is utilized to specify which bean definition profiles should be active when loading an <code>ApplicationContext</code> for integration tests. This is particularly useful in scenarios where different configurations are required for different environments (e.g., development, testing, production).​</p><p><strong>Key Features of </strong><code><strong>@ActiveProfiles</strong></code><strong>:</strong></p><ul><li><p><strong>Class-Level Annotation:</strong> <code>@ActiveProfiles</code> is applied at the class level on test classes to control the active profiles during testing.​<a href=\"https://docs.spring.io/spring-framework/reference/testing/annotations/integration-spring/annotation-activeprofiles.html\">docs.spring.io+1docs.spring.io+1</a></p></li><li><p><strong>Specifying Profiles:</strong> The <code>profiles</code> attribute of the annotation is used to define one or more profiles that should be active. For example:​</p><pre class=\"prettyprint linenums\">java\nKopiujEdytuj@ActiveProfiles(profiles = {\"dev\", \"integration\"})\npublic class MyIntegrationTests {\n    // test methods\n}\n</pre></li></ul><p><br></p><ul><li><p><strong>Inheritance:</strong> By default, <code>@ActiveProfiles</code> supports inheritance, meaning that a test class will inherit active bean definition profiles defined by a superclass or enclosing class. This behavior can be controlled using the <code>inheritProfiles</code> attribute.​<a href=\"https://docs.spring.io/spring-framework/docs/current/javadoc-api/org/springframework/test/context/ActiveProfiles.html\">docs.spring.io+1docs.spring.io+1</a></p></li><li><p><strong>Custom Profile Resolution:</strong> Developers can implement the <code>ActiveProfilesResolver</code> interface to programmatically determine which profiles should be active. This custom resolver can be specified using the <code>resolver</code> attribute of the <code>@ActiveProfiles</code> annotation.​<a href=\"https://docs.spring.io/spring-framework/reference/testing/annotations/integration-spring/annotation-activeprofiles.html\">docs.spring.io</a></p></li></ul><p><strong>Usage Example:</strong></p><p>Consider a scenario where you have a test class that should run with the \"test\" profile active:​<a href=\"https://reflectoring.io/dont-use-spring-profile-annotation/\">reflectoring.io+1javatechonline.com+1</a></p><pre class=\"prettyprint linenums\">java\nKopiujEdytuj@RunWith(SpringRunner.class)\n@SpringBootTest\n@ActiveProfiles(\"test\")\npublic class ApplicationTests {\n    // test methods\n}\n</pre><p><br></p><p>In this example, the <code>@ActiveProfiles(\"test\")</code> annotation ensures that the \"test\" profile is active when the <code>ApplicationContext</code> is loaded for the test, allowing beans and configurations specific to the \"test\" profile to be utilized.​</p><p><br></p>",
                "answers": [
                    "<p>@ActiveProfiles is a class level annotation that you can use to configure the location of properties files and inlined properties to added to the set of PropertySources in the Environment for an ApplicationContext loaded for an integration test</p>",
                    "<p>@ActiveProfiles is a class level annotation that is used to declare which bean definition profiles should be active when loading an ApplicationContext for an integration test</p>",
                    "<p>@ActiveProfiles is a class level annotation that is used to instruct the Spring TestContext Framework to record all application events that are published in the ApplicationContainer during the execution of a single test</p>",
                    "<p>@ActiveProfiles is a class level annotation that you can use to configure how the Spring TestContext Framework is bootstrapped</p>"
                ]
            },
            "correct_response": [
                "b"
            ],
            "section": "Testing",
            "question_plain": "Which statement is true?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571861,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements about the @Autowired annotation are true?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework Reference - Using @Autowired](https://docs.spring.io/spring-framework/reference/core/beans/annotation-config/autowired.html)",
                    "[Guide to Spring @Autowired](https://www.baeldung.com/spring-autowire)"
                ],
                "feedbacks": [
                    "<p>Correct, field injection happens after the bean is instantiated and configuration methods have run, because dependency wiring occurs during bean initialization.</p>",
                    "<p>The <code>@Autowired</code> annotation cannot be applied at the class level. It is used on constructors, methods, and fields to indicate that dependency injection should be performed.</p>",
                    "<p>By default, if a dependency cannot be satisfied with <code>@Autowired</code>, Spring throws a <code>NoSuchBeanDefinitionException</code>, which is a type of <code>BeansException</code>, not a general <code>RuntimeException</code>.</p>",
                    "<p><code>@Autowired</code> cannot be used to inject references into <code>BeanPostProcessor</code> and <code>BeanFactoryPostProcessor</code> instances because these interfaces are instantiated before the container processes <code>@Autowired</code> annotations. Therefore, their dependencies must be injected manually.</p>",
                    "<p><code>@Autowired</code> can be used on methods with multiple parameters, and Spring will attempt to resolve each parameter's dependency. This allows for the injection of multiple dependencies into a single method.</p>"
                ],
                "explanation": "<p>The <code>@Autowired</code> annotation in Spring facilitates automatic dependency injection, allowing Spring to resolve and inject collaborating beans into your bean. ​<a href=\"https://www.baeldung.com/spring-autowire\">baeldung.com</a></p><p><strong>Key Features of </strong><code><strong>@Autowired</strong></code><strong>:</strong></p><ul><li><p><strong>Injection Points:</strong> <code>@Autowired</code> can be applied to constructors, methods, and fields. When applied:​</p><ul><li><p><strong>Constructor:</strong> Injects dependencies through the constructor.​<a href=\"https://www.reddit.com/r/learnjava/comments/qyn5gw/spring_boot_autowired_use_cases/\">reddit.com</a></p></li><li><p><strong>Method:</strong> Injects dependencies through a method, which can have multiple parameters.​</p></li><li><p><strong>Field:</strong> Injects dependencies directly into fields.​</p></li></ul></li><li><p><strong>Required Attribute:</strong> By default, <code>@Autowired</code> expects the dependency to be present in the Spring context. If not found, it throws a <code>NoSuchBeanDefinitionException</code>. However, by setting <code>required=false</code>, you can indicate that the dependency is optional.​</p></li></ul><p><strong>Usage Examples:</strong></p><ul><li><p><strong>Constructor Injection:</strong></p><pre class=\"prettyprint linenums\">@Component\npublic class MyService {\n    private final MyRepository repository;\n\n    @Autowired\n    public MyService(MyRepository repository) {\n        this.repository = repository;\n    }\n}\n</pre></li></ul><p><br></p><ul><li><p><strong>Setter Injection:</strong></p><pre class=\"prettyprint linenums\">@Component\npublic class MyService {\n    private MyRepository repository;\n\n    @Autowired\n    public void setRepository(MyRepository repository) {\n        this.repository = repository;\n    }\n}\n</pre></li></ul><p><br></p><ul><li><p><strong>Field Injection:</strong></p><pre class=\"prettyprint linenums\">@Component\npublic class MyService {\n    @Autowired\n    private MyRepository repository;\n}</pre></li></ul><p><br></p><p><strong>Considerations:</strong></p><ul><li><p><strong>Optional Dependencies:</strong> If a dependency is not mandatory, set <code>required=false</code> to avoid exceptions when the bean is not present.​</p></li><li><p><strong>Multiple Candidates:</strong> When multiple beans of the same type exist, use the <code>@Qualifier</code> annotation to specify which one to inject.​</p></li><li><p><strong>Lifecycle:</strong> Be cautious when using <code>@Autowired</code> in <code>BeanPostProcessor</code> or <code>BeanFactoryPostProcessor</code> implementations, as these are instantiated before the container processes <code>@Autowired</code> annotations.​</p></li></ul><p><br></p>",
                "answers": [
                    "<p>@Autowired fields are injected after any config methods are invoked</p>",
                    "<p>If @Autowired is used on a class, field injection is automatically performed for all dependencies</p>",
                    "<p>By default, if a dependency cannot by satisfied with @Autowired, Spring throws a RuntimeException</p>",
                    "<p>@Autowired can be used to inject references into BeanPostProcessor and BeanFactoryPostProcessor</p>",
                    "<p>Multiple arguments can be injected into a single method using @Autowired</p>"
                ]
            },
            "correct_response": [
                "e",
                "a"
            ],
            "section": "Spring Core",
            "question_plain": "Which two statements about the @Autowired annotation are true?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571863,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are correct regarding the Health Indicator status?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Actuator Documentation](https://docs.spring.io/spring-boot/reference/actuator/index.html)",
                    "[Baeldung - Guide to Spring Boot Actuator Health Indicators](https://www.baeldung.com/spring-boot-health-indicators)"
                ],
                "feedbacks": [
                    "<p>Spring Boot allows developers to define <strong>custom health statuses</strong> beyond the built-in ones. This flexibility enables representation of additional system states specific to application requirements. To introduce a custom status, you can use the <code>Health.status(String statusCode)</code> method. For example:</p><pre class=\"prettyprint linenums\">Health customHealthStatus = Health.status(\"WARNING\").build();\n</pre><p><br></p><p>To ensure proper integration of custom statuses, you may need to configure the severity order in your application's properties:</p><pre class=\"prettyprint linenums\">management.endpoint.health.status.order=FATAL, DOWN, OUT_OF_SERVICE, UNKNOWN, UP\n</pre><p><br></p><p>This configuration informs Spring Boot of the relative severity of each status, ensuring accurate health reporting.</p>",
                    "<p>This is incorrect. Spring Boot uses the status with the <strong>highest severity</strong> as the top-level status when aggregating multiple health indicators. For example, if one indicator reports <code>DOWN</code> and another reports <code>UP</code>, the overall system health will be <code>DOWN</code> due to its higher severity.</p>",
                    "<p>This is incorrect. Developers can customize the severity order of health statuses by configuring the <code>management.endpoint.health.status.order</code> property in the application settings. This flexibility allows tailoring health reporting to specific application needs.</p>",
                    "<p>By default, Spring Boot defines the following health statuses, ordered from most severe to least severe:​</p><ol><li><p><code>DOWN</code>​</p></li><li><p><code>OUT_OF_SERVICE</code>​</p></li><li><p><code>UNKNOWN</code>​</p></li><li><p><code>UP</code>​</p></li></ol><p>This default severity order ensures that the most critical statuses are prioritized when aggregating health indicators.</p>",
                    "<p>This is incorrect. Spring Boot determines the final system health based on the <strong>most severe</strong> status reported by any health indicator, not the position of a status in a sorted list.</p>"
                ],
                "explanation": "<p><strong>1. Introduction to Health Indicators</strong></p><p>Spring Boot provides a built-in <strong>Actuator</strong> module that enables monitoring and management of applications in production. One of its most powerful features is <strong>Health Indicators</strong>, which provide an overview of the application's health status.</p><p>Health Indicators are particularly useful in <strong>cloud-native environments</strong> and <strong>microservices</strong>, where service health status is used by orchestration tools like Kubernetes or AWS Load Balancers to decide whether an instance is operational.</p><p>Spring Boot's <code><strong>/actuator/health</strong></code> endpoint exposes this health information.</p><p><strong>2. Built-in Health Statuses</strong></p><p>Spring Boot defines the following <strong>default health statuses</strong> in decreasing order of severity:</p><ol><li><p><strong>DOWN</strong> – The component is unavailable or non-functional.</p></li><li><p><strong>OUT_OF_SERVICE</strong> – The component is temporarily out of service for maintenance.</p></li><li><p><strong>UNKNOWN</strong> – The health status is indeterminate.</p></li><li><p><strong>UP</strong> – The component is fully operational.</p></li></ol><p><strong>Example of a Default Health Check Response</strong></p><p>By default, if you access <code>http://localhost:8080/actuator/health</code>, you'll see:</p><pre class=\"prettyprint linenums\">json\nCopyEdit{\n  \"status\": \"UP\"\n}\n</pre><p>If one of the critical components fails (e.g., database is down), the status will change to:</p><pre class=\"prettyprint linenums\">json\nCopyEdit{\n  \"status\": \"DOWN\",\n  \"components\": {\n    \"db\": {\n      \"status\": \"DOWN\",\n      \"details\": {\n        \"error\": \"java.sql.SQLException: Connection refused\"\n      }\n    }\n  }\n}\n</pre><p><strong>3. Configuring Health Indicators</strong></p><p>Spring Boot <strong>automatically</strong> includes Health Indicators for various components, but you can configure which indicators should be included or excluded.</p><p><strong>Enable/Disable Specific Health Indicators</strong></p><p>In <code>application.properties</code> or <code>application.yml</code>, you can control which indicators should be active:</p><pre class=\"prettyprint linenums\">properties\nCopyEditmanagement.health.db.enabled=false  # Disables database health check\nmanagement.health.diskspace.enabled=true  # Enables disk space monitoring\n</pre><pre class=\"prettyprint linenums\">yaml\nCopyEditmanagement:\n  health:\n    db:\n      enabled: false\n    diskspace:\n      enabled: true\n</pre><p><strong>Configuring Severity Order</strong></p><p>You can <strong>customize the severity order</strong> of health statuses in <code>application.properties</code>:</p><pre class=\"prettyprint linenums\">properties\nCopyEditmanagement.endpoint.health.status.order=FATAL,DOWN,OUT_OF_SERVICE,UNKNOWN,UP\n</pre><p>This configuration allows you to introduce additional statuses like <code>FATAL</code>.</p><p><strong>4. Creating Custom Health Indicators</strong></p><p>Spring Boot allows defining <strong>custom health indicators</strong> by implementing the <code>HealthIndicator</code> interface.</p><p><strong>Example: Custom Health Indicator for an External API</strong></p><p>Suppose your application depends on an external API, and you want to check its availability:</p><pre class=\"prettyprint linenums\">java\nCopyEditimport org.springframework.boot.actuate.health.Health;\nimport org.springframework.boot.actuate.health.HealthIndicator;\nimport org.springframework.stereotype.Component;\nimport org.springframework.web.client.RestTemplate;\n\n@Component\npublic class ExternalApiHealthIndicator implements HealthIndicator {\n\n    private final RestTemplate restTemplate = new RestTemplate();\n\n    @Override\n    public Health health() {\n        try {\n            restTemplate.getForObject(\"https://api.example.com/health\", String.class);\n            return Health.up().withDetail(\"External API\", \"Available\").build();\n        } catch (Exception e) {\n            return Health.down().withDetail(\"External API\", \"Unavailable\").build();\n        }\n    }\n}\n</pre><p><strong>Explanation:</strong></p><ul><li><p>The <code>health()</code> method checks whether <code>https://api.example.com/health</code> is reachable.</p></li><li><p>If the API responds, it marks the service as <code>UP</code>.</p></li><li><p>If the request fails, it marks the service as <code>DOWN</code>.</p></li></ul><p><strong>Testing the Custom Health Indicator</strong></p><p>Once implemented, this custom indicator will be visible under <code>http://localhost:8080/actuator/health</code>:</p><pre class=\"prettyprint linenums\">json\nCopyEdit{\n  \"status\": \"DOWN\",\n  \"components\": {\n    \"externalApiHealthIndicator\": {\n      \"status\": \"DOWN\",\n      \"details\": {\n        \"External API\": \"Unavailable\"\n      }\n    }\n  }\n}\n</pre><p><strong>5. Exposing Health Endpoints</strong></p><p>By default, only <strong>basic health status</strong> (<code>UP</code> or <code>DOWN</code>) is returned when calling <code>/actuator/health</code>. To expose <strong>detailed health information</strong>, configure:</p><pre class=\"prettyprint linenums\">properties\nCopyEditmanagement.endpoint.health.show-details=always\nmanagement.endpoints.web.exposure.include=health\n</pre><p>This will enable detailed responses like:</p><pre class=\"prettyprint linenums\">json\nCopyEdit{\n  \"status\": \"UP\",\n  \"components\": {\n    \"diskSpace\": {\n      \"status\": \"UP\",\n      \"details\": {\n        \"total\": 500000000000,\n        \"free\": 300000000000,\n        \"threshold\": 10485760\n      }\n    },\n    \"db\": {\n      \"status\": \"UP\",\n      \"details\": {\n        \"database\": \"MySQL\",\n        \"result\": \"Healthy\"\n      }\n    }\n  }\n}\n</pre><p><strong>6. Securing Health Endpoints</strong></p><p>Since health information might expose sensitive details, restrict access to the <code>/actuator/health</code> endpoint.</p><p><strong>Securing Actuator Endpoints via Spring Security</strong></p><pre class=\"prettyprint linenums\">java\nCopyEdit@Configuration\npublic class SecurityConfig extends WebSecurityConfigurerAdapter {\n    @Override\n    protected void configure(HttpSecurity http) throws Exception {\n        http.authorizeRequests()\n            .antMatchers(\"/actuator/health\").permitAll()  // Allow public access to health\n            .antMatchers(\"/actuator/**\").authenticated()  // Restrict other endpoints\n            .and()\n            .httpBasic();\n    }\n}\n</pre><p>This configuration:</p><ul><li><p><strong>Allows public access</strong> to <code>/actuator/health</code></p></li><li><p><strong>Restricts other actuator endpoints</strong> to authenticated users.</p></li></ul><p><strong>7. Integrating with Kubernetes and Cloud Providers</strong></p><p>In cloud environments like <strong>Kubernetes</strong>, health endpoints are used for <strong>readiness</strong> and <strong>liveness probes</strong>.</p><p><strong>Kubernetes Readiness Probe Example</strong></p><p>A readiness probe ensures that traffic is only routed to a pod if it's ready to accept requests:</p><pre class=\"prettyprint linenums\">yaml\nCopyEditlivenessProbe:\n  httpGet:\n    path: /actuator/health/liveness\n    port: 8080\n  initialDelaySeconds: 5\n  periodSeconds: 10\n</pre><p><strong>Kubernetes Liveness Probe Example</strong></p><p>A liveness probe checks if the application is still running and restarts the pod if necessary:</p><pre class=\"prettyprint linenums\">yaml\nCopyEditlivenessProbe:\n  httpGet:\n    path: /actuator/health/readiness\n    port: 8080\n  initialDelaySeconds: 5\n  periodSeconds: 10\n</pre><p><strong>8. Best Practices for Health Indicators</strong></p><ol><li><p><strong>Expose only necessary information</strong></p><ul><li><p>Avoid leaking sensitive details in health responses.</p></li><li><p>Use <code>management.endpoint.health.show-details=when_authorized</code> to restrict details to authenticated users.</p></li></ul></li><li><p><strong>Use Custom Health Indicators for Critical Services</strong></p><ul><li><p>Monitor database connections, third-party APIs, and caches.</p></li><li><p>Implement failover mechanisms if a critical component goes <code>DOWN</code>.</p></li></ul></li><li><p><strong>Integrate Health Indicators with Monitoring Tools</strong></p><ul><li><p>Use <strong>Prometheus</strong> and <strong>Grafana</strong> to collect and visualize health metrics.</p></li><li><p>Configure alerts in tools like <strong>New Relic</strong>, <strong>Datadog</strong>, or <strong>AWS CloudWatch</strong>.</p></li></ul></li><li><p><strong>Use Health Indicators for Auto-Scaling</strong></p><ul><li><p>Configure cloud providers to automatically scale services based on health status.</p></li></ul></li></ol><p><strong>9. Summary</strong></p><ul><li><p>Spring Boot Actuator provides <strong>built-in</strong> and <strong>customizable</strong> Health Indicators.</p></li><li><p><strong>Default statuses</strong> are <code>UP</code>, <code>DOWN</code>, <code>OUT_OF_SERVICE</code>, and <code>UNKNOWN</code>.</p></li><li><p><strong>Custom Health Indicators</strong> can be implemented via <code>HealthIndicator</code>.</p></li><li><p><strong>Health statuses can be secured</strong> and restricted via Spring Security.</p></li><li><p><strong>Kubernetes and cloud providers</strong> use health endpoints for auto-scaling and service management.</p></li><li><p><strong>Health data can be monitored</strong> using Prometheus, Grafana, or CloudWatch.</p></li></ul>",
                "answers": [
                    "<p>Custom status values can be created</p>",
                    "<p>The status with the least severity is used as top-level status</p>",
                    "<p>The severity order cannot be changed due to security reasons</p>",
                    "<p>The built-in status values are DOWN, OUT_OF_SERVICE, UNKNOWN and UP is decreasing order of severity</p>",
                    "<p>The last status in a sorted list of HealthIndicators is used to derive the final system health</p>"
                ]
            },
            "correct_response": [
                "a",
                "d"
            ],
            "section": "Spring Boot Actuator",
            "question_plain": "Which two statements are correct regarding the Health Indicator status?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571865,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>If a class is annotated with @Component, what should be done to have Spring automatically detect the annotated class and load it as a bean?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework Documentation - Component Scanning](https://docs.spring.io/spring-framework/reference/core/beans/classpath-scanning.html)",
                    "[Spring Boot Documentation - Spring Boot Application Setup](https://docs.spring.io/spring-boot/docs/current/reference/html/using.html#using.structuring-your-code)",
                    "[Baeldung - Guide to @Component and @ComponentScan](https://www.baeldung.com/spring-component-scanning)"
                ],
                "feedbacks": [
                    "<p>The <code>@Scope</code> annotation is used to define a bean's scope (<code>singleton</code>, <code>prototype</code>, etc.), but it does not influence whether a class is detected and loaded as a Spring bean. <code>@Component</code> scanning must still be configured separately.</p>",
                    "<p>The <code>@Component</code> annotation can optionally take a name, but it is not required. If no name is provided, Spring will generate a default name based on the class name. Whether a bean is detected depends on <code>@ComponentScan</code>, not the bean name.</p>",
                    "<p>The <code>@Bean</code> annotation is used inside <code>@Configuration</code> classes to manually define beans. It is <strong>not</strong> required for <code>@Component</code> classes to be detected since <code>@ComponentScan</code> is responsible for finding <code>@Component</code>-annotated beans.</p>",
                    "<p>By default, Spring Boot <strong>automatically scans components in the package of the main application class and its sub-packages</strong>. However, if a <code>@Component</code> class is outside the scanned package, <code>@ComponentScan</code> must be explicitly declared to specify the package where Spring should look for beans.</p><p><strong>Example:</strong></p><pre class=\"prettyprint linenums\">@Configuration\n@ComponentScan(basePackages = \"com.example.service\")\npublic class AppConfig {\n}\n</pre><p>This ensures that Spring scans for <code>@Component</code> classes in the <code>com.example.service</code> package and registers them as beans.</p>"
                ],
                "explanation": "<p>Spring provides <strong>automatic bean detection</strong> using the <code>@Component</code> annotation and scans for such annotated classes using <code>@ComponentScan</code>.</p><p><strong>How </strong><code><strong>@Component</strong></code><strong> Works</strong></p><ul><li><p>The <code>@Component</code> annotation marks a class as a <strong>Spring-managed bean</strong>, allowing it to be automatically detected and registered in the Spring container.</p></li><li><p>It is a <strong>stereotype annotation</strong>, and there are specialized versions of it:</p><ul><li><p><code>@Service</code> (for service-layer components)</p></li><li><p><code>@Repository</code> (for data-access components)</p></li><li><p><code>@Controller</code> (for Spring MVC controllers)</p></li></ul></li></ul><p>Example:</p><pre class=\"prettyprint linenums\">@Component\npublic class MyService {\n    public void performTask() {\n        System.out.println(\"Task executed.\");\n    }\n}\n</pre><p>Spring detects this class and registers it as a bean.</p><p><strong>How </strong><code><strong>@ComponentScan</strong></code><strong> Works</strong></p><ul><li><p>By default, Spring Boot scans the package of the class with <code>@SpringBootApplication</code>, including its sub-packages.</p></li><li><p>If you need to <strong>scan additional packages</strong>, explicitly specify them using <code>@ComponentScan</code>.</p></li></ul><p>Example:</p><pre class=\"prettyprint linenums\">@Configuration\n@ComponentScan(basePackages = {\"com.example.services\", \"com.example.repositories\"})\npublic class AppConfig {\n}\n</pre><p><strong>How Spring Boot Automatically Scans Components</strong></p><ul><li><p>If your main application class is in <code>com.example</code>, Spring Boot <strong>automatically scans</strong> everything in <code>com.example</code> and its sub-packages.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">@SpringBootApplication\npublic class MyApplication {\n    public static void main(String[] args) {\n        SpringApplication.run(MyApplication.class, args);\n    }\n}\n</pre><p> This setup automatically detects <code>@Component</code> beans in <code>com.example</code> and sub-packages.</p></li></ul><p><strong>Manually Registering Beans with </strong><code><strong>@Bean</strong></code></p><ul><li><p>If a class is <strong>not</strong> annotated with <code>@Component</code>, it can be registered manually using <code>@Bean</code> in a configuration class:</p><pre class=\"prettyprint linenums\">@Configuration\npublic class BeanConfig {\n    @Bean\n    public MyService myService() {\n        return new MyService();\n    }\n}\n</pre></li></ul><p><strong>4. Practical Guide for Spring Developers</strong></p><ol><li><p><strong>When to Use </strong><code><strong>@Component</strong></code><strong>?</strong></p><ul><li><p>Use <code>@Component</code> for general-purpose beans.</p></li><li><p>Use <code>@Service</code>, <code>@Repository</code>, and <code>@Controller</code> for specific layers.</p></li></ul></li><li><p><strong>When to Use </strong><code><strong>@ComponentScan</strong></code><strong>?</strong></p><ul><li><p>If all components are in the same package as your main class, <strong>you don't need </strong><code><strong>@ComponentScan</strong></code>.</p></li><li><p>If your components are in a different package, add <code>@ComponentScan(basePackages = \"your.package\")</code>.</p></li></ul></li><li><p><strong>How to Debug Missing Beans?</strong></p><ul><li><p>Ensure the package is being scanned.</p></li><li><p>Check if the class has <code>@Component</code> (or a specialization like <code>@Service</code>).</p></li><li><p>Verify that Spring Boot is running the correct package.</p></li></ul></li><li><p><strong>How to Exclude a Bean from Scanning?</strong></p><ul><li><p>Use <code>@ComponentScan</code> with the <code>excludeFilters</code> attribute:</p><pre class=\"prettyprint linenums\">@ComponentScan(basePackages = \"com.example\", excludeFilters = @ComponentScan.Filter(type = FilterType.ANNOTATION, value = Repository.class))\n</pre></li><li><p>Or, use <code>@ConditionalOnMissingBean</code> if using Spring Boot auto-configuration.</p></li></ul></li><li><p><strong>Common Mistakes to Avoid</strong></p><ul><li><p><strong>Forgetting to scan components</strong>: If beans are not in the main package, <code>@ComponentScan</code> is required.</p></li><li><p><strong>Manually creating beans unnecessarily</strong>: Use <code>@Component</code> instead of defining <code>@Bean</code> manually when possible.</p></li><li><p><strong>Using </strong><code><strong>@Component</strong></code><strong> on abstract classes</strong>: Spring cannot instantiate abstract classes, so <code>@Component</code> will not work.</p></li></ul></li></ol><p>By understanding <code>@Component</code>, <code>@ComponentScan</code>, and Spring Boot's automatic scanning, developers can <strong>efficiently manage dependencies and ensure smooth application startup</strong>.</p>",
                "answers": [
                    "<p>Ensure a valid @Scope for the class is specified</p>",
                    "<p>Ensure a valid bean name in the @Component annotation is specified</p>",
                    "<p>Ensure a valid @Bean for the class is specified</p>",
                    "<p>Ensure a valid @ComponentScan annotation in the Java configuration is specified</p>"
                ]
            },
            "correct_response": [
                "d"
            ],
            "section": "Spring Core",
            "question_plain": "If a class is annotated with @Component, what should be done to have Spring automatically detect the annotated class and load it as a bean?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571867,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are true about Spring Data?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Data JPA Reference Documentation](https://docs.spring.io/spring-data/jpa/docs/current/reference/html/)",
                    "[Baeldung - Introduction to Spring Data](https://www.baeldung.com/spring-data)"
                ],
                "feedbacks": [
                    "<p>Spring Data provides a <strong>repository abstraction</strong> that eliminates the need for writing repetitive CRUD operations.</p><ul><li><p>It allows automatic generation of query methods based on method names.</p></li><li><p>The <code>CrudRepository</code> and <code>JpaRepository</code> interfaces reduce boilerplate code for data access.</p></li></ul>",
                    "<p>Spring Data is <strong>not limited to relational databases</strong>. There are implementations for:</p><ul><li><p><strong>Spring Data JPA</strong> (for relational databases with JPA and Hibernate)</p></li><li><p><strong>Spring Data MongoDB</strong> (for NoSQL databases)</p></li><li><p><strong>Spring Data Neo4j</strong> (for graph databases)</p></li><li><p><strong>Spring Data Redis</strong> (for in-memory key-value stores)</p></li></ul>",
                    "<p>This is incorrect because <strong>JPA annotations are specific to relational databases</strong>.</p><ul><li><p>MongoDB, Neo4j, and Redis use their <strong>own</strong> Spring Data modules (<code>spring-data-mongodb</code>, <code>spring-data-neo4j</code>, <code>spring-data-redis</code>), which do not rely on JPA.</p></li></ul>",
                    "<p>Incorrect because Spring Data supports <strong>both relational and NoSQL databases</strong>.</p><ul><li><p>It provides integrations with <strong>document databases (MongoDB, Couchbase), graph databases (Neo4j), key-value stores (Redis), and search engines (Elasticsearch, Solr).</strong></p></li></ul>",
                    "<p>Incorrect because Spring Data <strong>is fully compatible</strong> with Spring MVC.</p><ul><li><p>Spring Data repositories can be <strong>directly injected into Spring MVC controllers</strong> to retrieve data.</p></li></ul>"
                ],
                "explanation": "<p>Spring Data is a <strong>high-level abstraction</strong> designed to <strong>simplify data access</strong> across multiple storage technologies. It provides a <strong>unified programming model</strong> for working with relational databases, NoSQL databases, and in-memory data stores.</p><p><strong>Core Features of Spring Data</strong></p><ol><li><p><strong>Repository Abstraction</strong></p><ul><li><p>Spring Data provides interfaces like <code>CrudRepository</code>, <code>JpaRepository</code>, and <code>MongoRepository</code> that eliminate the need for writing basic CRUD operations.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">public interface UserRepository extends JpaRepository&lt;User, Long&gt; {\n    List&lt;User&gt; findByLastName(String lastName);\n}\n</pre></li><li><p>The method <code>findByLastName</code> is automatically implemented by Spring Data.</p></li></ul></li><li><p><strong>Support for Multiple Data Stores</strong></p><ul><li><p>Spring Data supports:</p><ul><li><p><strong>Relational databases</strong>: JPA, JDBC, Hibernate</p></li><li><p><strong>NoSQL databases</strong>: MongoDB, Neo4j, Cassandra, Couchbase</p></li><li><p><strong>In-memory stores</strong>: Redis</p></li><li><p><strong>Search engines</strong>: Elasticsearch, Solr</p></li></ul></li></ul></li><li><p><strong>Query Methods and Derived Queries</strong></p><ul><li><p>Developers can define query methods based on method naming conventions, and Spring Data will automatically generate the SQL or NoSQL queries.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">List&lt;Customer&gt; findByFirstName(String firstName);\n</pre></li></ul></li><li><p><strong>Pagination and Sorting Support</strong></p><ul><li><p>Spring Data provides built-in pagination and sorting.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">Page&lt;User&gt; findByLastName(String lastName, Pageable pageable);\n</pre></li></ul></li><li><p><strong>Custom Query Support</strong></p><ul><li><p>You can define custom queries using:</p><ul><li><p><strong>JPQL</strong>:</p><pre class=\"prettyprint linenums\">@Query(\"SELECT u FROM User u WHERE u.email = ?1\")\nUser findByEmail(String email);\n</pre></li><li><p><strong>Native SQL</strong>:</p><pre class=\"prettyprint linenums\">@Query(value = \"SELECT * FROM users WHERE email = ?1\", nativeQuery = true)\nUser findByEmailNative(String email);\n</pre></li></ul></li></ul></li><li><p><strong>Transactions and Caching</strong></p><ul><li><p>Spring Data integrates with <strong>Spring Transaction Management</strong>.</p></li><li><p>It also supports <strong>second-level caching</strong> for databases.</p></li></ul></li></ol><p><strong>4. Practical Developer Guide for Using Spring Data</strong></p><p><strong>How to Use Spring Data in a Spring Boot Application</strong></p><ol><li><p><strong>Add Dependencies</strong></p><ul><li><p>For <strong>JPA (Relational Databases)</strong>:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-starter-data-jpa&lt;/artifactId&gt;\n&lt;/dependency&gt;\n</pre></li><li><p>For <strong>MongoDB</strong>:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-starter-data-mongodb&lt;/artifactId&gt;\n&lt;/dependency&gt;\n</pre></li><li><p>For <strong>Redis</strong>:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-starter-data-redis&lt;/artifactId&gt;\n&lt;/dependency&gt;\n</pre></li></ul></li><li><p><strong>Define an Entity (For JPA)</strong></p><pre class=\"prettyprint linenums\">@Entity\npublic class User {\n    @Id\n    @GeneratedValue(strategy = GenerationType.IDENTITY)\n    private Long id;\n    private String firstName;\n    private String lastName;\n}\n</pre></li><li><p><strong>Create a Repository Interface</strong></p><pre class=\"prettyprint linenums\">public interface UserRepository extends JpaRepository&lt;User, Long&gt; {\n    List&lt;User&gt; findByLastName(String lastName);\n}\n</pre></li><li><p><strong>Use the Repository in a Service</strong></p><pre class=\"prettyprint linenums\">@Service\npublic class UserService {\n    @Autowired\n    private UserRepository userRepository;\n\n    public List&lt;User&gt; getUsersByLastName(String lastName) {\n        return userRepository.findByLastName(lastName);\n    }\n}\n</pre></li><li><p><strong>Expose Data in a REST Controller</strong></p><pre class=\"prettyprint linenums\">@RestController\n@RequestMapping(\"/users\")\npublic class UserController {\n    @Autowired\n    private UserService userService;\n\n    @GetMapping(\"/{lastName}\")\n    public List&lt;User&gt; getUsers(@PathVariable String lastName) {\n        return userService.getUsersByLastName(lastName);\n    }\n}\n</pre></li></ol><p><strong>Best Practices for Spring Data</strong></p><ol><li><p><strong>Use Interface-Based Repositories</strong></p><ul><li><p>Extend <code>JpaRepository</code>, <code>CrudRepository</code>, or <code>MongoRepository</code> instead of writing DAO code manually.</p></li></ul></li><li><p><strong>Optimize Queries</strong></p><ul><li><p>Use <strong>pagination</strong> to avoid large result sets.</p></li><li><p>Prefer <strong>JPQL</strong> queries over native queries when possible.</p></li></ul></li><li><p><strong>Avoid N+1 Query Issues</strong></p><ul><li><p>Use <code>@EntityGraph</code> to fetch related entities in a single query.</p></li></ul></li><li><p><strong>Enable Caching for Performance</strong></p><ul><li><p>Use <strong>Spring Cache</strong> to cache frequently accessed data.</p></li></ul></li><li><p><strong>Secure Data Access</strong></p><ul><li><p>Use <strong>Spring Security</strong> to restrict access to repositories.</p></li><li><p>Avoid exposing sensitive information in query results.</p></li></ul></li></ol>",
                "answers": [
                    "<p>Spring Data can greatly reduce the amount of \"boilerplate\" code typically needed for data access</p>",
                    "<p>Spring Data implementations exist for many data storage types, such as MongoDB, Neo4j, and Redis</p>",
                    "<p>Spring Data works by applying the JPA annotations to data stores such as MongoDB, Neo4j, and Redis</p>",
                    "<p>Spring Data is specifically designed for JPA, JDBC and relational database access only</p>",
                    "<p>Spring Data cannot be used together with Spring MVC</p>"
                ]
            },
            "correct_response": [
                "a",
                "b"
            ],
            "section": "Spring Data",
            "question_plain": "Which two statements are true about Spring Data?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571869,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are true regarding Spring and Spring Boot Testing?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Testing Documentation](https://docs.spring.io/spring-boot/reference/testing/index.html#testing)",
                    "[Baeldung - Guide to Spring Boot Testing](https://www.baeldung.com/spring-boot-testing)"
                ],
                "feedbacks": [
                    "<p>Spring Boot <strong>does not</strong> provide built-in support for EasyMock. Instead, <strong>Mockito</strong> is the default mocking framework in Spring Boot tests.</p><ul><li><p>If you want to use EasyMock, you need to manually add its dependency.</p></li></ul>",
                    "<p>Spring Boot provides <strong>full integration testing</strong> and <strong>slice testing</strong> (focused tests for specific layers like repositories, services, and controllers).</p><ul><li><p><strong>Integration Testing:</strong> Uses <code>@SpringBootTest</code> to load the full <code>ApplicationContext</code>.</p></li><li><p><strong>Slice Testing:</strong> Uses annotations like <code>@WebMvcTest</code>, <code>@DataJpaTest</code>, and <code>@MockBean</code> to test specific layers.</p></li></ul>",
                    "<p>This is incorrect because <strong>Mockito Spy (</strong><code><strong>@SpyBean</strong></code><strong>) is fully supported in Spring Boot testing</strong> and is provided by the <code>spring-boot-test</code> module.</p><ul><li><p>Example:</p><pre class=\"prettyprint linenums\">@SpyBean\nprivate MyService myService;</pre></li></ul>",
                    "<p><code>@SpringBootTest</code> loads the <strong>entire</strong> Spring application context for integration tests.</p><p><code>@SpringJUnitConfig</code> is an alias for <code>@ExtendWith(SpringExtension.class)</code> + <code>@ContextConfiguration</code>, used in traditional Spring tests.</p>",
                    "<ul><li><p><code>@Mock</code> comes from <strong>Mockito</strong>, not <code>spring-test</code>.</p></li><li><p><code>@MockBean</code> is provided by <strong>Spring Boot Test</strong>, not <code>spring-test</code>.</p></li><li><p>The correct way to mock dependencies in Spring Boot tests is:</p><pre class=\"prettyprint linenums\">@MockBean\nprivate MyRepository myRepository;</pre></li></ul>"
                ],
                "explanation": "<p>Spring Boot provides a <strong>comprehensive testing framework</strong> that includes <strong>unit tests, integration tests, and slice tests</strong>.</p><p><strong>1. Types of Tests in Spring Boot</strong></p><ol><li><p><strong>Unit Tests (Testing Individual Components)</strong></p><ul><li><p>Tests small units like services and utility classes.</p></li><li><p>Uses <strong>Mockito</strong> to mock dependencies.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">@ExtendWith(MockitoExtension.class)\npublic class MyServiceTest {\n    @Mock\n    private MyRepository myRepository;\n\n    @InjectMocks\n    private MyService myService;\n\n    @Test\n    void testServiceLogic() {\n        when(myRepository.findById(1L)).thenReturn(Optional.of(new User(\"John\")));\n        assertEquals(\"John\", myService.getUserName(1L));\n    }\n}\n</pre></li></ul></li><li><p><strong>Integration Tests (Testing the Entire Application Context)</strong></p><ul><li><p>Loads the <strong>full application context</strong> using <code>@SpringBootTest</code>.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">@SpringBootTest\n@RunWith(SpringRunner.class)\npublic class MyApplicationTest {\n    @Autowired\n    private MyService myService;\n\n    @Test\n    void testApplicationStartup() {\n        assertNotNull(myService);\n    }\n}\n</pre></li></ul></li><li><p><strong>Slice Tests (Testing Specific Layers)</strong></p><ul><li><p>Loads only <strong>parts of the application</strong>.</p></li><li><p>Uses annotations like:</p><ul><li><p><code>@WebMvcTest</code> (for testing controllers)</p></li><li><p><code>@DataJpaTest</code> (for testing JPA repositories)</p></li><li><p><code>@MockBean</code> (for mocking dependencies)</p></li></ul></li></ul><p>Example:</p><pre class=\"prettyprint linenums\">@WebMvcTest(MyController.class)\npublic class MyControllerTest {\n    @Autowired\n    private MockMvc mockMvc;\n\n    @MockBean\n    private MyService myService;\n\n    @Test\n    void testControllerEndpoint() throws Exception {\n        when(myService.getData()).thenReturn(\"Hello\");\n        mockMvc.perform(get(\"/data\"))\n            .andExpect(status().isOk())\n            .andExpect(content().string(\"Hello\"));\n    }\n}\n</pre></li></ol><p><strong>4. Practical Developer Guide for Spring Boot Testing</strong></p><p><strong>How to Use Spring Boot Testing in a Project</strong></p><ol><li><p><strong>Add Dependencies</strong><br>Spring Boot Starter Test provides all necessary testing dependencies:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-starter-test&lt;/artifactId&gt;\n    &lt;scope&gt;test&lt;/scope&gt;\n&lt;/dependency&gt;\n</pre></li><li><p><strong>Mock Dependencies Using </strong><code><strong>@MockBean</strong></code></p><ul><li><p><code>@MockBean</code> replaces an actual Spring bean with a mock.</p></li></ul><pre class=\"prettyprint linenums\">@SpringBootTest\npublic class MyServiceTest {\n    @MockBean\n    private MyRepository myRepository;\n\n    @Autowired\n    private MyService myService;\n\n    @Test\n    void testMocking() {\n        when(myRepository.getData()).thenReturn(\"Mocked Data\");\n        assertEquals(\"Mocked Data\", myService.getData());\n    }\n}\n</pre></li><li><p><strong>Test REST Controllers with </strong><code><strong>MockMvc</strong></code></p><pre class=\"prettyprint linenums\">@WebMvcTest(MyController.class)\npublic class MyControllerTest {\n    @Autowired\n    private MockMvc mockMvc;\n\n    @MockBean\n    private MyService myService;\n\n    @Test\n    void testEndpoint() throws Exception {\n        when(myService.getData()).thenReturn(\"Hello\");\n        mockMvc.perform(get(\"/api/data\"))\n            .andExpect(status().isOk())\n            .andExpect(content().string(\"Hello\"));\n    }\n}\n</pre></li><li><p><strong>Use </strong><code><strong>@DataJpaTest</strong></code><strong> for Repository Tests</strong></p><pre class=\"prettyprint linenums\">@DataJpaTest\npublic class MyRepositoryTest {\n    @Autowired\n    private MyRepository myRepository;\n\n    @Test\n    void testFindById() {\n        myRepository.save(new User(\"John\"));\n        Optional&lt;User&gt; user = myRepository.findById(1L);\n        assertTrue(user.isPresent());\n        assertEquals(\"John\", user.get().getName());\n    }\n}\n</pre></li></ol><p><strong>Best Practices for Spring Boot Testing</strong></p><ul><li><p><strong>Use </strong><code><strong>@MockBean</strong></code><strong> for external dependencies</strong> instead of loading the full application context.</p></li><li><p><strong>Use </strong><code><strong>@WebMvcTest</strong></code><strong> instead of </strong><code><strong>@SpringBootTest</strong></code> when testing only controllers.</p></li><li><p><strong>Write integration tests for critical features</strong> that involve database access.</p></li><li><p><strong>Use TestContainers for database testing</strong> instead of an in-memory database.</p></li></ul>",
                "answers": [
                    "<p>EasyMock is supported out of the box</p>",
                    "<p>Integration and slice testing are both supported</p>",
                    "<p>Mockito spy is not supported in Spring Boot testing by default</p>",
                    "<p>@SpringBootTest or @SpringJUnitConfig can be used for creating an ApplicationContext</p>",
                    "<p>The spring-test dependency provides annotations such as @Mock and @MockBean</p>"
                ]
            },
            "correct_response": [
                "b",
                "d"
            ],
            "section": "Testing",
            "question_plain": "Which two statements are true regarding Spring and Spring Boot Testing?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571871,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are true regarding a Spring Boot based Spring MVC applications?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Web Application Documentation](https://docs.spring.io/spring-boot/reference/web/index.html)"
                ],
                "feedbacks": [
                    "<ul><li><p>Spring MVC <strong>does not</strong> automatically start an <strong>in-memory database</strong>.</p></li><li><p>An in-memory database (<strong>H2, HSQLDB, or Derby</strong>) is <strong>only started if </strong><code><strong>spring-boot-starter-data-jpa</strong></code><strong> and an H2/HSQLDB dependency are present</strong>.</p></li><li><p>For pure Spring MVC apps (without JPA or database dependencies), no database is started.</p></li></ul>",
                    "<ul><li><p>Spring Boot <strong>uses Tomcat as the default embedded servlet container</strong>, but it <strong>can be replaced</strong> with:</p><ul><li><p><strong>Jetty</strong> (<code>spring-boot-starter-jetty</code>)</p></li><li><p><strong>Undertow</strong> (<code>spring-boot-starter-undertow</code>)</p></li></ul></li><li><p>To use Undertow instead of Tomcat:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-starter-undertow&lt;/artifactId&gt;\n&lt;/dependency&gt;\n</pre></li></ul>",
                    "<ul><li><p><strong>Spring Boot auto-configures an embedded servlet container</strong> (Tomcat by default).</p></li><li><p>This allows running Spring MVC applications <strong>without deploying a WAR file to an external server</strong>.</p></li><li><p>The embedded container <strong>can be disabled</strong> by setting:</p><pre class=\"prettyprint linenums\">properties\nCopyEditspring.main.web-application-type=none\n</pre></li></ul>",
                    "<ul><li><p><strong>Incorrect because Tomcat is the default.</strong></p></li><li><p>Jetty <strong>can be used instead</strong>, but it is <strong>not</strong> the default.</p></li></ul>",
                    "<ul><li><p><strong>Incorrect because the default port is 8080.</strong></p></li><li><p>The port can be changed in <code>application.properties</code>:</p><pre class=\"prettyprint linenums\">properties\nCopyEditserver.port=8088\n</pre></li><li><p>By default, if no port is specified, Spring Boot starts the application on <strong>port 8080</strong>.</p></li></ul>"
                ],
                "explanation": "<p>Spring Boot <strong>simplifies the configuration</strong> of Spring MVC applications by <strong>auto-configuring essential components</strong>, including an embedded servlet container.</p><p><strong>1. Embedded Servlet Container in Spring Boot</strong></p><ul><li><p>By default, Spring Boot <strong>starts an embedded Tomcat server</strong>.</p></li><li><p>The embedded server allows <strong>running applications as standalone JARs</strong>.</p></li><li><p>Supported embedded containers:</p><ul><li><p><strong>Tomcat</strong> (default)</p></li><li><p><strong>Jetty</strong></p></li><li><p><strong>Undertow</strong></p></li></ul></li></ul><p><strong>2. Changing the Default Embedded Servlet Container</strong></p><ul><li><p>To <strong>switch from Tomcat to Jetty</strong>:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-starter-jetty&lt;/artifactId&gt;\n&lt;/dependency&gt;</pre></li><li><p>To <strong>switch to Undertow</strong>:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-starter-undertow&lt;/artifactId&gt;\n&lt;/dependency&gt;</pre></li></ul><p><strong>3. Configuring the Embedded Server</strong></p><ul><li><p>Change the <strong>default port</strong> (8080 → 9090):</p><pre class=\"prettyprint linenums\">server.port=9090</pre></li><li><p>Enable <strong>HTTP/2 support</strong>:</p><pre class=\"prettyprint linenums\">server.http2.enabled=true</pre></li><li><p>Set <strong>custom context path</strong>:</p><pre class=\"prettyprint linenums\">server.servlet.context-path=/myapp</pre></li></ul><p><strong>4. Running a Spring MVC Application Without an Embedded Server</strong></p><ul><li><p>Disable the embedded web server (useful for <strong>WAR deployments</strong>):</p><pre class=\"prettyprint linenums\">spring.main.web-application-type=none</pre></li></ul><p><strong>4. Practical Developer Guide for Spring Boot MVC Applications</strong></p><p><strong>1. Creating a Spring Boot MVC Application</strong></p><ul><li><p><strong>Add Dependencies:</strong></p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-starter-web&lt;/artifactId&gt;\n&lt;/dependency&gt;</pre></li><li><p><strong>Create a Controller:</strong></p><pre class=\"prettyprint linenums\">@RestController\n@RequestMapping(\"/api\")\npublic class MyController {\n\n    @GetMapping(\"/hello\")\n    public String sayHello() {\n        return \"Hello, Spring Boot!\";\n    }\n}</pre></li><li><p><strong>Run the Application:</strong></p><pre class=\"prettyprint linenums\">@SpringBootApplication\npublic class MyApplication {\n    public static void main(String[] args) {\n        SpringApplication.run(MyApplication.class, args);\n    }\n}</pre></li></ul><p><strong>2. Changing the Embedded Server</strong></p><ul><li><p>To <strong>replace Tomcat with Jetty</strong>:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-starter-jetty&lt;/artifactId&gt;\n&lt;/dependency&gt;</pre></li><li><p>To <strong>replace Tomcat with Undertow</strong>:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-starter-undertow&lt;/artifactId&gt;\n&lt;/dependency&gt;</pre></li></ul><p><strong>3. Configuring Spring Boot MVC</strong></p><ul><li><p><strong>Set a Custom Server Port (Default: 8080)</strong></p><pre class=\"prettyprint linenums\">server.port=9090</pre></li><li><p><strong>Enable Compression</strong></p><pre class=\"prettyprint linenums\">server.compression.enabled=true</pre></li><li><p><strong>Disable Embedded Web Server (For WAR Deployments)</strong></p><pre class=\"prettyprint linenums\">spring.main.web-application-type=none</pre></li></ul><p><strong>4. Best Practices for Spring Boot MVC</strong></p><ul><li><p>Use <code><strong>@RestController</strong></code> instead of <code>@Controller</code> for REST APIs.</p></li><li><p>Optimize <strong>server performance</strong> using HTTP/2 and Gzip compression.</p></li><li><p>Implement <strong>exception handling</strong> using <code>@ControllerAdvice</code>.</p></li></ul>",
                "answers": [
                    "<p>Spring MVC starts up an in memory database by default</p>",
                    "<p>The default embedded servlet container can be replaced with Undertow</p>",
                    "<p>Spring Boot starts up an embedded servlet container by default</p>",
                    "<p>Jetty is the default servlet container</p>",
                    "<p>The default port of the embedded servlet container is 8088</p>"
                ]
            },
            "correct_response": [
                "b",
                "c"
            ],
            "section": "Spring Boot",
            "question_plain": "Which two statements are true regarding a Spring Boot based Spring MVC applications?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571873,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are correct regarding Spring Boot 2.x Actuator Metrics?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Actuator Documentation](https://docs.spring.io/spring-boot/reference/actuator/index.html)",
                    "[Baeldung - Guide to Spring Boot Actuator](https://www.baeldung.com/spring-boot-actuators)"
                ],
                "feedbacks": [
                    "<p>Spring Boot Actuator <strong>does not require</strong> an external monitoring system to function. It provides built-in endpoints to monitor and manage applications. However, it can integrate with external systems like Prometheus or Grafana for enhanced monitoring capabilities.</p>",
                    "<p>While <strong>tags</strong> (or dimensions) are useful for adding context to metrics, they are <strong>not mandatory</strong>. Metrics can be created without any tags.</p>",
                    "<p>A <strong>Timer</strong> in Micrometer (used by Spring Boot Actuator) <strong>records both the count of events and the total time taken</strong> for these events, providing insights into frequency and duration.</p>",
                    "<p>Spring Boot Actuator, through Micrometer, allows the creation of custom metrics using primitives like <strong>Counter</strong> (to count occurrences), <strong>Gauge</strong> (to measure current values), <strong>Timer</strong> (to measure durations), and <strong>DistributionSummary</strong> (to record samples and track distributions).</p>",
                    "<p>By default, <strong>only a limited set of Actuator endpoints are exposed over HTTP</strong> for security reasons. The <code>/actuator/metrics</code> endpoint may not be exposed by default and might require explicit configuration to be accessible.</p>"
                ],
                "explanation": "<p>Spring Boot Actuator provides production-ready features to help monitor and manage applications. In Spring Boot 2.x, Actuator integrates with <strong>Micrometer</strong>, offering a flexible and powerful metrics facade that supports numerous monitoring systems. ​</p><p><strong>Key Components:</strong></p><ol><li><p><strong>Micrometer Integration:</strong><br>Micrometer acts as an application metrics facade, supporting various monitoring systems like Prometheus, Grafana, and Datadog. It provides a simple API to collect metrics, which can be exported to multiple backends.</p></li><li><p><strong>Meter Primitives:</strong></p><ul><li><p><strong>Counter:</strong> Measures the count of events.​</p></li><li><p><strong>Gauge:</strong> Captures the current value of a metric.​</p></li><li><p><strong>Timer:</strong> Records the number of occurrences and the total time taken.</p></li><li><p><strong>DistributionSummary:</strong> Captures samples and tracks their distribution.​</p></li></ul></li><li><p><strong>Endpoints:</strong><br>Actuator provides various endpoints to interact with application metrics:</p><ul><li><p><strong>/actuator/metrics:</strong> Lists available metrics.</p></li><li><p><strong>/actuator/metrics/{metricName}:</strong> Provides details for a specific metric.​</p></li></ul></li><li><p><strong>Custom Metrics:</strong><br>Developers can define custom metrics using the Meter primitives to monitor application-specific parameters.</p></li><li><p><strong>Integration with Monitoring Systems:</strong><br>Actuator's metrics can be integrated with external monitoring systems for enhanced visualization and alerting.</p></li></ol><p><strong>4. Practical Developer Guide for Spring Boot 2.x Actuator Metrics</strong></p><p><strong>Setting Up Actuator:</strong></p><ol><li><p><strong>Add Dependency:</strong></p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-starter-actuator&lt;/artifactId&gt;\n&lt;/dependency&gt;</pre></li></ol><p><br></p><ol><li><p><strong>Enable Endpoints:</strong> Configure the <code>application.properties</code> or <code>application.yml</code> to expose desired endpoints:</p><pre class=\"prettyprint linenums\">management.endpoints.web.exposure.include=health,info,metrics</pre></li></ol><p><br></p><p><strong>Creating Custom Metrics:</strong></p><ol><li><p><strong>Inject </strong><code><strong>MeterRegistry</strong></code><strong>:</strong></p><pre class=\"prettyprint linenums\">@Component\npublic class CustomMetrics {\n\n    private final Counter requestCounter;\n\n    public CustomMetrics(MeterRegistry registry) {\n        this.requestCounter = registry.counter(\"custom.request.count\");\n    }\n\n    public void handleRequest() {\n        requestCounter.increment();\n        // handle the request\n    }\n}</pre></li></ol><p><br></p><p><strong>Accessing Metrics:</strong></p><ol><li><p><strong>List All Metrics:</strong> Access <code>/actuator/metrics</code> to view all available metrics.</p></li><li><p><strong>View Specific Metric:</strong> Access <code>/actuator/metrics/{metricName}</code> to view details of a specific metric.</p></li></ol><p><strong>Integrating with Monitoring Systems:</strong></p><ol><li><p><strong>Add Specific Registry Dependency:</strong> For Prometheus integration, add:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;io.micrometer&lt;/groupId&gt;\n    &lt;artifactId&gt;micrometer-registry-prometheus&lt;/artifactId&gt;\n&lt;/dependency&gt;</pre></li></ol><p><br></p><ol><li><p><strong>Configure Export:</strong> Set up properties to enable exporting metrics to the chosen system.</p></li></ol>",
                "answers": [
                    "<p>An external monitoring system must be used with Actuator</p>",
                    "<p>A metric must be created with one or more tags</p>",
                    "<p>Timer measures both the number of timed events and the total time of all events timed</p>",
                    "<p>Custom metrics can be measured using Meter primitives such as Counter, Gauge, Timer and DistributionSummary</p>",
                    "<p>The metrics endpoint /actuator/metrics is exposed over HTTP by default</p>"
                ]
            },
            "correct_response": [
                "c",
                "d"
            ],
            "section": "Spring Boot Actuator",
            "question_plain": "Which two statements are correct regarding Spring Boot 2.x Actuator Metrics?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571875,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p> Which three dependencies are provided by the spring-boot-starter-test?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Testing Documentation](https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.testing)"
                ],
                "feedbacks": [
                    "<ol><li><p>Spring Boot <strong>does not</strong> include EasyMock by default in <code>spring-boot-starter-test</code>.</p></li><li><p>Instead, <strong>Mockito</strong> is used as the default mocking framework.</p></li></ol>",
                    "<p><code>spring-test</code> is part of the Spring Framework and provides essential testing utilities, including: </p><ul><li><p><code>@SpringBootTest</code></p></li><li><p><code>@MockBean</code></p></li><li><p><code>TestRestTemplate</code></p></li><li><p>Support for <strong>integration testing</strong>.</p></li></ul>",
                    "<ul><li><p><code>Cucumber</code> is <strong>not</strong> included in <code>spring-boot-starter-test</code>.</p></li><li><p>Cucumber is a <strong>BDD (Behavior-Driven Development)</strong> tool and must be added separately.</p></li></ul>",
                    "<ul><li><p>PowerMock is <strong>not included</strong> in Spring Boot’s test starter.</p></li><li><p>Spring Boot prefers <strong>Mockito</strong>, which integrates seamlessly.</p></li></ul>",
                    "<ul><li><p><strong>JUnit 5 (Jupiter API) is included</strong> in <code>spring-boot-starter-test</code> as the primary testing framework.</p></li></ul>",
                    "<ul><li><p><code>Hamcrest</code> is included and provides <strong>matchers</strong> for writing expressive assertions in tests.</p></li></ul>"
                ],
                "explanation": "<p>Spring Boot provides the <code><strong>spring-boot-starter-test</strong></code> module, which is a convenient way to include all necessary <strong>testing dependencies</strong> in a Spring Boot application. This starter includes <strong>frameworks for unit testing, integration testing, and mocking</strong>.</p><p><strong>1. Dependencies Included in </strong><code><strong>spring-boot-starter-test</strong></code></p><p>When you add <code>spring-boot-starter-test</code> to your <code>pom.xml</code>:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-starter-test&lt;/artifactId&gt;\n    &lt;scope&gt;test&lt;/scope&gt;\n&lt;/dependency&gt;\n</pre><p>Spring Boot automatically <strong>includes the following libraries</strong>:</p><ol><li><p><strong>JUnit 5 (Jupiter API)</strong> – Core testing framework for writing and executing unit tests.</p></li><li><p><strong>Spring Test (</strong><code><strong>spring-test</strong></code><strong>)</strong> – Provides integration testing utilities, such as <code>@SpringBootTest</code>, <code>MockMvc</code>, and test support for <code>@Transactional</code>.</p></li><li><p><strong>Mockito</strong> – A popular Java mocking framework for writing unit tests.</p></li><li><p><strong>Hamcrest</strong> – A library providing expressive matchers for writing assertions.</p></li><li><p><strong>AssertJ</strong> – A fluent assertion library for writing easy-to-read test assertions.</p></li><li><p><strong>JSONPath</strong> – For asserting JSON responses in Spring Boot tests.</p></li><li><p><strong>Spring Boot Test Autoconfiguration</strong> – Adds Spring Boot-specific test configurations.</p></li></ol><p><strong>2. Breakdown of the Most Important Dependencies</strong></p><p><strong>1. JUnit 5 (Jupiter)</strong></p><ul><li><p>JUnit is the <strong>default</strong> testing framework in Spring Boot.</p></li><li><p>You can write unit tests using <code>@Test</code> annotations.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">import org.junit.jupiter.api.Test;\nimport static org.junit.jupiter.api.Assertions.assertEquals;\n\nclass ExampleTest {\n\n    @Test\n    void testAddition() {\n        int sum = 2 + 3;\n        assertEquals(5, sum);\n    }\n}\n</pre></li></ul><p><strong>2. Spring Test (</strong><code><strong>spring-test</strong></code><strong>)</strong></p><ul><li><p>Provides utilities for <strong>integration testing</strong>.</p></li><li><p>Allows testing of Spring beans, MVC controllers, and repositories.</p></li><li><p>Example of testing with <code>@SpringBootTest</code>:</p><pre class=\"prettyprint linenums\">@SpringBootTest\nclass ApplicationTests {\n\n    @Test\n    void contextLoads() {\n    }\n}\n</pre></li></ul><p><strong>3. Mockito (Mocking Framework)</strong></p><ul><li><p>Helps in unit testing by creating <strong>mock objects</strong> to simulate dependencies.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">@ExtendWith(MockitoExtension.class)\nclass MyServiceTest {\n\n    @Mock\n    private MyRepository repository;\n\n    @InjectMocks\n    private MyService service;\n\n    @Test\n    void testFindById() {\n        when(repository.findById(1L)).thenReturn(Optional.of(new User(\"John\")));\n        assertEquals(\"John\", service.getUserName(1L));\n    }\n}\n</pre></li></ul><p><strong>4. Hamcrest (Matchers for Assertions)</strong></p><ul><li><p>Provides expressive assertion methods.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">import static org.hamcrest.MatcherAssert.assertThat;\nimport static org.hamcrest.Matchers.*;\n\n@Test\nvoid testHamcrestMatchers() {\n    String name = \"SpringBoot\";\n    assertThat(name, startsWith(\"Spring\"));\n}\n</pre></li></ul><p><strong>5. AssertJ (Fluent Assertions)</strong></p><ul><li><p>More readable assertions compared to JUnit.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">import static org.assertj.core.api.Assertions.assertThat;\n\n@Test\nvoid testAssertJ() {\n    int[] numbers = {1, 2, 3};\n    assertThat(numbers).contains(2).doesNotContain(5);\n}\n</pre></li></ul><p><strong>6. JSONPath (Testing JSON Responses)</strong></p><ul><li><p>Helps in <strong>validating JSON responses</strong> in REST API tests.</p></li><li><p>Example:</p><pre class=\"prettyprint linenums\">@Test\nvoid testJsonPath() {\n    String json = \"{ \\\"name\\\": \\\"Spring\\\", \\\"version\\\": \\\"2.5.0\\\" }\";\n    assertThatJson(json).node(\"name\").isEqualTo(\"Spring\");\n}\n</pre></li></ul><p><strong>3. How to Use </strong><code><strong>spring-boot-starter-test</strong></code><strong> Effectively</strong></p><ol><li><p><strong>Use </strong><code><strong>@SpringBootTest</strong></code><strong> for Integration Testing</strong></p><ul><li><p>Loads the entire Spring application context for full integration tests.</p></li></ul><pre class=\"prettyprint linenums\">@SpringBootTest\nclass ApplicationIntegrationTest {\n    @Test\n    void contextLoads() {\n    }\n}\n</pre></li><li><p><strong>Use </strong><code><strong>@WebMvcTest</strong></code><strong> for Controller Testing</strong></p><ul><li><p>Loads only the <strong>controller layer</strong> without starting the full application.</p></li></ul><pre class=\"prettyprint linenums\">@WebMvcTest(MyController.class)\nclass ControllerTest {\n\n    @Autowired\n    private MockMvc mockMvc;\n\n    @MockBean\n    private MyService service;\n\n    @Test\n    void testEndpoint() throws Exception {\n        when(service.getMessage()).thenReturn(\"Hello\");\n        mockMvc.perform(get(\"/message\"))\n            .andExpect(status().isOk())\n            .andExpect(content().string(\"Hello\"));\n    }\n}\n</pre></li><li><p><strong>Use </strong><code><strong>@DataJpaTest</strong></code><strong> for Repository Testing</strong></p><ul><li><p>Loads an <strong>in-memory database</strong> and tests repository methods.</p></li></ul><pre class=\"prettyprint linenums\">@DataJpaTest\nclass RepositoryTest {\n\n    @Autowired\n    private UserRepository repository;\n\n    @Test\n    void testFindByName() {\n        repository.save(new User(\"Alice\"));\n        User user = repository.findByName(\"Alice\");\n        assertThat(user.getName()).isEqualTo(\"Alice\");\n    }\n}\n</pre></li><li><p><strong>Mock Dependencies Using </strong><code><strong>@MockBean</strong></code></p><ul><li><p>Replaces actual Spring beans with mocks in tests.</p></li></ul><pre class=\"prettyprint linenums\">@SpringBootTest\nclass MyServiceTest {\n\n    @MockBean\n    private MyRepository repository;\n\n    @Autowired\n    private MyService service;\n\n    @Test\n    void testMocking() {\n        when(repository.getData()).thenReturn(\"Mocked Data\");\n        assertEquals(\"Mocked Data\", service.getData());\n    }\n}\n</pre></li></ol><p><strong>4. Best Practices for Testing in Spring Boot</strong></p><ul><li><p><strong>Use </strong><code><strong>@SpringBootTest</strong></code><strong> only for full integration tests</strong> (avoid slow startup times).</p></li><li><p><strong>Use </strong><code><strong>@MockBean</strong></code><strong> for service-layer tests</strong> to avoid unnecessary database interactions.</p></li><li><p><strong>Use </strong><code><strong>@DataJpaTest</strong></code><strong> for repository testing</strong> with an in-memory database.</p></li><li><p><strong>Enable logs for debugging failed tests</strong>:</p><pre class=\"prettyprint linenums\">logging.level.org.springframework=DEBUG</pre></li></ul>",
                "answers": [
                    "<p>EasyMock</p>",
                    "<p>spring-test</p>",
                    "<p>Cucumber</p>",
                    "<p>PowerMock</p>",
                    "<p>JUnit</p>",
                    "<p>Hamcrest</p>"
                ]
            },
            "correct_response": [
                "b",
                "e",
                "f"
            ],
            "section": "Testing",
            "question_plain": "Which three dependencies are provided by the spring-boot-starter-test?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571877,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<pre class=\"prettyprint linenums\">@PutMapping(\"/accounts/{id}\")\npublic void update(){}</pre><p>Which option is a valid way to retrieve the account id?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Documentation - @PathVariable and @RequestParam](https://docs.spring.io/spring-framework/reference/web/webmvc/mvc-controller/ann-methods/response-body.html)",
                    "[Baeldung - Guide to @PathVariable and @RequestParam](https://www.baeldung.com/spring-requestparam-vs-pathvariable)"
                ],
                "feedbacks": [
                    "<p><code>@RequestParam</code> is used for query parameters, such as <code>/accounts/update?id=5</code>. In this case, the <code>id</code> is part of the URL path (<code>/accounts/{id}</code>), not a query parameter, so <code>@RequestParam</code> will not work.</p>",
                    "<p><code>@PathVariable</code> is the correct annotation for extracting values from path parameters, but you should use id argument name to match name from @PutMapping annotation.</p><p>Example:</p><pre class=\"prettyprint linenums\">@PutMapping(\"/accounts/{id}\")\npublic void update(@PathVariable long id) {\n    System.out.println(\"Updating account with ID: \" + id);\n}</pre>",
                    "<p><code>@RequestParam(\"id\")</code> expects query parameters, not path parameters. Since the <code>id</code> is part of the URL path, this approach will not work.</p>",
                    "<p>Using <code>@PathVariable(\"id\")</code> explicitly specifies that the method parameter corresponds to the <code>{id}</code> in the URL path.</p><p>Example:</p><pre class=\"prettyprint linenums\">@PutMapping(\"/accounts/{id}\")\npublic void update(@PathVariable(\"id\") String accountId) {\n    System.out.println(\"Updating account with ID: \" + accountId);\n}\n</pre>"
                ],
                "explanation": "<p><strong>Using </strong><code><strong>@PathVariable</strong></code><strong> for Path Parameters</strong></p><p><code>@PathVariable</code> is used to extract values from the URL path. It is commonly used in RESTful APIs where values are embedded in the URI.</p><p>Example:</p><pre class=\"prettyprint linenums\">@GetMapping(\"/users/{userId}\")\npublic String getUser(@PathVariable(\"userId\") Long id) {\n    return \"User ID: \" + id;\n}</pre><p>If the request is <code>GET /users/42</code>, the method extracts <code>42</code> as <code>userId</code>.</p><p><strong>Using </strong><code><strong>@RequestParam</strong></code><strong> for Query Parameters</strong></p><p><code>@RequestParam</code> is used to extract values from the query string instead of the path.</p><p>Example:</p><pre class=\"prettyprint linenums\">@GetMapping(\"/users\")\npublic String getUser(@RequestParam(\"id\") Long id) {\n    return \"User ID: \" + id;\n}</pre><p>If the request is <code>GET /users?id=42</code>, the method extracts <code>42</code> as <code>id</code>.</p><p><strong>When to Use </strong><code><strong>@PathVariable</strong></code><strong> vs. </strong><code><strong>@RequestParam</strong></code></p><p>Use <code>@PathVariable</code> when the value is required and part of the <strong>URL structure</strong>. Use <code>@RequestParam</code> when the value is <strong>optional</strong> or provided as a query parameter.</p><p><strong>4. Practical Developer Guide for Using Path Parameters in Spring Boot</strong></p><p><strong>Correct Usage of </strong><code><strong>@PathVariable</strong></code></p><pre class=\"prettyprint linenums\">@RestController\n@RequestMapping(\"/accounts\")\npublic class AccountController {\n\n    @PutMapping(\"/{id}\")\n    public ResponseEntity&lt;String&gt; updateAccount(@PathVariable(\"id\") Long id) {\n        return ResponseEntity.ok(\"Updating account with ID: \" + id);\n    }\n}\n</pre><p>Request: <code>PUT /accounts/123</code><br>Response: <code>\"Updating account with ID: 123\"</code></p><p><strong>Using </strong><code><strong>@PathVariable</strong></code><strong> with Multiple Parameters</strong></p><pre class=\"prettyprint linenums\">@GetMapping(\"/users/{userId}/posts/{postId}\")\npublic String getUserPost(@PathVariable Long userId, @PathVariable Long postId) {\n    return \"User ID: \" + userId + \", Post ID: \" + postId;\n}\n</pre><p>Request: <code>GET /users/10/posts/5</code><br>Response: <code>\"User ID: 10, Post ID: 5\"</code></p><p><strong>Handling Optional Path Variables</strong></p><p>To make a path variable optional, use <code>required = false</code>.</p><pre class=\"prettyprint linenums\">@GetMapping(\"/users/{userId}\")\npublic String getUser(@PathVariable(required = false) Long userId) {\n    return userId != null ? \"User ID: \" + userId : \"No user ID provided\";\n}\n</pre><p><strong>Validating Path Variables</strong></p><p>Use <code>@Min</code> to validate numeric path variables.</p><pre class=\"prettyprint linenums\">@GetMapping(\"/accounts/{id}\")\npublic ResponseEntity&lt;String&gt; getAccount(@PathVariable @Min(1) Long id) {\n    return ResponseEntity.ok(\"Account ID: \" + id);\n}</pre><p>If an invalid ID (e.g., 0) is provided, validation will fail.</p><p><strong>Using </strong><code><strong>@RequestParam</strong></code><strong> for Query Parameters</strong></p><pre class=\"prettyprint linenums\">@GetMapping(\"/accounts\")\npublic String getAccount(@RequestParam(name = \"id\", required = false) Long id) {\n    return id != null ? \"Account ID: \" + id : \"No account ID provided\";\n}</pre><p>Request: <code>GET /accounts?id=123</code><br>Response: <code>\"Account ID: 123\"</code></p><p><strong>5. Best Practices for Using </strong><code><strong>@PathVariable</strong></code><strong> in Spring Boot</strong></p><ol><li><p>Always specify the path variable name explicitly using <code>@PathVariable(\"id\")</code>. This improves readability and prevents potential mismatches.</p></li><li><p>Use appropriate data types for path variables, such as <code>Long</code> or <code>String</code>, to match expected values.</p></li><li><p>Use <code>@RequestParam</code> for optional values and <code>@PathVariable</code> for required identifiers.</p></li><li><p>Validate incoming path variables using annotations like <code>@Min(1)</code> to prevent errors.</p></li><li><p>Avoid exposing sensitive information in the URL path. Use query parameters or request bodies where appropriate.</p></li></ol>",
                "answers": [
                    "<p>Add @RequestParam long accountId argument to the update() handler method</p>",
                    "<p>Add @PathVariable long accountId argument to the update() handler method</p>",
                    "<p>Add @RequestParam(\"id\") String accountId argument to the update() handler method</p>",
                    "<p>Add @PathVariable(\"id\") String accountId argument to the update() handler method</p>"
                ]
            },
            "correct_response": [
                "d"
            ],
            "section": "Spring MVC",
            "question_plain": "@PutMapping(\"/accounts/{id}\")\npublic void update(){}Which option is a valid way to retrieve the account id?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571879,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>Which statement describes the @AfterReturning advice type?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring AOP Documentation](https://docs.spring.io/spring-framework/reference/core/aop.html)",
                    "[Baeldung - Guide to Spring AOP](https://www.baeldung.com/spring-aop)"
                ],
                "feedbacks": [
                    "<p>The <code>@AfterReturning</code> advice <strong>does not handle exceptions</strong>. It only executes <strong>after</strong> a method successfully returns. If an exception is thrown, <code>@AfterReturning</code> <strong>will not be triggered</strong>.</p>",
                    "<p>The <code>@AfterReturning</code> advice <strong>executes only when the target method completes successfully</strong>. If an exception is thrown, this advice <strong>does not run</strong>.</p><p>Example:</p><pre class=\"prettyprint linenums\">@Aspect\n@Component\npublic class LoggingAspect {\n\n    @AfterReturning(pointcut = \"execution(* com.example.service.*.*(..))\", returning = \"result\")\n    public void logAfterReturning(Object result) {\n        System.out.println(\"Method executed successfully. Result: \" + result);\n    }\n}\n</pre><ul><li><p>If the method completes successfully, this advice runs.</p></li><li><p>If an exception is thrown, this advice <strong>does not execute</strong>.</p></li></ul>",
                    "<p><code>@AfterReturning</code> <strong>does not</strong> control whether the method executes or not. It <strong>only runs after the method completes</strong> successfully.</p><p><br></p><p>If you need control over whether a method executes, use <code><strong>@Around</strong></code><strong> advice</strong> instead.</p>",
                    "<p>This is incorrect because <code>@AfterReturning</code> <strong>only executes if the method completes successfully</strong>. If an exception occurs, it does <strong>not</strong> run.</p><p><br></p><p>If you need advice that runs regardless of whether the method succeeds or fails, use <code><strong>@After</strong></code><strong> advice</strong> instead.</p>"
                ],
                "explanation": "<p>Spring AOP (<strong>Aspect-Oriented Programming</strong>) allows defining <strong>cross-cutting concerns</strong> like logging, security, or transactions <strong>separately from business logic</strong>.</p><p><strong>What is </strong><code><strong>@AfterReturning</strong></code><strong> Advice?</strong></p><p><code>@AfterReturning</code> is an <strong>AspectJ annotation</strong> in Spring AOP that allows running advice <strong>after</strong> a method completes successfully.</p><p><strong>Key Features of </strong><code><strong>@AfterReturning</strong></code><strong> Advice:</strong></p><ol><li><p>It <strong>only executes if the method returns normally</strong> (without throwing an exception).</p></li><li><p>It can <strong>access the returned value</strong> using the <code>returning</code> attribute.</p></li><li><p>It is commonly used for <strong>logging, modifying return values, or triggering additional logic</strong>.</p></li></ol><p><strong>4. Practical Developer Guide for Using </strong><code><strong>@AfterReturning</strong></code><strong> Advice</strong></p><p><strong>1. Basic Example of </strong><code><strong>@AfterReturning</strong></code><strong> Advice</strong></p><p>This advice runs <strong>after</strong> a successful method execution and logs the return value.</p><pre class=\"prettyprint linenums\">@Aspect\n@Component\npublic class LoggingAspect {\n\n    @AfterReturning(pointcut = \"execution(* com.example.service.UserService.getUser(..))\", returning = \"result\")\n    public void logAfterReturning(Object result) {\n        System.out.println(\"Method executed successfully. Result: \" + result);\n    }\n}</pre><p>If <code>getUser()</code> returns <code>\"John Doe\"</code>, the output will be:</p><pre class=\"prettyprint linenums\">Method executed successfully. Result: John Doe</pre><p><strong>2. Using </strong><code><strong>@AfterReturning</strong></code><strong> to Modify Return Values</strong></p><p>You can modify the returned value <strong>before the method result is passed back</strong>.</p><pre class=\"prettyprint linenums\">@Aspect\n@Component\npublic class ModifyReturnAspect {\n\n    @AfterReturning(pointcut = \"execution(* com.example.service.UserService.getUserName(..))\", returning = \"result\")\n    public void modifyReturnValue(JoinPoint joinPoint, String result) {\n        result = result.toUpperCase();\n        System.out.println(\"Modified Return Value: \" + result);\n    }\n}\n</pre><p>If <code>getUserName()</code> originally returns <code>\"john\"</code>, the modified output will be:</p><pre class=\"prettyprint linenums\">Modified Return Value: JOHN</pre><p><strong>3. Handling Methods with Specific Return Types</strong></p><p>If you only want the advice to execute for methods returning a specific type (e.g., <code>String</code>), specify it in the <code>returning</code> attribute.</p><pre class=\"prettyprint linenums\">@AfterReturning(pointcut = \"execution(* com.example.service.UserService.*(..))\", returning = \"result\")\npublic void logStringReturnMethods(String result) {\n    System.out.println(\"Method returned a String: \" + result);\n}\n</pre><p><strong>5. Alternative AOP Advice Types and When to Use Them</strong></p><ul><li><p><code><strong>@Before</strong></code>: Runs <strong>before</strong> a method executes.</p></li><li><p><code><strong>@AfterReturning</strong></code>: Runs <strong>after</strong> a method returns successfully.</p></li><li><p><code><strong>@AfterThrowing</strong></code>: Runs <strong>if a method throws an exception</strong>.</p></li><li><p><code><strong>@After</strong></code>: Runs <strong>after a method completes (whether successful or failed)</strong>.</p></li><li><p><code><strong>@Around</strong></code>: Runs <strong>before and after</strong> the method, allowing control over execution.</p></li></ul><p>Example comparison of <code><strong>@AfterReturning</strong></code> vs. <code><strong>@AfterThrowing</strong></code>:</p><pre class=\"prettyprint linenums\">@AfterReturning(pointcut = \"execution(* com.example.service.PaymentService.processPayment(..))\", returning = \"result\")\npublic void logSuccess(String result) {\n    System.out.println(\"Payment processed successfully: \" + result);\n}\n\n@AfterThrowing(pointcut = \"execution(* com.example.service.PaymentService.processPayment(..))\", throwing = \"ex\")\npublic void logFailure(Exception ex) {\n    System.out.println(\"Payment failed. Exception: \" + ex.getMessage());\n}\n</pre><ul><li><p>If <code>processPayment()</code> succeeds, the <code>@AfterReturning</code> advice runs.</p></li><li><p>If <code>processPayment()</code> throws an exception, the <code>@AfterThrowing</code> advice runs instead.</p></li></ul><p><strong>6. Best Practices for Using </strong><code><strong>@AfterReturning</strong></code><strong> in Spring AOP</strong></p><ol><li><p><strong>Use </strong><code><strong>@AfterReturning</strong></code><strong> for logging, caching, or modifying return values</strong>.</p></li><li><p><strong>Avoid using it for exception handling</strong> – use <code>@AfterThrowing</code> instead.</p></li><li><p><strong>Minimize business logic in AOP</strong> – keep cross-cutting concerns separate from core functionality.</p></li><li><p><strong>Use specific pointcuts</strong> to avoid running advice on unnecessary methods.</p></li><li><p><strong>Ensure compatibility with Spring Boot Auto-Configuration</strong> – use <code>@Component</code> or declare aspects in <code>@Configuration</code>.</p></li></ol>",
                "answers": [
                    "<p>Typically used to prevent any exception thrown by the advised method, from propagating up the call-stack</p>",
                    "<p>The advice is invoked only if the method returns successfully but not if it throws an exception</p>",
                    "<p>The advice has complete control over the method invocation; it could even prevent the method from being called at all</p>",
                    "<p>The @AfterReturning advice allows behavior to be added after a method returns even if it throws an exception</p>"
                ]
            },
            "correct_response": [
                "b"
            ],
            "section": "Spring AOP",
            "question_plain": "Which statement describes the @AfterReturning advice type?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571881,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>Which option is true about use of mocks in a Spring Boot web slice test?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Testing Documentation](https://docs.spring.io/spring-boot/docs/current/reference/html/features.html#features.testing)",
                    "[Testing Spring Boot Applications: Best Practices and Frameworks](https://medium.com/simform-engineering/testing-spring-boot-applications-best-practices-and-frameworks-6294e1068516)"
                ],
                "feedbacks": [
                    "<p>Spring Boot <strong>allows replacing existing beans with mock beans</strong> using <code>@MockBean</code>. Even if a real bean is present in the test context, <code>@MockBean</code> <strong>overrides</strong> it with a mock.</p>",
                    "<p><code>@Mock</code> comes from <strong>Mockito</strong>, but it does <strong>not</strong> integrate with the Spring ApplicationContext. To mock Spring-managed beans, <code>@MockBean</code> should be used instead.</p>",
                    "<p><code>@MockBean</code> is the <strong>correct way</strong> to mock a Spring-managed bean in <strong>Spring Boot web slice tests</strong>. It ensures that a mock is registered in the Spring ApplicationContext, allowing the test to work with a <strong>mocked dependency</strong>.</p><p><br></p><p>Example:</p><pre class=\"prettyprint linenums\">@WebMvcTest(MyController.class)\npublic class MyControllerTest {\n\n    @Autowired\n    private MockMvc mockMvc;\n\n    @MockBean\n    private MyService myService;\n\n    @Test\n    void testControllerMethod() throws Exception {\n        when(myService.getData()).thenReturn(\"Mocked Data\");\n\n        mockMvc.perform(get(\"/data\"))\n            .andExpect(status().isOk())\n            .andExpect(content().string(\"Mocked Data\"));\n    }\n}</pre><p>Here, <code>@MockBean</code> <strong>mocks</strong> <code>MyService</code> in the <strong>test context</strong>, ensuring the controller is tested in isolation.</p>",
                    "<p>This is incorrect because <strong>Spring Boot supports mocking in web slice tests</strong> using <code>@MockBean</code>.</p>"
                ],
                "explanation": "<p>Spring Boot provides <strong>slice testing</strong> to focus on specific parts of an application, such as <strong>controllers</strong> (<code>@WebMvcTest</code>) or <strong>repositories</strong> (<code>@DataJpaTest</code>).</p><p>When testing <strong>Spring MVC controllers</strong>, <strong>mocking dependencies</strong> ensures the test <strong>only verifies the controller logic</strong> without involving actual service or repository layers.</p><p><strong>How </strong><code><strong>@MockBean</strong></code><strong> Works in Spring Boot Tests</strong></p><ul><li><p><strong>Replaces the actual Spring-managed bean</strong> in the test context with a <strong>Mockito mock</strong>.</p></li><li><p>Ensures <strong>isolation</strong> of the component under test.</p></li><li><p>Works with <strong>WebMvcTest, DataJpaTest, and other slice tests</strong>.</p></li></ul><p>Example:</p><pre class=\"prettyprint linenums\">@WebMvcTest(UserController.class)\npublic class UserControllerTest {\n\n    @Autowired\n    private MockMvc mockMvc;\n\n    @MockBean\n    private UserService userService;\n\n    @Test\n    void testGetUser() throws Exception {\n        when(userService.getUserName()).thenReturn(\"John Doe\");\n\n        mockMvc.perform(get(\"/user\"))\n            .andExpect(status().isOk())\n            .andExpect(content().string(\"John Doe\"));\n    }\n}\n</pre><ul><li><p>The <code>UserService</code> bean is <strong>mocked</strong> using <code>@MockBean</code>.</p></li><li><p>The controller <strong>does not use the actual UserService</strong>, ensuring <strong>only the controller logic is tested</strong>.</p></li></ul><p><strong>4. Practical Developer Guide for Using Mocks in Spring Boot Web Slice Tests</strong></p><p><strong>1. Use </strong><code><strong>@WebMvcTest</strong></code><strong> for Isolated Controller Testing</strong></p><p><code>@WebMvcTest</code> loads <strong>only the web layer</strong>, excluding services, repositories, and other components.</p><pre class=\"prettyprint linenums\">@WebMvcTest(MyController.class)\npublic class MyControllerTest {\n\n    @Autowired\n    private MockMvc mockMvc;\n\n    @MockBean\n    private MyService myService;\n\n    @Test\n    void testGetData() throws Exception {\n        when(myService.getData()).thenReturn(\"Test Data\");\n\n        mockMvc.perform(get(\"/data\"))\n            .andExpect(status().isOk())\n            .andExpect(content().string(\"Test Data\"));\n    }\n}\n</pre><p><strong>2. Use </strong><code><strong>@MockBean</strong></code><strong> to Replace Service Beans</strong></p><p>If a controller depends on a service, mock the service using <code>@MockBean</code>.</p><pre class=\"prettyprint linenums\">@WebMvcTest(MyController.class)\npublic class MyControllerTest {\n\n    @MockBean\n    private MyService myService;\n}\n</pre><p>This <strong>prevents database access</strong> and ensures the test only evaluates the <strong>controller logic</strong>.</p><p><strong>3. Difference Between </strong><code><strong>@MockBean</strong></code><strong> and </strong><code><strong>@Mock</strong></code></p><ul><li><p><code>@MockBean</code> integrates <strong>with the Spring Context</strong>.</p></li><li><p><code>@Mock</code> is a <strong>pure Mockito annotation</strong> that does <strong>not</strong> interact with Spring.</p></li><li><p><strong>Use </strong><code><strong>@MockBean</strong></code><strong> for Spring components</strong> and <code>@Mock</code> for <strong>regular Java objects</strong>.</p></li></ul><p>Example:</p><pre class=\"prettyprint linenums\">@Mock\nprivate MyService mockService;  // NOT registered in Spring context\n\n@MockBean\nprivate MyRepository mockRepository;  // REGISTERED in Spring context</pre><p><strong>4. Avoid Using </strong><code><strong>@SpringBootTest</strong></code><strong> for Web Slice Tests</strong></p><ul><li><p><code>@SpringBootTest</code> loads the <strong>entire application context</strong>, making tests <strong>slower</strong>.</p></li><li><p>Instead, prefer <code><strong>@WebMvcTest</strong></code><strong> for controllers</strong> and <code><strong>@DataJpaTest</strong></code><strong> for repositories</strong>.</p></li></ul><p><strong>5. Best Practices for Mocking in Spring Boot Web Slice Tests</strong></p><ol><li><p><strong>Use </strong><code><strong>@MockBean</strong></code><strong> instead of </strong><code><strong>@Mock</strong></code><strong> for Spring-managed components</strong>.</p></li><li><p><strong>Prefer </strong><code><strong>@WebMvcTest</strong></code><strong> for testing controllers in isolation</strong>.</p></li><li><p><strong>Mock dependencies instead of loading full services or databases</strong>.</p></li><li><p><strong>Keep tests independent</strong> – do not rely on a database connection or external APIs.</p></li><li><p><strong>Use </strong><code><strong>MockMvc</strong></code><strong> to simulate HTTP requests</strong> in controller tests.</p></li></ol>",
                "answers": [
                    "<p>If a Spring bean already exists in the web slice test context, it cannot be mocked</p>",
                    "<p>Mocking a Spring Bean requires annotating it with @Mock annotation</p>",
                    "<p>Mocking a Spring Bean requires annotating it with @MockBean annotation</p>",
                    "<p>Mocks cannot be used in a Spring Boot web slice test</p>"
                ]
            },
            "correct_response": [
                "c"
            ],
            "section": "Testing",
            "question_plain": "Which option is true about use of mocks in a Spring Boot web slice test?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571883,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements about pointcut expressions are true?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring AOP Documentation](https://docs.spring.io/spring-framework/reference/core/aop.html)",
                    "[Spring Boot AOP Best Practices](https://www.baeldung.com/spring-aop-annotation)"
                ],
                "feedbacks": [
                    "<p><code>\"A pointcut expression will throw an exception if no methods are matched.\"</code><br>If a pointcut expression <strong>matches no methods</strong>, no exception is thrown. Instead, the aspect will simply <strong>not be applied</strong> to any methods.</p>",
                    "<p>Pointcut expressions <strong>can</strong> specify method parameters using wildcard (<code>*</code>), specific types, or <code>..</code> (any number of parameters).<br>Example:</p><pre class=\"prettyprint linenums\">@Pointcut(\"execution(* com.example.service.*.*(String))\")  // Matches methods with a single String parameter</pre>",
                    "<p>Pointcuts can target <strong>methods with specific annotations</strong> using <code>@annotation</code>.<br>Example:</p><pre class=\"prettyprint linenums\">@Pointcut(\"@annotation(org.springframework.transaction.annotation.Transactional)\")\npublic void transactionalMethods() {}\n</pre><p>This pointcut <strong>matches methods annotated with </strong><code><strong>@Transactional</strong></code>.</p>",
                    "<p>Pointcut expressions <strong>support wildcards</strong> for method names using <code>*</code>.<br>Example:</p><pre class=\"prettyprint linenums\">@Pointcut(\"execution(* com.example.service.UserService.*(..))\")  // Matches all methods in UserService</pre><p>The wildcard <code>*</code> allows matching <strong>any method name</strong>.</p>",
                    "<p>Pointcut expressions support <strong>logical operators</strong>:</p><ul><li><p><code>&amp;&amp;</code> (and) – Requires both conditions to be true.</p></li><li><p><code>||</code> (or) – Matches if <strong>either</strong> condition is true.</p></li><li><p><code>!</code> (not) – Negates the condition.</p></li></ul><p>Example:</p><pre class=\"prettyprint linenums\">@Pointcut(\"execution(* com.example.service.*.*(..)) &amp;&amp; @annotation(org.springframework.transaction.annotation.Transactional)\")\npublic void transactionalServiceMethods() {}</pre><p>This matches <strong>all service methods</strong> that are <strong>also annotated with </strong><code><strong>@Transactional</strong></code>.</p>"
                ],
                "explanation": "<p>Spring AOP (<strong>Aspect-Oriented Programming</strong>) allows <strong>separating cross-cutting concerns</strong> such as logging, security, and transactions from business logic. <strong>Pointcut expressions</strong> define <strong>where</strong> an aspect should be applied.</p><p><strong>Key Components of a Pointcut Expression</strong></p><ol><li><p><strong>Execution Pointcut (</strong><code><strong>execution</strong></code><strong>)</strong> – Matches method executions.</p><pre class=\"prettyprint linenums\">@Pointcut(\"execution(* com.example.service.*.*(..))\")\n</pre><p>This matches <strong>all methods</strong> in the <code>service</code> package.</p></li><li><p><strong>Annotation-Based Pointcut (</strong><code><strong>@annotation</strong></code><strong>)</strong> – Matches methods annotated with a specific annotation.</p><pre class=\"prettyprint linenums\">@Pointcut(\"@annotation(org.springframework.transaction.annotation.Transactional)\")\n</pre></li><li><p><strong>Within a Class (</strong><code><strong>within</strong></code><strong>)</strong> – Matches all methods within a class.</p><pre class=\"prettyprint linenums\">@Pointcut(\"within(com.example.service.UserService)\")\n</pre><p>This applies to all methods in <code>UserService</code>.</p></li><li><p><strong>Logical Operators in Pointcuts</strong></p><ul><li><p><code>&amp;&amp;</code> (and): Both conditions must be true.</p></li><li><p><code>||</code> (or): At least one condition must be true.</p></li><li><p><code>!</code> (not): Excludes certain join points.</p></li></ul><p>Example:</p><pre class=\"prettyprint linenums\">@Pointcut(\"execution(* com.example.service.*.*(..)) &amp;&amp; !execution(* com.example.service.AdminService.*(..))\")\n</pre><p>This applies to all service methods <strong>except</strong> those in <code>AdminService</code>.</p></li></ol><p><strong>4. Practical Developer Guide for Using Pointcut Expressions in Spring Boot</strong></p><p><strong>1. Applying AOP to Specific Methods</strong></p><p>Example of an aspect that logs execution of service methods:</p><pre class=\"prettyprint linenums\">@Aspect\n@Component\npublic class LoggingAspect {\n\n    @Pointcut(\"execution(* com.example.service.*.*(..))\")\n    public void serviceMethods() {}\n\n    @Before(\"serviceMethods()\")\n    public void logBefore(JoinPoint joinPoint) {\n        System.out.println(\"Executing method: \" + joinPoint.getSignature().getName());\n    }\n}\n</pre><p>This logs <strong>all method calls</strong> in the <code>service</code> package.</p><p><strong>2. Applying AOP Only to Annotated Methods</strong></p><p>Example of applying advice <strong>only to transactional methods</strong>:</p><pre class=\"prettyprint linenums\">@Pointcut(\"@annotation(org.springframework.transaction.annotation.Transactional)\")\npublic void transactionalMethods() {}\n\n@AfterReturning(\"transactionalMethods()\")\npublic void afterTransaction() {\n    System.out.println(\"Transaction committed.\");\n}\n</pre><p><strong>3. Combining Multiple Pointcuts</strong></p><p>Applying AOP only to <strong>public methods</strong> that <strong>return a </strong><code><strong>String</strong></code>:</p><pre class=\"prettyprint linenums\">@Pointcut(\"execution(public String com.example..*(..))\")\npublic void publicStringMethods() {}\n</pre><p><strong>4. Excluding Certain Methods</strong></p><p>Ignore <strong>admin-related methods</strong> in logging:</p><pre class=\"prettyprint linenums\">@Pointcut(\"execution(* com.example.service.*.*(..)) &amp;&amp; !execution(* com.example.service.AdminService.*(..))\")\npublic void nonAdminMethods() {}\n</pre><p><strong>5. Best Practices for Using Pointcut Expressions in Spring AOP</strong></p><ol><li><p><strong>Use </strong><code><strong>@annotation</strong></code><strong> for specific behaviors</strong> like security (<code>@Secured</code>), transactions (<code>@Transactional</code>), or caching (<code>@Cacheable</code>).</p></li><li><p><strong>Avoid overusing execution-based pointcuts</strong> (<code>execution(* *..*(..))</code>), as they can affect many methods unintentionally.</p></li><li><p><strong>Use logical operators</strong> to refine pointcuts, such as <code>&amp;&amp;</code>, <code>||</code>, and <code>!</code>.</p></li><li><p><strong>Validate your pointcut expressions</strong> by logging matched join points using <code>joinPoint.getSignature()</code>.</p></li><li><p><strong>Combine </strong><code><strong>within()</strong></code><strong> and </strong><code><strong>execution()</strong></code> for better specificity.</p></li></ol>",
                "answers": [
                    "<p>A pointcut expression will throw exception if no methods are matched</p>",
                    "<p> A pointcut expression cannot specify the type of parameters</p>",
                    "<p>A pointcut expression can be used to select join points which have been annotated with a specific annotation</p>",
                    "<p>A pointcut expression cannot have a wildcard for a method name</p>",
                    "<p>A pointcut expression can include operators such as the following: &amp;&amp; (and), || (or), ! (not)</p>"
                ]
            },
            "correct_response": [
                "c",
                "e"
            ],
            "section": "Spring AOP",
            "question_plain": "Which two statements about pointcut expressions are true?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571885,
            "assessment_type": "multiple-choice",
            "prompt": {
                "question": "<p>Which dependency enables an automatic restart of the application when code changes during development of a Spring Boot application?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot DevTools Documentation](https://docs.spring.io/spring-boot/docs/current/reference/html/using.html#using.devtools)",
                    "[Baeldung - Guide to Spring Boot DevTools](https://www.baeldung.com/spring-boot-devtools)"
                ],
                "feedbacks": [
                    "<p>Spring Boot Initializr is a <strong>web-based tool</strong> used to generate Spring Boot projects with dependencies. It does <strong>not</strong> provide auto-restart functionality.</p>",
                    "<p>There is no dependency called <code><strong>spring-boot-starter-devtools</strong></code>.</p><p>The correct dependency is <code><strong>spring-boot-devtools</strong></code>.</p>",
                    "<p>There is <strong>no</strong> dependency named <code>spring-boot-restart</code>.</p>",
                    "<ul><li><p><code>spring-boot-devtools</code> is the <strong>correct</strong> dependency that provides <strong>automatic restart, live reload, and development-time optimizations</strong>.</p></li><li><p>Adding this dependency allows Spring Boot to <strong>detect changes and restart the application automatically</strong>.</p></li></ul><p>To use it, add this dependency in <code>pom.xml</code>:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-devtools&lt;/artifactId&gt;\n    &lt;scope&gt;runtime&lt;/scope&gt;\n    &lt;optional&gt;true&lt;/optional&gt;\n&lt;/dependency&gt;</pre>"
                ],
                "explanation": "<p>Spring Boot DevTools is a <strong>development-time</strong> module that enhances the <strong>developer experience</strong> by enabling:</p><ol><li><p><strong>Automatic restart</strong> – Restarts the application when code changes.</p></li><li><p><strong>Live reload</strong> – Reloads HTML, CSS, and JavaScript without restarting the application.</p></li><li><p><strong>Cache disabling</strong> – Disables caching for templates like Thymeleaf, FreeMarker, and Velocity.</p></li><li><p><strong>Remote Development Support</strong> – Supports remote deployment for cloud-based development.</p></li></ol><p><strong>4. Practical Developer Guide for Using Spring Boot DevTools</strong></p><p><strong>1. Adding DevTools to a Spring Boot Project</strong></p><p>Include the following dependency in <code>pom.xml</code>:</p><pre class=\"prettyprint linenums\">&lt;dependency&gt;\n    &lt;groupId&gt;org.springframework.boot&lt;/groupId&gt;\n    &lt;artifactId&gt;spring-boot-devtools&lt;/artifactId&gt;\n    &lt;scope&gt;runtime&lt;/scope&gt;\n    &lt;optional&gt;true&lt;/optional&gt;\n&lt;/dependency&gt;</pre><p>The <code><strong>optional=true</strong></code> ensures that DevTools is <strong>not included in production builds</strong>.</p><p><strong>2. Enabling LiveReload in IntelliJ and VS Code</strong></p><ul><li><p><strong>For IntelliJ IDEA</strong>:</p><ul><li><p>Go to <strong>Settings → Build, Execution, Deployment → Compiler</strong></p></li><li><p>Enable <strong>\"Build project automatically\"</strong></p></li><li><p>Open <strong>Registry (</strong><code><strong>Ctrl + Shift + A</strong></code><strong>)</strong> and enable <code>compiler.automake.allow.when.app.running</code></p></li></ul></li><li><p><strong>For VS Code</strong>:</p><ul><li><p>Install the <strong>LiveReload</strong> extension and activate it.</p></li></ul></li></ul><p><strong>3. Configuring DevTools in </strong><code><strong>application.properties</strong></code></p><ul><li><p><strong>Disable DevTools Restart for Static Files</strong></p><pre class=\"prettyprint linenums\">spring.devtools.restart.exclude=static/**,public/**</pre></li><li><p><strong>Enable Remote Restart (For Cloud Development)</strong></p><pre class=\"prettyprint linenums\">spring.devtools.remote.secret=mySecretKey</pre></li></ul><p><strong>5. Best Practices for Using Spring Boot DevTools</strong></p><ol><li><p><strong>Use </strong><code><strong>spring-boot-devtools</strong></code><strong> only for local development.</strong></p></li><li><p><strong>Do not include DevTools in production builds.</strong></p></li><li><p><strong>Disable caching for templates in </strong><code><strong>application.properties</strong></code><strong> for real-time changes.</strong></p></li><li><p><strong>Use LiveReload to update frontend files without restarting the server.</strong></p></li><li><p><strong>Avoid using DevTools with Dockerized applications</strong> (use JRebel instead).</p></li></ol>",
                "answers": [
                    "<p>spring-boot-initializr</p>",
                    "<p>spring-boot-starter-devtools</p>",
                    "<p>spring-boot-restart</p>",
                    "<p>spring-boot-devtools</p>"
                ]
            },
            "correct_response": [
                "d"
            ],
            "section": "Spring Boot",
            "question_plain": "Which dependency enables an automatic restart of the application when code changes during development of a Spring Boot application?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571887,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which two statements are true about @Controller annotated classes?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Baeldung - Guide to Spring Controllers](https://www.baeldung.com/spring-controllers)",
                    "[Spring MVC Controllers Documentation](https://docs.spring.io/spring-framework/docs/3.2.x/spring-framework-reference/html/mvc.html)"
                ],
                "feedbacks": [
                    "<p>The <code>@Controller</code> annotation is a <strong>stereotype annotation</strong> in Spring, just like <code>@Component</code>. It serves as a specialization of <code>@Component</code>, meaning Spring automatically detects classes annotated with <code>@Controller</code> during component scanning and registers them as Spring beans. This enables them to participate in dependency injection and request handling within a Spring MVC application.</p><p>Example:</p><pre class=\"prettyprint linenums\">@Controller\npublic class HomeController {\n    @GetMapping(\"/\")\n    public String home() {\n        return \"index\"; // Returns the view name\n    }\n}</pre>",
                    "<p>The statement that classes must be annotated with <code>@EnableMvcMappings</code> to be discovered via component scanning is incorrect because <code>@EnableMvcMappings</code> does not exist in Spring MVC. In Spring Boot, controllers are <strong>automatically</strong> discovered through component scanning, and <code>@ComponentScan</code> ensures this detection.</p>",
                    "<p>The claim that <code>@Controller</code> is interchangeable with <code>@RestController</code> without extra code changes is incorrect. While both annotations allow request handling, <code>@RestController</code> is <strong>not</strong> a direct replacement for <code>@Controller</code>. The <code>@RestController</code> annotation is effectively a combination of <code>@Controller</code> and <code>@ResponseBody</code>, meaning all methods return <strong>JSON</strong> responses by default rather than resolving to a view. If a method inside an <code>@Controller</code> class returns raw data instead of a view, it must be explicitly annotated with <code>@ResponseBody</code>.</p><p>Example difference:</p><pre class=\"prettyprint linenums\">@Controller\npublic class WebController {\n    @GetMapping(\"/hello\")\n    public String hello() {\n        return \"hello\";  // Resolves to a view template\n    }\n}\n\n@RestController\npublic class ApiController {\n    @GetMapping(\"/hello\")\n    public String hello() {\n        return \"Hello, World!\";  // Returns JSON response\n    }\n}</pre>",
                    "<p>Classes annotated with <code>@Controller</code> are <strong>eligible to handle HTTP requests</strong> in a Spring MVC application. They define request mappings such as <code>@GetMapping</code>, <code>@PostMapping</code>, and <code>@RequestMapping</code> to manage incoming requests and determine appropriate responses. These controllers handle web requests by returning <strong>views</strong> or <strong>data responses</strong> if explicitly instructed.</p><p>Example:</p><pre class=\"prettyprint linenums\">@Controller\npublic class MyController {\n    @GetMapping(\"/welcome\")\n    public String welcome() {\n        return \"welcome\"; // Returns the view \"welcome.html\"\n    }\n}</pre>",
                    "<p>The claim that <code>@Controller</code> annotated classes can <strong>only render views</strong> is incorrect. While <code>@Controller</code> is typically used for <strong>view rendering</strong>, it can also return <strong>JSON responses</strong> when <code>@ResponseBody</code> is applied at the method level. However, <code>@RestController</code> is preferred for RESTful applications since it <strong>implicitly applies </strong><code><strong>@ResponseBody</strong></code><strong> to all methods</strong> in the class.</p><p>Example of <code>@Controller</code> returning JSON:</p><pre class=\"prettyprint linenums\">@Controller\npublic class ApiController {\n    @GetMapping(\"/json\")\n    @ResponseBody\n    public String getJson() {\n        return \"This is JSON response\";\n    }\n}</pre>"
                ],
                "explanation": "<p><strong>What is </strong><code><strong>@Controller</strong></code><strong>?</strong></p><p>The <code>@Controller</code> annotation is a <strong>Spring MVC annotation</strong> used to define a class as a Spring controller. It enables the class to handle <strong>web requests</strong> and return <strong>views</strong> or responses. This annotation allows the class to participate in Spring’s <strong>component scanning</strong>, ensuring that it is automatically registered as a bean in the application context.</p><p><strong>Differences Between </strong><code><strong>@Controller</strong></code><strong> and </strong><code><strong>@RestController</strong></code></p><ul><li><p><code>@Controller</code> is used for <strong>handling web views</strong>, typically returning HTML pages.</p></li><li><p><code>@RestController</code> is designed for <strong>RESTful APIs</strong>, returning JSON or other serialized data.</p></li><li><p><code>@RestController</code> is equivalent to <code>@Controller</code> + <code>@ResponseBody</code>, meaning all methods return raw responses instead of resolving views.</p></li></ul><p><strong>4. Practical Developer Guide for Using </strong><code><strong>@Controller</strong></code></p><p><strong>Using </strong><code><strong>@Controller</strong></code><strong> to Return a View</strong></p><p>This example demonstrates how <code>@Controller</code> returns an HTML page using Thymeleaf.</p><pre class=\"prettyprint linenums\">@Controller\npublic class HomeController {\n    @GetMapping(\"/\")\n    public String home() {\n        return \"home\";  // Returns the \"home.html\" view\n    }\n}\n</pre><p>The corresponding Thymeleaf template (<code>home.html</code> in <code>src/main/resources/templates</code>) might look like this:</p><pre class=\"prettyprint linenums\">&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;&lt;title&gt;Home&lt;/title&gt;&lt;/head&gt;\n&lt;body&gt;\n    &lt;h1&gt;Welcome to Spring Boot&lt;/h1&gt;\n&lt;/body&gt;\n&lt;/html&gt;</pre><p><strong>Returning JSON with </strong><code><strong>@ResponseBody</strong></code><strong> in </strong><code><strong>@Controller</strong></code></p><p>If a controller method needs to return a JSON response, it must be annotated with <code>@ResponseBody</code>:</p><pre class=\"prettyprint linenums\">@Controller\npublic class ApiController {\n    @GetMapping(\"/api/message\")\n    @ResponseBody\n    public String getMessage() {\n        return \"Hello, JSON response!\";\n    }\n}\n</pre><p>When calling <code>/api/message</code>, the response will be:</p><pre class=\"prettyprint linenums\">\"Hello, JSON response!\"</pre><p><strong>Using </strong><code><strong>@RestController</strong></code><strong> for APIs</strong></p><p>If all methods in a class should return JSON responses, <code>@RestController</code> should be used instead of <code>@Controller</code>:</p><pre class=\"prettyprint linenums\">@RestController\npublic class ApiController {\n    @GetMapping(\"/api/data\")\n    public Map&lt;String, String&gt; getData() {\n        return Map.of(\"message\", \"Hello, World!\");\n    }\n}\n</pre><p>Calling <code>/api/data</code> will return:</p><pre class=\"prettyprint linenums\">json\nCopyEdit{ \"message\": \"Hello, World!\" }\n</pre><p><strong>Using </strong><code><strong>@ControllerAdvice</strong></code><strong> for Global Error Handling</strong></p><p>To centralize error handling for controllers, <code>@ControllerAdvice</code> can be used:</p><pre class=\"prettyprint linenums\">@ControllerAdvice\npublic class GlobalExceptionHandler {\n\n    @ExceptionHandler(Exception.class)\n    public String handleError(Model model, Exception ex) {\n        model.addAttribute(\"message\", ex.getMessage());\n        return \"error\"; // Returns error.html\n    }\n}\n</pre><p><strong>5. Best Practices for Using </strong><code><strong>@Controller</strong></code><strong> in Spring MVC</strong></p><ul><li><p>Use <code>@Controller</code> for applications that <strong>render views</strong> rather than returning JSON responses.</p></li><li><p>Use <code>@RestController</code> instead of <code>@Controller + @ResponseBody</code> for REST APIs to simplify the code.</p></li><li><p>Avoid returning raw data in <code>@Controller</code> unless <code>@ResponseBody</code> is explicitly used.</p></li><li><p>Use <code>@ControllerAdvice</code> for handling global exceptions in web applications.</p></li><li><p>Ensure that <code>@Controller</code> classes are properly discovered by enabling <strong>component scanning</strong>, which is enabled by default in Spring Boot.</p></li></ul>",
                "answers": [
                    "<p>The @Controller annotation is a stereotype annotation like @Component</p>",
                    "<p>The classes must be annotated together with @EnableMvcMappings to be discovered via component scanning</p>",
                    "<p>@Controller is interchangeable with @RestController with no extra code changes for the methods inside the class</p>",
                    "<p>The classes are eligible for handling requests in Spring MVC</p>",
                    "<p>The @Controller annotated classes can only render views</p>"
                ]
            },
            "correct_response": [
                "a",
                "d"
            ],
            "section": "Spring MVC",
            "question_plain": "Which two statements are true about @Controller annotated classes?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571889,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>What two options are auto-configured Spring Boot Actuator HealthIndicators?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Boot Actuator: Production-ready Features](https://docs.spring.io/spring-boot/docs/2.2.x/reference/html/production-ready-features.html)",
                    "[Creating Custom HealthIndicators in Spring Boot](https://www.baeldung.com/spring-boot-health-indicators)"
                ],
                "feedbacks": [
                    "<p><code>OktaHealthIndicator</code> is not an auto-configured HealthIndicator in Spring Boot. Okta is an identity management service, and Spring Boot does not include a default HealthIndicator for it.</p>",
                    "<p><code>DynamoDBHealthIndicator</code> is not among Spring Boot's auto-configured HealthIndicators. While DynamoDB is a widely used NoSQL database service by AWS, Spring Boot does not provide a default HealthIndicator for it.</p>",
                    "<p><code>DataSourceHealthIndicator</code> is an auto-configured HealthIndicator in Spring Boot. It checks the availability of a <code>DataSource</code> by attempting to obtain a connection, ensuring that the database is accessible.</p>",
                    "<ul><li><p><code>GoogleCloudDataStoreHealthIndicator</code> is not an auto-configured HealthIndicator in Spring Boot. Google Cloud Datastore is a NoSQL document database, but Spring Boot does not include a built-in HealthIndicator for it.</p></li></ul>",
                    "<p><code>RabbitHealthIndicator</code> is another auto-configured HealthIndicator provided by Spring Boot. It verifies the status of a RabbitMQ server, ensuring that the messaging service is operational.</p>"
                ],
                "explanation": "<p><strong>What are HealthIndicators?</strong></p><p>In Spring Boot Actuator, <strong>HealthIndicators</strong> are components that provide health information about various parts of the application. They contribute to the overall health status exposed by the <code>/health</code> endpoint, which can be used by monitoring systems to check the application's status.</p><p><strong>Auto-configured HealthIndicators:</strong></p><p>Spring Boot automatically configures several HealthIndicators based on the presence of specific components or configurations in the application. Some of these include:</p><ul><li><p><strong>CassandraHealthIndicator:</strong> Checks if a Cassandra database is up.</p></li><li><p><strong>CouchbaseHealthIndicator:</strong> Verifies the status of a Couchbase cluster.</p></li><li><p><strong>DiskSpaceHealthIndicator:</strong> Monitors available disk space.</p></li><li><p><strong>DataSourceHealthIndicator:</strong> Ensures that a connection to the <code>DataSource</code> can be obtained.</p></li><li><p><strong>ElasticsearchRestHealthIndicator:</strong> Checks the status of an Elasticsearch cluster.</p></li><li><p><strong>JmsHealthIndicator:</strong> Verifies that a JMS broker is operational.</p></li><li><p><strong>MailHealthIndicator:</strong> Ensures that a mail server is reachable.</p></li><li><p><strong>MongoHealthIndicator:</strong> Checks the status of a MongoDB database.</p></li><li><p><strong>Neo4jHealthIndicator:</strong> Verifies the status of a Neo4j database.</p></li><li><p><strong>RabbitHealthIndicator:</strong> Ensures that a RabbitMQ server is up and running.</p></li><li><p><strong>RedisHealthIndicator:</strong> Checks the availability of a Redis server.</p></li><li><p><strong>SolrHealthIndicator:</strong> Verifies the status of a Solr server.</p></li></ul><p>These indicators are auto-configured when the corresponding technology is present and configured in the application context.</p><p><strong>Custom HealthIndicators:</strong></p><p>Developers can also create custom HealthIndicators by implementing the <code>HealthIndicator</code> interface. This is useful for adding health checks specific to the application's requirements, such as verifying the availability of an external service or checking custom application metrics.</p><p><strong>Example of a Custom HealthIndicator:</strong></p><pre class=\"prettyprint linenums\">java\nCopyEditimport org.springframework.boot.actuate.health.Health;\nimport org.springframework.boot.actuate.health.HealthIndicator;\nimport org.springframework.stereotype.Component;\n\n@Component\npublic class MyCustomHealthIndicator implements HealthIndicator {\n\n    @Override\n    public Health health() {\n        // Perform custom health check logic\n        boolean isHealthy = checkSomeServiceHealth();\n        if (isHealthy) {\n            return Health.up().withDetail(\"service\", \"Available\").build();\n        } else {\n            return Health.down().withDetail(\"service\", \"Unavailable\").build();\n        }\n    }\n\n    private boolean checkSomeServiceHealth() {\n        // Custom logic to check health\n        return true; // or false based on health check\n    }\n}\n</pre><p>In this example, <code>MyCustomHealthIndicator</code> implements the <code>HealthIndicator</code> interface and overrides the <code>health()</code> method to provide custom health check logic. Depending on the result of the health check, it returns a status of <code>UP</code> or <code>DOWN</code> with additional details.</p><p><strong>Accessing Health Information:</strong></p><p>The health information provided by all configured HealthIndicators is aggregated and exposed through the <code>/actuator/health</code> endpoint. By default, this endpoint returns a simple status (e.g., <code>{\"status\":\"UP\"}</code>), but it can be configured to show detailed information, including the status of each individual HealthIndicator.</p><p><strong>Configuring Health Endpoint Details:</strong></p><p>To include detailed health information in the <code>/actuator/health</code> endpoint, set the following property in your <code>application.properties</code> or <code>application.yml</code>:</p><pre class=\"prettyprint linenums\">properties\nCopyEditmanagement.endpoint.health.show-details=always\n</pre><p>This configuration ensures that the health endpoint provides comprehensive information about each HealthIndicator's status, which can be useful for monitoring and debugging purposes.</p><p><strong>Security Considerations:</strong></p><p>Exposing detailed health information can reveal sensitive details about the application's infrastructure. It's important to secure the <code>/actuator/health</code> endpoint appropriately, especially in production environments. You can configure access controls using Spring Security to restrict who can view detailed health information.</p><p><strong>Example of Securing the Health Endpoint:</strong></p><pre class=\"prettyprint linenums\">java\nCopyEditimport org.springframework.context.annotation.Configuration;\nimport org.springframework.security.config.annotation.web.builders.HttpSecurity;\nimport org.springframework.security.config.annotation.web.configuration.WebSecurityConfigurerAdapter;\n\n@Configuration\npublic class SecurityConfig extends WebSecurityConfigurerAdapter {\n\n    @Override\n    protected void configure(HttpSecurity http) throws Exception {\n        http.authorizeRequests()\n            .antMatchers(\"/actuator/health\").permitAll() // Allow public access to health status\n            .antMatchers(\"/actuator/health/**\").hasRole(\"ADMIN\") // Restrict detailed health info\n            .anyRequest().authenticated()\n            .and()\n            .httpBasic();\n    }\n}\n</pre><p>In this configuration, the general health status is publicly accessible, but detailed health information requires administrative privileges.</p><p><strong>Conclusion:</strong></p><p>Spring Boot Actuator's HealthIndicators provide a flexible and extensible way to monitor the health of various application components. By leveraging auto-configured and custom HealthIndicators, developers can ensure that their applications are running smoothly and are properly monitored. Remember to secure the health endpoints appropriately to prevent unauthorized access to sensitive information.</p>",
                "answers": [
                    "<p>OktaHealthIndicator</p>",
                    "<p>DynamoDBHealthIndicator</p>",
                    "<p>DataSourceHealthIndicator</p>",
                    "<p>GoogleCloudDataStoreHealthIndicator</p>",
                    "<p>RabbitHealthIndicator</p>"
                ]
            },
            "correct_response": [
                "c",
                "e"
            ],
            "section": "Spring Boot Actuator",
            "question_plain": "What two options are auto-configured Spring Boot Actuator HealthIndicators?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571891,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>Which three types can be used as @Controller method arguments?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Method Arguments](https://docs.spring.io/spring-framework/reference/web/webmvc/mvc-controller/ann-methods/arguments.html)"
                ],
                "feedbacks": [
                    "<p><code>Session</code> is not a recognized parameter type for Spring MVC controller methods. The correct type to use is <code>HttpSession</code>.</p>",
                    "<p><code>HttpSession</code> represents the HTTP session associated with a user's request. In a Spring MVC controller method, you can include <code>HttpSession</code> as a parameter to access session attributes or manage session-related information.​</p><p>Example:</p><pre class=\"prettyprint linenums\">@Controller\n  public class MyController {\n      @RequestMapping(\"/session\")\n      public String handleSession(HttpSession session) {\n          // Access session attributes\n          return \"sessionView\";\n      }\n  }</pre>",
                    "<p><code>Principal</code> represents the currently authenticated user. By including <code>Principal</code> as a method parameter, you can access the user's authentication details, such as their username.</p><p>Example:</p><pre class=\"prettyprint linenums\">@Controller\n  public class MyController {\n      @RequestMapping(\"/user\")\n      public String handleUser(Principal principal) {\n          String username = principal.getName();\n          // Use username in your logic\n          return \"userView\";\n      }\n  }</pre>",
                    "<p><code>Request</code> is not a standard parameter type in Spring MVC. To access the HTTP request, you should use <code>HttpServletRequest</code>.</p>",
                    "<p><code>Language</code> is not a recognized parameter type in Spring MVC. To handle language preferences, you should use the <code>Locale</code> parameter.</p>",
                    "<p><code>Locale</code> indicates the current locale of the request, which can be useful for internationalization purposes. Including <code>Locale</code> as a parameter allows you to tailor responses based on the user's locale.​</p><p>Example:</p><pre class=\"prettyprint linenums\">@Controller\n  public class MyController {\n      @RequestMapping(\"/greet\")\n      public String greetUser(Locale locale) {\n          // Use locale to determine language-specific greetings\n          return \"greetingView\";\n      }\n  }</pre>"
                ],
                "explanation": "<p>In Spring MVC, controller methods can accept a variety of parameters that provide access to different aspects of the HTTP request and application context. These parameters allow developers to handle requests more effectively and access necessary data without manual extraction.​</p><p><strong>Commonly Used Method Arguments:</strong></p><ul><li><p><strong>HttpServletRequest / HttpServletResponse:</strong><br>Direct access to the HTTP request and response objects. These can be used to read request data or manipulate the response directly.​</p><p>Example:</p></li></ul><pre class=\"prettyprint linenums\">@Controller\n  public class MyController {\n      @RequestMapping(\"/handle\")\n      public void handleRequest(HttpServletRequest request, HttpServletResponse response) {\n          // Process request and response\n      }\n  }\n</pre><p><br></p><ul><li><p><strong>Model / ModelMap:</strong><br>Used to pass data from the controller to the view. By adding attributes to the model, you can make them accessible in the view layer.​</p><p>Example:</p></li></ul><pre class=\"prettyprint linenums\">@Controller\n  public class MyController {\n      @RequestMapping(\"/welcome\")\n      public String welcomeUser(Model model) {\n          model.addAttribute(\"message\", \"Welcome to Spring MVC!\");\n          return \"welcomeView\";\n      }\n  }\n</pre><p><br></p><ul><li><p><strong>@RequestParam:</strong><br>Retrieves query parameters from the request URL. This is useful for extracting simple parameters from the request.​</p><p>Example:</p></li></ul><pre class=\"prettyprint linenums\">@Controller\n  public class MyController {\n      @RequestMapping(\"/search\")\n      public String search(@RequestParam(\"q\") String query) {\n          // Use query parameter in search logic\n          return \"searchResultsView\";\n      }\n  }\n</pre><p><br></p><ul><li><p><strong>@PathVariable:</strong><br>Extracts values from URI templates, allowing for dynamic URL handling.​</p><p>Example:</p></li></ul><pre class=\"prettyprint linenums\">@Controller\n  public class MyController {\n      @RequestMapping(\"/user/{id}\")\n      public String getUser(@PathVariable(\"id\") Long userId) {\n          // Use userId to retrieve user information\n          return \"userView\";\n      }\n  }\n</pre><p><br></p><ul><li><p><strong>@RequestBody:</strong><br>Maps the body of the HTTP request to a Java object, typically used for handling JSON or XML payloads.​</p><p>Example:</p></li></ul><pre class=\"prettyprint linenums\">@Controller\n  public class MyController {\n      @RequestMapping(value = \"/update\", method = RequestMethod.POST)\n      public String updateUser(@RequestBody User user) {\n          // Process user data\n          return \"updateConfirmationView\";\n      }\n  }\n</pre><p><br></p><ul><li><p><strong>@RequestHeader:</strong><br>Accesses specific HTTP headers from the request.​</p><p>Example:</p></li></ul><pre class=\"prettyprint linenums\">@Controller\n  public class MyController {\n      @RequestMapping(\"/headers\")\n      public String handleHeaders(@RequestHeader(\"User-Agent\") String userAgent) {\n          // Use userAgent information\n          return \"headerInfoView\";\n      }\n  }\n</pre><p><br></p><ul><li><p><strong>@CookieValue:</strong><br>Retrieves the value of a specific cookie from the request.​</p><p>Example:</p></li></ul><pre class=\"prettyprint linenums\">@Controller\n  public class MyController {\n      @RequestMapping(\"/cookies\")\n      public String handleCookies(@CookieValue(\"sessionId\") String sessionId) {\n          // Use sessionId cookie value\n          return \"cookieInfoView\";\n      }\n  }\n</pre><p><br></p><p>These method arguments provide a flexible and declarative way to access various parts of the HTTP request and application context, simplifying request handling in Spring MVC controllers.​</p>",
                "answers": [
                    "<p>Session</p>",
                    "<p>HttpSession</p>",
                    "<p>Principal</p>",
                    "<p>Request</p>",
                    "<p>Language</p>",
                    "<p>Locale</p>"
                ]
            },
            "correct_response": [
                "b",
                "c",
                "f"
            ],
            "section": "Spring MVC",
            "question_plain": "Which three types can be used as @Controller method arguments?",
            "related_lectures": []
        },
        {
            "_class": "assessment",
            "id": 136571893,
            "assessment_type": "multi-select",
            "prompt": {
                "question": "<p>What are the types of dependency injection supported by Spring IoC Container?</p>",
                "relatedLectureIds": [],
                "links": [
                    "[Spring Framework Documentation - Dependency Injection](https://docs.spring.io/spring-framework/reference/core/beans/dependencies/factory-collaborators.html)",
                    "[Baeldung - Guide to Dependency Injection in Spring](https://www.baeldung.com/spring-dependency-injection)"
                ],
                "feedbacks": [
                    "<p><strong>Field-based injection</strong> is performed by annotating class fields with <code>@Autowired</code>. Spring automatically injects dependencies without requiring explicit setters or constructors.</p><ul><li><p>It is <strong>not recommended</strong> for testability and maintainability since dependencies are injected via reflection and cannot be easily mocked in unit tests.</p></li></ul><p>Example:</p><pre class=\"prettyprint linenums\">@Component\npublic class OrderService {\n\n    @Autowired\n    private PaymentService paymentService; // Field Injection\n\n    public void processOrder() {\n        paymentService.pay();\n    }\n}</pre><ul><li><p><strong>Downside:</strong> Field injection makes unit testing harder because dependencies cannot be easily replaced with mocks.</p></li></ul><p><br></p>",
                    "<p><strong>Constructor injection</strong> is the preferred method in Spring because it ensures immutability and makes dependencies explicit. It is also best for <strong>mandatory dependencies</strong>.</p><p>Example:</p><pre class=\"prettyprint linenums\">t@Component\npublic class OrderService {\n\n    private final PaymentService paymentService;\n\n    @Autowired\n    public OrderService(PaymentService paymentService) { // Constructor Injection\n        this.paymentService = paymentService;\n    }\n\n    public void processOrder() {\n        paymentService.pay();\n    }\n}\n</pre><ul><li><p><strong>Advantages:</strong> Constructor injection is <strong>test-friendly</strong>, avoids reflection, and allows using <code>final</code> fields for dependencies.</p></li><li><p><strong>Spring Boot Feature:</strong> If a class has only <strong>one constructor</strong>, <code>@Autowired</code> can be omitted, and Spring automatically injects dependencies.</p></li></ul>",
                    "<p><strong>Interface-based injection</strong> is <strong>not supported by Spring IoC Container</strong>. While some frameworks use it (like Java EE's CDI), Spring does not support this type of injection. Instead, Spring relies on <strong>constructor-based</strong> or <strong>setter-based</strong> injection for dependency management.</p>",
                    "<p><strong>Setter injection</strong> is performed by annotating setter methods with <code>@Autowired</code>. It is useful for <strong>optional dependencies</strong>.</p><p>Example:</p><pre class=\"prettyprint linenums\">@Component\npublic class OrderService {\n\n    private PaymentService paymentService;\n\n    @Autowired\n    public void setPaymentService(PaymentService paymentService) { // Setter Injection\n        this.paymentService = paymentService;\n    }\n\n    public void processOrder() {\n        paymentService.pay();\n    }\n}\n</pre><ul><li><p><strong>Advantages:</strong> Setter injection allows optional dependencies and enables modifying dependencies at runtime.</p></li><li><p><strong>Downside:</strong> The object is <strong>not immutable</strong>, and dependencies might not be injected properly if the setter is not called.</p></li></ul>"
                ],
                "explanation": "<p><strong>What is Dependency Injection (DI)?</strong></p><p>Dependency Injection (DI) is a design pattern where objects receive their dependencies instead of creating them. Spring IoC (Inversion of Control) Container manages dependencies and injects them automatically.</p><p><strong>Types of Dependency Injection in Spring</strong></p><ol><li><p><strong>Field-based Injection (</strong><code><strong>@Autowired</strong></code><strong> on fields)</strong></p><ul><li><p><strong>Pros:</strong> Simple and requires less boilerplate code.</p></li><li><p><strong>Cons:</strong> Difficult to test, not ideal for large applications.</p></li></ul></li><li><p><strong>Constructor Injection (</strong><code><strong>@Autowired</strong></code><strong> on constructor)</strong></p><ul><li><p><strong>Pros:</strong> Recommended approach, ensures immutability, test-friendly.</p></li><li><p><strong>Cons:</strong> More boilerplate code for optional dependencies.</p></li></ul></li><li><p><strong>Setter Injection (</strong><code><strong>@Autowired</strong></code><strong> on setter methods)</strong></p><ul><li><p><strong>Pros:</strong> Flexible, allows optional dependencies.</p></li><li><p><strong>Cons:</strong> Can lead to partially initialized objects if setters are not called properly.</p></li></ul></li></ol><p><strong>Which Injection Type Should You Use?</strong></p><ul><li><p><strong>Use constructor injection</strong> for <strong>mandatory dependencies</strong>.</p></li><li><p><strong>Use setter injection</strong> for <strong>optional dependencies</strong>.</p></li><li><p><strong>Avoid field injection</strong> unless necessary.</p></li></ul><p><strong>4. Practical Developer Guide for Using Dependency Injection in Spring</strong></p><p><strong>Using Constructor Injection in a Spring Boot Service</strong></p><pre class=\"prettyprint linenums\">@Service\npublic class OrderService {\n\n    private final PaymentService paymentService;\n\n    public OrderService(PaymentService paymentService) {\n        this.paymentService = paymentService;\n    }\n\n    public void processOrder() {\n        paymentService.pay();\n    }\n}\n</pre><ul><li><p>Constructor injection is <strong>preferred</strong> because it allows <code>final</code> fields and ensures dependencies are always available.</p></li></ul><p><strong>Using Setter Injection for Optional Dependencies</strong></p><pre class=\"prettyprint linenums\">@Service\npublic class OrderService {\n\n    private PaymentService paymentService;\n\n    @Autowired\n    public void setPaymentService(PaymentService paymentService) {\n        this.paymentService = paymentService;\n    }\n\n    public void processOrder() {\n        if (paymentService != null) {\n            paymentService.pay();\n        }\n    }\n}\n</pre><ul><li><p>Setter injection is useful when the dependency is <strong>optional</strong> and may not always be available.</p></li></ul><p><strong>Using Field Injection (Not Recommended)</strong></p><pre class=\"prettyprint linenums\">@Service\npublic class OrderService {\n\n    @Autowired\n    private PaymentService paymentService;\n\n    public void processOrder() {\n        paymentService.pay();\n    }\n}\n</pre><ul><li><p>While field injection works, <strong>it is harder to test</strong> and should be avoided in favor of constructor injection.</p></li></ul><p><strong>Best Practices for Dependency Injection in Spring</strong></p><ol><li><p><strong>Favor constructor injection</strong> for required dependencies to improve testability and maintainability.</p></li><li><p><strong>Use setter injection</strong> only for optional dependencies.</p></li><li><p><strong>Avoid field injection</strong> to prevent issues with unit testing and immutability.</p></li><li><p><strong>Ensure proper dependency management</strong> using Spring’s <code>@Component</code>, <code>@Service</code>, and <code>@Repository</code> annotations.</p></li><li><p><strong>Use </strong><code><strong>@Primary</strong></code><strong> or </strong><code><strong>@Qualifier</strong></code> when multiple beans of the same type exist.</p></li></ol>",
                "answers": [
                    "<p>field-based injection</p>",
                    "<p>constructor injection</p>",
                    "<p>interface-based injection</p>",
                    "<p>setter injection</p>"
                ]
            },
            "correct_response": [
                "a",
                "b",
                "d"
            ],
            "section": "Spring Core",
            "question_plain": "What are the types of dependency injection supported by Spring IoC Container?",
            "related_lectures": []
        }
    ]
}